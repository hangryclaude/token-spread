[
  {
    "id": 309,
    "name": "langchain-core v3 streaming usage bridge silently zeros cache token details across all providers",
    "strictVerdict": "PASS_ABSOLUTE",
    "reasoning": "Re-fetched issue #37761 — title matches verbatim: \"_to_protocol_usage drops input_token_details in v3 streaming path, causing cache token counts to always be 0\", body confirms the repro (achunks_to_events dropping input_token_details). Re-fetched PR #38021 (merged 2026-06-16T14:04:56Z) — body confirms: \"the conversion to the protocol UsageInfo shape copied only the flat token counts\", fixed by threading nested dicts through _isolate_usage with a shallow copy. This is a pure client-side event-reassembly bug in how langchain-core normalizes provider-emitted usage chunks into its own internal v3 protocol schema — the provider request/response bytes are never touched, only what langchain's own downstream consumers (LangSmith, cost trackers) see reflected back. No token sequence, model, or reasoning amount changes on the wire.",
    "savings": "None claimed — accounting-integrity fix only. Every v3-streaming consumer across all chat-model providers (not just Anthropic) previously saw 0 cache tokens on every streamed response.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 144726 stars): 2026-08-21: Re-fetched issue #37761 (title exact match) and PR #38021 via gh CLI/API this session; confirmed merged state, read full PR body, and confirmed via `gh api .../pulls/38021/files` that test_compat_bridge.py contains test_isolate_usage_preserves_token_details.",
    "telemetrySignal": "Self-contained async repro in the issue body (fake_stream() yielding input_token_details={cache_read:90,...}); PR diff confirmed to add test_isolate_usage_preserves_token_details to tests/unit_tests/language_models/test_compat_bridge.py (renamed from test_to_protocol_usage_present/none).",
    "providers": [
      "langchain-ai/langchain"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched issue #37761 (title exact match) and PR #38021 via gh CLI/API this session; confirmed merged state, read full PR body, and confirmed via `gh api .../pulls/38021/files` that test_compat_bridge.py contains test_isolate_usage_preserves_token_details.",
    "stars": 144726,
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 310,
    "name": "ChatAnthropic extended-thinking output tokens missing from usage_metadata for both invoke and stream",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "The underlying phenomenon is real — re-fetched issue #39249, confirmed title \"Reasoning tokens not reported in `usage_metadata`\" and the MRE showing output_token_details.reasoning absent for both invoke and stream against claude-sonnet-5 with output_config effort=high. But the cited fix, PR #39255, is verifiably NOT what fixed it: `gh pr view 39255` returns `state:\"CLOSED\", mergedAt:null`, auto-closed by a github-actions bot with the message \"This PR has been automatically closed because you are not assigned to the linked issue... Opening a PR before assignment is discouraged and is not an indication that it will be accepted.\" The \"maintainer verification quote\" the candidate attributes to this PR (\"reproduced the original issue's script against a live API call... confirmed usage_metadata[\\\"output_token_details\\\"][\\\"reasoning\\\"] is now populated\") is literally the unassigned PR author's own \"How I verified this\" self-report inside that dead, unmerged PR — not a maintainer's confirmation of anything. I independently searched and found the issue was genuinely resolved by a different PR, #39590 (\"fix(anthropic): report reasoning tokens in usage metadata\", merged 2026-08-11T15:42:30Z, crediting co-author philemon-otera, not the #39255 author), which does contain a test named test_usage_metadata_reasoning_tokens. So a real PASS_ABSOLUTE-shaped fix exists in this repo for this exact issue, but the specific citation given by the candidate (PR #39255, \"maintainer verification\") fails re-verification and cannot be certified as stated.",
    "savings": "None claimed as savings — visibility fix for often-dominant thinking-token spend, if and when the real fix (PR #39590, not #39255) is what's being relied on.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 144726 stars): Re-fetched issue #39249 and PR #39255 via gh CLI this session — found #39255 state=CLOSED, mergedAt=null, auto-closed by bot before maintainer review. Cross-searched (gh api search/issues) and found the actual merged fix, PR #39590, merged 2026-08-11, containing test_usage_metadata_reasoning_tokens per `gh api .../pulls/39590/files`.",
    "telemetrySignal": "Miner's cited telemetry (test_usage_metadata_reasoning_tokens) does exist, but in PR #39590, not the cited #39255.",
    "providers": [
      "langchain-ai/langchain"
    ],
    "verifiedAgainst": "Re-fetched issue #39249 and PR #39255 via gh CLI this session — found #39255 state=CLOSED, mergedAt=null, auto-closed by bot before maintainer review. Cross-searched (gh api search/issues) and found the actual merged fix, PR #39590, merged 2026-08-11, containing test_usage_metadata_reasoning_tokens per `gh api .../pulls/39590/files`.",
    "stars": 144726,
    "trap": "A miner cited an unmerged, bot-closed PR as \"fixed by X, maintainer-verified\" when X never merged and the quote is the unassigned author's own self-report — always check pr view state/mergedAt, not just that the quoted text exists somewhere in the PR body."
  },
  {
    "id": 311,
    "name": "spring-ai Anthropic cache strategies never placed a breakpoint on tool-result messages, so every agentic tool round re-paid full price for the growing tool-result history",
    "strictVerdict": "PASS_METADATA",
    "reasoning": "Re-fetched issue #6261 — body confirms verbatim: \"each tool result is billed as uncached `input_tokens` on the round that introduces it... the growing tool-result prefix is not reliably written to / read from the cache across rounds\", and confirms none of the five AnthropicCacheStrategy values (NONE/TOOLS_ONLY/SYSTEM_ONLY/SYSTEM_AND_TOOLS/CONVERSATION_HISTORY) target MessageType.TOOL. Re-fetched PR #6268 body: \"place a cache_control breakpoint on the last block of the last tool result message of a request\" — a pure cache_control-placement fix, no change to tokenized content, model, or reasoning depth. GitHub reports PR #6268 as CLOSED/mergedAt:null, which looked suspicious, so I checked further: `gh api repos/.../compare/main...2433eecb09...` returns status \"behind\", confirming that commit (message: \"GH-6261: Cache tool result messages during tool-calling rounds / Closes #6261\") is already an ancestor of main — genuinely merged via Spring's non-GitHub-button squash workflow, corroborated by author sobychacko's own PR comment \"This PR has been merged upstream.\" Reporter's RC1 confirmation on the issue thread verified verbatim: \"I upgraded to RC1, which includes this fix, and verified that it works as advertised.\"",
    "savings": "Not dollar-quantified by the reporter; structurally, prior to the fix every tool-calling round paid full uncached input price for the entire accumulated tool-result history instead of ~90% cache-read discount. Unverified-by-me follow-on comment on the same issue (ssamir0602) says the identical gap remains open for the Bedrock-Anthropic path.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 9334 stars): 2026-08-21: Re-fetched issue #6261 and PR #6268 via gh CLI/API this session; confirmed PR body and issue-thread comments verbatim; used gh api compare to confirm the squash commit is on main despite GitHub's PR state showing CLOSED not MERGED.",
    "telemetrySignal": "Reporter states production traffic (\"We run an assistant on Spring AI + Anthropic\"), reproduced against 2.0.0-SNAPSHOT post-M8 with raw request-log cache_read/cache_creation/input tables, reporter-confirmed fixed on RC1.",
    "providers": [
      "spring-projects/spring-ai"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched issue #6261 and PR #6268 via gh CLI/API this session; confirmed PR body and issue-thread comments verbatim; used gh api compare to confirm the squash commit is on main despite GitHub's PR state showing CLOSED not MERGED.",
    "stars": 9334,
    "trap": "PR API state can read CLOSED/mergedAt:null for a change that is genuinely on main — Spring's contribution flow squash-merges outside GitHub's button. Don't fail evidence solely on raw PR state; check git ancestry.",
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 312,
    "name": "Vercel AI Gateway's Anthropic-compatible stream reports usage 0/0 on message_start while its own billing ledger stays correct",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "Re-fetched issue #17326 — body confirms verbatim: \"the gateway's own billing ledger (`GET /v1/generation`) has the right numbers\" and \"clients that snapshot usage at `message_start`... see `0/0` on every assistant turn\", with a concrete production trace (54/54 messages showing 0/0 while the ledger for the same generation id shows tokens_prompt:21635 etc.). Re-fetched PR #17534 body — root-cause quote matches verbatim: \"The Anthropic provider parsed upstream message_start usage but omitted it from response metadata.\" By the reporter's own account (and the issue's own reproduction), the actual billed/metered amount was correct throughout; only one echoed SSE field was wrong. This changes what metadata is relayed in one event, not the request sent to Anthropic, the model, or its reasoning — textbook PASS_ABSOLUTE. Caveat: as of this session all four cited PRs (#17531, #17532, #17533, #17534) show state=OPEN, mergedAt=null — none have actually landed yet, so \"fixed\" in the candidate's framing is premature; the verdict rests on the confirmed bug + confirmed-correct ledger, not on a shipped fix.",
    "savings": "None — pure reporting bug. The candidate also flags (unverified by me beyond noting it) that the same gateway 404s /v1/messages/count_tokens for non-Anthropic models, which Claude Code polls for context management (~53/day in the reporter's logs); I did not verify whether that starves any downstream compaction/thinking decision.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 26337 stars): 2026-08-21: Re-fetched issue #17326 and PRs #17531/17532/17533/17534 via gh CLI this session; confirmed quotes verbatim; confirmed all four PRs are still OPEN (not merged) as of today, contradicting the candidate's 'fixed' framing though not the underlying identity-preservation analysis.",
    "telemetrySignal": "Confirmed via gh api that PR #17531 touches packages/gateway/src/__fixtures__/issue-17326-anthropic-stream.chunks.txt and issue-17326-xai-stream.chunks.txt (recorded live SSE), matching the candidate's fixture-path description.",
    "providers": [
      "vercel/ai"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched issue #17326 and PRs #17531/17532/17533/17534 via gh CLI this session; confirmed quotes verbatim; confirmed all four PRs are still OPEN (not merged) as of today, contradicting the candidate's 'fixed' framing though not the underlying identity-preservation analysis.",
    "stars": 26337,
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "INSUFFICIENT_EVIDENCE",
      "why": "The miner's cited quotes check out verbatim as far as they went, but they stopped short of a decisive piece of evidence on the very PR they cited as the fix. PR #17534 carries its own automated review: \"Outcome: changes-required / Reproduction replay: still-reproduces — The exact original bug signal appeared when replayed against the pull request / Fixes issue: does-not-address.\" That directly contradicts treating this as a well-understood, cleanly-fixable metadata-echo bug. Further: issue #17326 is labeled factory-active/factory-automatic, and every comment plus five-plus fix-attempt PRs (#17531/17532/17533/17534/17535/17538, zero merged) come from one account (aayush-kapoor) in templated \"Bug Reproduction\"/\"Bugfix\" boilerplate — this reads as an internal automated bug-discovery/fix-agent pipeline rather than an organically human-vetted fix. Unlike the spring-ai entry, there is no independent human/maintainer confirmation that \"the gateway's billing ledger stays correct\" — that load-bearing claim originates solely from the original reporter's own issue text (author_association: NONE), never corroborated by anyone else. Star count 26337 confirmed exact, but with zero merged fixes and the leading fix attempt's own automated verification saying it doesn't work, I can't sustain a PASS at any tier pending an actual merge and independent confirmation. | Went beyond the entry's own check. `gh api issues/17326/timeline` surfaces two more cross-referenced PRs the entry never lists (#17535, #17538) -- six fix attempts total -- and as of today every one of the six is OPEN/mergedAt:null, and the issue itself is still open. Nothing has shipped; there is no technique in production to grade, only a live unpatched bug, so 'PASS_ABSOLUTE' asserts identity-preservation for an artifact that doesn't exist yet. Separately, the issue's own pasted evidence shows `\"stop_reason\": null` co-occurring with the usage:0/0 defect on all 54/54 sampled messages -- unexamined by the entry, and unlike entries 1/2 (isolated accounting bugs with no behavioral hook), a null stop_reason is exactly the kind of field an agent harness's continuation/retry/compaction logic could key on, making a 'different amount of thinking' plausible and unruled-out. Downgrade to INSUFFICIENT_EVIDENCE."
    }
  },
  {
    "id": 313,
    "name": "llama_index cache_idx stamps cache_control on every flattened text block, blowing past Anthropic's 4-breakpoint limit and hard-failing the request",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched issue #20854 — body confirms the exact quoted API error verbatim: \"invalid_request_error: A maximum of 4 blocks with cache_control may be provided. Found 29.\", and root-causes it to blocks_to_anthropic_blocks() stamping cache_control on every block via global_cache_control with no cap. Re-fetched PR #20875 (merged 2026-03-09T10:49:09Z) — confirms the fix: \"Apply cache_control only to the last block of each message instead of every block. This follows Anthropic's recommended cache breakpoint pattern\", plus a new 7-test class TestCacheControlOnlyLastBlock including the exact 29-block scenario. I'm overruling the miner's own hedge (they proposed INSUFFICIENT_EVIDENCE, worried this doesn't map cleanly to the bar): the technique being evaluated is the post-fix behavior (cache_control confined to the last block per message), which is precisely the rubric's own worked example of PASS_METADATA (\"only a field the model never reads changed (cache_control placement...)\"). That the pre-fix bug caused an outright 400 rather than a silent cost change is irrelevant to identity-preservation — comparing the fixed technique (last-block-only cache_control) against a no-cache-control baseline, the tokenized content, model, and reasoning depth are unchanged in both cases; only an envelope annotation differs.",
    "savings": "None claimed; this is a reliability bug whose fix happens to be a clean cache_control-placement change.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 51790 stars): 2026-08-21: Re-fetched issue #20854 and PR #20875 via gh CLI/API this session; confirmed merged state, verbatim error-message quote, and PR body's description of the fix and test additions.",
    "telemetrySignal": "Reporter's own AgentWorkflow + output_cls + cache_idx repro producing a live 29-block API rejection; PR diff confirmed to add TestCacheControlOnlyLastBlock with 7 tests including the 29-block reproduction.",
    "providers": [
      "run-llama/llama_index"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched issue #20854 and PR #20875 via gh CLI/API this session; confirmed merged state, verbatim error-message quote, and PR body's description of the fix and test additions.",
    "stars": 51790,
    "crosscheckOverride": {
      "from": "PASS_METADATA",
      "to": "FAIL",
      "why": "Pulled the full utils.py diff for merged PR #20875 (the entry only checked the test file). In blocks_to_anthropic_beta_blocks, the pre-fix 'legacy tool_calls compatibility' block sat INSIDE the per-block `for block in blocks` loop, so for any message hitting the legacy kwargs['tool_calls'] path with N content blocks it re-appended the identical BetaToolUseBlockParam (same id/name/input) up to N times -- the append is gated only on 'not in unique_tool_calls', which that legacy branch never updates. The fix moves this block outside the loop so it runs once per message instead of N times. That is not a cache_control placement change: it changes the actual number and content of blocks sent to Anthropic for any caller on that legacy path, bundled into the exact same commit the entry scores as pure PASS_METADATA. The merged artifact does two things: (a) genuinely metadata-only cache_control-placement fix, and (b) a genuinely token-sequence-altering duplicate-content fix. Crediting the whole thing as PASS_METADATA is wrong -- clause 'does the model read a different sequence of tokens' is answered YES for part of what shipped in this PR. Automatic FAIL per the mechanism lens."
    }
  },
  {
    "id": 314,
    "name": "llama.cpp cache_prompt KV-reuse silently drops tool_calls vs full recompute (hybrid/recurrent models)",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "Re-fetched the README quote verbatim (tools/server/README.md line 581): \"Because (depending on the backend) the logits are not guaranteed to be bit-for-bit identical for different batch sizes (prompt processing vs. token generation) enabling this option can cause nondeterministic results. Default: true\" — real and accurate, but this is boilerplate about batch-size-dependent floating-point non-associativity common to essentially any batched inference engine, not evidence specific to this technique causing systematically different answers. More importantly, re-fetching issue #26817's full comment thread (which the candidate's evidence field did not quote) surfaces the reporter's own retraction, posted on the same issue: \"Correction to my own report after more controlled runs — the causal claim was too strong... A later run FAILED the light-control case with cache_prompt:false (the mode I claimed never fails)... cache_prompt:false failed 2 of 2 tool cases while default caching passed 3/3 — the exact mirror of the original observation. Revised characterization: for prompts near the call/don't-call boundary, tool-call emission is nondeterministic at temperature 0 on this stack, in both cache modes, with direction flipping between runs and across restarts. Cache mode changes which perturbation the request sees, but it is not the cause.\" This directly contradicts the load-bearing claim the candidate uses to justify FAIL (byte-identical request → different answer, caused specifically by cache reuse). The issue remains OPEN with no maintainer confirmation either way. Given the same reporter has since disowned the causal link between cache_prompt and the divergence, I cannot certify a confirmed FAIL here — this is genuinely unsettled.",
    "savings": "Skips reprocessing the matched prefix on every follow-up turn — llama.cpp's headline server speedup, on by default (cache_prompt:true).",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 125016 stars): 2026-08-21: Re-fetched issue #26817 (body + full comment thread) and tools/server/README.md via gh CLI/API this session; confirmed the README quote verbatim and discovered the reporter's own retraction comment, which the candidate's evidence field omitted.",
    "telemetrySignal": "Server response fields n_prompt_tokens_cache / cache_n report cache-served vs. recomputed prompt tokens (cited by candidate; not independently re-verified in source this session — not load-bearing for the verdict).",
    "providers": [
      "ggml-org/llama.cpp"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched issue #26817 (body + full comment thread) and tools/server/README.md via gh CLI/API this session; confirmed the README quote verbatim and discovered the reporter's own retraction comment, which the candidate's evidence field omitted.",
    "stars": 125016,
    "trap": "Candidate quoted only the original issue body and stopped there; the same issue's comment thread contains the reporter's own retraction of the exact causal claim being used as proof. Always read the full thread, not just the opening post, before certifying a 'reproduced' bug as FAIL-grade evidence."
  },
  {
    "id": 315,
    "name": "llama.cpp --cache-reuse: mid-sequence KV chunks spliced in via RoPE position shift, not recomputed",
    "strictVerdict": "FAIL",
    "reasoning": "Confirmed at tools/server/server-context.cpp:3147-3215. First the exact prefix is computed (n_past = get_common_prefix, ordinary/already-covered prefix caching). THEN, independently, lines 3167-3212 scan for further chunks matching at a DIFFERENT offset and splice them in: verbatim comment line 3167 'reuse chunks from the cached prompt by shifting their KV cache in the new position', and the shift itself at lines 3197-3200: 'const int64_t kv_shift = (int64_t) head_p - (int64_t) head_c; slot.mem.seq_rm(slot.id, head_p, head_c); slot.mem.seq_add(slot.id, head_c, head_c + n_match, kv_shift);' README.md:217 confirms: '--cache-reuse N | min chunk size to attempt reusing from the cache via KV shifting, requires prompt caching to be enabled'. The spliced KV values were computed by attention over a different left-context than the one they're relocated into; a RoPE position add only relabels position, it does not redo the attention pass against the new neighbors. That is a genuine approximation of the forward pass for the recycled span (and everything generated downstream of it) done specifically to avoid recomputation — an automatic-FAIL-adjacent context-editing move, not exact caching.",
    "savings": "Avoids reprocessing any matched chunk >= N tokens anywhere in the cached sequence, not just a leading prefix (useful when RAG/tool-context document order shuffles between requests).",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 125016 stars): 2026-08-21: Fetched full tools/server/server-context.cpp (5475 lines) and tools/server/README.md (2132 lines) via gh api raw content this session; grep+Read confirmed both quotes verbatim at the cited line numbers and read the surrounding 90-line mechanism block.",
    "telemetrySignal": "Server debug log line confirmed present: 'reusing chunk with size %zu, shifting KV cache [%zu, %zu) -> [%zu, %zu)' at server-context.cpp:3192.",
    "providers": [
      "ggml-org/llama.cpp"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full tools/server/server-context.cpp (5475 lines) and tools/server/README.md (2132 lines) via gh api raw content this session; grep+Read confirmed both quotes verbatim at the cited line numbers and read the surrounding 90-line mechanism block.",
    "stars": 125016
  },
  {
    "id": 316,
    "name": "Jan reorders post-tool-call messages to keep the llama.cpp prompt prefix byte-identical",
    "strictVerdict": "FAIL",
    "reasoning": "Confirmed at web-app/src/lib/custom-chat-transport.ts:511-529, verbatim: 'Prompt-cache stability: when a tool-call turn completes, the AI SDK stores the follow-up text in the SAME assistant UIMessage as the tool call, so `convertToModelMessages` renders `assistant(tool-call, text)` then `tool(result)`. But that turn was generated in two requests — the cache was seeded with `assistant(tool-call)` then `tool(result)`, with no text yet. Splitting restores the generated order `assistant(tool-call)` -> `tool(result)` -> `assistant(text)`, so the prefix stays byte-identical across turns and llama.cpp reuses the KV cache.' The function (line 530) only splits an existing message's parts into separately-ordered UIMessages — it drops nothing and adds nothing, it only serializes the same full content in the order it actually happened, which the same comment block notes is independently required for Claude API validity (tool_use/text interleaving is a 400 error there). Test file web-app/src/lib/prompt-cache-toolcall.test.ts:78 asserts `expect(prefix).toEqual(cachedPrompt)`, confirmed present. Zero net content change — pure client-side serialization correction.",
    "savings": "Preserves llama.cpp KV-cache hits across every tool-call turn instead of forcing a full-prefix recompute after each tool round-trip.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 44089 stars): 2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts (1675 lines) and web-app/src/lib/prompt-cache-toolcall.test.ts via gh api raw content this session; confirmed both the comment block and the `expect(prefix).toEqual(cachedPrompt)` assertion verbatim at the cited lines.",
    "telemetrySignal": "n/a — verified via source-level byte-equality unit test, not runtime telemetry.",
    "providers": [
      "janhq/jan"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts (1675 lines) and web-app/src/lib/prompt-cache-toolcall.test.ts via gh api raw content this session; confirmed both the comment block and the `expect(prefix).toEqual(cachedPrompt)` assertion verbatim at the cited lines.",
    "stars": 44089,
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "FAIL",
      "why": "Evidence is verbatim-accurate (fetched custom-chat-transport.ts, 1675 lines; the cited comment at 511-529 and the `expect(prefix).toEqual(cachedPrompt)` assertion at prompt-cache-toolcall.test.ts:78 both confirmed exactly) — this is not an evidence-forgery problem, it's a categorization problem. splitAssistantToolWaves() genuinely reorders content: WITHOUT the patch, convertToModelMessages renders `assistant(tool-call, text)` then `tool(result)` — text tokens BEFORE the tool-result tokens; WITH the patch, it's `assistant(tool-call)` -> `tool(result)` -> `assistant(text)` — text tokens AFTER. Same total content, provably different token order fed to the model on the very next request (confirmed by reading the function at lines 530-554 and cross-checking against the test's own three assertions about ordering). That is a direct 'yes' to the bar's question 1 ('does the model read a different sequence of tokens') under the plain reading: with-patch vs without-patch, the sequence differs. The candidate's own entry name says it plainly — 'reorders...messages' — which is a content-order change, not a metadata or scheduling change, and structurally close to 'context editing,' one of the rubric's explicit automatic-FAIL categories. The counter-argument the candidate leans on (this only restores the 'true' chronological generation order, so it's a correction not a change) is a real, non-frivolous argument — PASS_ABSOLUTE explicitly allows 'client-side correction' — but the rubric's bar is a literal token-sequence test with no carve-out for 'the reordering happens to be the more correct one.' Under 'default to downgrading when uncertain,' and given the mechanism is an unambiguous reorder (not zero-change), this should be FAIL, not PASS_ABSOLUTE. | The technique's own description is a reordering of content the model reads: default AI SDK conversion would emit assistant(tool-call,text) then tool(result); this code splits and re-emits assistant(tool-call) -> tool(result) -> assistant(text). That is a different token sequence (position of the text block moves relative to the tool-result block) on every turn where this fires, compared to what the naive/default rendering would send. The mechanism lens states this plainly: 'anything that reorders ... content the model reads is FAIL.' That the reordered version happens to reproduce a PRIOR turn's byte sequence (which is the entire cache-affinity rationale) doesn't make it 'zero request change' — it's a deliberate, systematic reordering baked into every tool-call turn, which is exactly the class of change PASS_ABSOLUTE ('nothing on the wire changed') excludes. Downgrade to FAIL."
    }
  },
  {
    "id": 317,
    "name": "Jan freezes MCP tool routing per-thread so the cached tool-schema prefix doesn't move",
    "strictVerdict": "FAIL",
    "reasoning": "The miner's framing (freezing = holding a value constant, opposite of slimming) does not survive reading what gets frozen. Confirmed at custom-chat-transport.ts:892-933: when routing is enabled, the tool list sent to the model is NOT the full available set — it's `mcpTools = await mcpOrchestrator.getRelevantTools(this.lastUserMessage, ...)`, a relevance/router-model-driven SUBSET selection (the full-set path `mcpService.getTools()` is only the else-branch fallback when routing is off, line 935). That subsetting is itself squarely the auto-FAIL 'tool-schema slimming' pattern. The 'freeze' (lines 730-734, verbatim: 'Smart tool routing selects tools from the latest user message, which would change the tool set (and thus the cached prompt prefix) every turn. Freeze the routed set for the thread's lifetime so the prefix stays stable; re-route only when the connected servers or disabled-tool set changes.') then locks that already-narrowed subset in place and explicitly does NOT refresh it when the conversation's topic drifts — only a server/disabled-tool-list change triggers re-routing (line 908 checks `frozenRoutedSig`, keyed on servers+disabled tools, not on message content). So for every turn after the first, the model reads a tool-schema block that is a stale, content-narrowed snapshot of turn 1's topic rather than the live-routed set the system's own logic would otherwise compute for the current message — a real difference in the sequence of tokens read, traded deliberately for cache stability. That is the auto-FAIL shape, not a metadata/scheduling no-op.",
    "savings": "Keeps the tool-schema portion of the prompt stable across a whole conversation thread, maximizing the prefix eligible for KV-cache reuse — at the cost of tool relevance drifting as the conversation moves on.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 44089 stars): 2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts (1675 lines) via gh api raw content this session; read lines 725-940 in full and confirmed the getRelevantTools call, the frozen-set comment, and the re-route trigger condition verbatim at the cited lines. Did not find/verify the candidate's cited 'custom-chat-transport-tool-freeze.test.ts' assertions beyond confirming the file exists and its test names (routes once and freezes / re-routes when disabled-tool set changes / re-routes when connected server set changes) — none of those tests exercise mid-thread topic drift, consistent with the freeze never re-evaluating on message content.",
    "telemetrySignal": "n/a — verified via source read, not runtime telemetry.",
    "providers": [
      "janhq/jan"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts (1675 lines) via gh api raw content this session; read lines 725-940 in full and confirmed the getRelevantTools call, the frozen-set comment, and the re-route trigger condition verbatim at the cited lines. Did not find/verify the candidate's cited 'custom-chat-transport-tool-freeze.test.ts' assertions beyond confirming the file exists and its test names (routes once and freezes / re-routes when disabled-tool set changes / re-routes when connected server set changes) — none of those tests exercise mid-thread topic drift, consistent with the freeze never re-evaluating on message content.",
    "stars": 44089
  },
  {
    "id": 318,
    "name": "Jan pins llama.cpp completions to a fixed server slot (id_slot=0) for cache affinity",
    "strictVerdict": "PASS_METADATA",
    "reasoning": "Client side confirmed verbatim at custom-chat-transport.ts:1184-1189: '// Pin chat to slot 0 so llama-server reuses this thread's cached KV prefix across turns; title generation uses the reserved background slot (RESERVED_BACKGROUND_SLOTS) and can't evict it.' with `mergedParams.id_slot = 0`. Server side, get_available_slot() (server-context.cpp:1500-1563) confirms `id_slot` is routing-only: line 1505-1506, 'if a specific slot is requested, use it (still goes through cache update logic below)' — `ret` is set to the requested slot immediately and, when the requested id_slot is set, the subsequent LCP-similarity loop is restricted via a `continue` guard (line 1518: `if (task.id_slot != -1 && slot.id != task.id_slot) continue;`) to that one slot, so it cannot select a different slot; the LRU fallback (line 1566) is skipped entirely since `ret != nullptr`. Note: the candidate's claim that this 'short-circuits its LCP/LRU slot-selection logic entirely' overstates it slightly — the server's own comment says the cache-update bookkeeping still runs for that slot — but the practical effect (final slot forced to the id given, regardless of similarity/recency) is confirmed, and in no case is `id_slot` fed into the tokenized prompt or read by the model. It is pure completions-API routing metadata.",
    "savings": "Removes a source of unforced cache misses in multi-slot deployments (`-np N`) by guaranteeing turn-to-turn slot affinity for a given conversation.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 44089 stars): 2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts and full tools/server/server-context.cpp via gh api raw content this session; confirmed the client-side comment/field verbatim at lines 1184-1189, and read get_available_slot() in full (lines 1500-1610) plus common/common.h:678 (`slot_prompt_similarity = 0.1f` default, meaning the similarity loop does run by default even when id_slot is pinned, just restricted to that one slot).",
    "telemetrySignal": "n/a — verified via source cross-reference between client (id_slot=0) and server slot-selection logic, not runtime telemetry.",
    "providers": [
      "janhq/jan"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full web-app/src/lib/custom-chat-transport.ts and full tools/server/server-context.cpp via gh api raw content this session; confirmed the client-side comment/field verbatim at lines 1184-1189, and read get_available_slot() in full (lines 1500-1610) plus common/common.h:678 (`slot_prompt_similarity = 0.1f` default, meaning the similarity loop does run by default even when id_slot is pinned, just restricted to that one slot).",
    "stars": 44089,
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 319,
    "name": "llama-server /slots/{id}?action=save|restore — byte-exact KV-cache persistence across process restarts",
    "strictVerdict": "PASS_ABSOLUTE",
    "reasoning": "Confirmed at tools/server/server-context.cpp:2475-2574. Save handler (SERVER_TASK_TYPE_SLOT_SAVE, lines 2475-2524): 'const size_t nwrite = llama_state_seq_save_file(ctx_tgt, filepath.c_str(), slot->id, reinterpret_cast<const llama_token *>(packed.data()), packed.size() / sizeof(llama_token));' — a byte-exact tensor serialize. Restore handler (SERVER_TASK_TYPE_SLOT_RESTORE, lines 2525-2574) calls the matching `llama_state_seq_load_file`, then only accepts it after `if (!restored.validate(ctx_tgt)) { throw ...'Invalid tokens in slot save file'... }`, and installs it directly into `slot->prompt.tokens` — no lossy approximation, no re-tokenization. README.md confirms the operator-facing surface: line 221 '--slot-save-path PATH | path to save slot kv cache (default: disabled)', lines 1146/1166 describe the save/restore endpoints' `filename` param. This is an explicit, operator-triggered REST action performing an exact round-trip of the same KV state that ordinary in-memory prefix caching would already produce — it changes no tokens, no model, no reasoning depth, only whether that state survives a process restart.",
    "savings": "Avoids reprocessing a saved conversation's entire prompt history after a server restart/redeploy — restore time scales with file I/O, not with re-running the model over the prompt.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 125016 stars): 2026-08-21: Fetched full tools/server/server-context.cpp (5475 lines) and tools/server/README.md (2132 lines) via gh api raw content this session; read both handlers in full (lines 2475-2574) and confirmed both README lines verbatim.",
    "telemetrySignal": "Server task result includes n_tokens, n_bytes, t_ms for both save and restore, confirmed at lines 2520-2522.",
    "providers": [
      "ggml-org/llama.cpp"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full tools/server/server-context.cpp (5475 lines) and tools/server/README.md (2132 lines) via gh api raw content this session; read both handlers in full (lines 2475-2574) and confirmed both README lines verbatim.",
    "stars": 125016,
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 320,
    "name": "fail-closed spend-cap enforcement on Redis brown-out",
    "strictVerdict": "FAIL",
    "reasoning": "PR #12990 confirmed MERGED 2026-05-04T04:38:56Z via `gh pr view`. Body quote confirmed verbatim: 'check_rate_limit was failing open when Redis was unreachable...this means a user (or a script-driven user) on the affected slot can blast the LLM loop unmetered for the duration of the brown-out, bypassing the per-user daily/weekly USD caps.' Diff (`gh pr diff`) confirms the mechanism: `check_rate_limit` now raises `RateLimitUnavailable`, caught in routes.py and mapped to `raise HTTPException(status_code=503, detail='Rate limit service degraded, retry shortly', headers={'Retry-After': '30'})`, replacing the prior silent pass-through. On the allowed path (Redis healthy, or user under cap) the request sent to the model is byte-identical to before the fix — zero request change. On the denied path the request is blocked outright before it reaches the model (503, LLM never invoked) rather than degraded or altered — matching PASS_ABSOLUTE's 'client-side correction... outright blocking (not degrading)' shape. The PR body's own table, confirmed present, shows only the enforcement path was made fail-closed while four other Redis call sites (get_usage_status, record_cost_usage, release_reset_lock, increment_daily_reset_count) were deliberately left fail-open, matching the candidate's claim exactly.",
    "savings": "Author's dollar-risk estimate: $50-500 of LLM spend per affected user per ~5-minute Redis brown-out, previously uncapped for the outage duration; no aggregate production savings figure given, only the risk estimate plus new unit/route tests asserting the fail-closed 503 behavior.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 186720 stars): 2026-08-21: Fetched PR #12990 metadata via `gh pr view` (title, state, mergedAt, full body) and full diff via `gh pr diff` this session; confirmed the body quote verbatim, confirmed merge state/date, and confirmed the RateLimitUnavailable exception, 503+Retry-After mapping, and fail-open/fail-closed tables in the actual diff of routes.py and rate_limit.py.",
    "telemetrySignal": "Cites a real GKE rolling-upgrade postmortem (Significant-Gravitas/AutoGPT_cloud_infrastructure#318) as the triggering incident; confirmed linked in the PR body. No aggregate production savings number given.",
    "providers": [
      "Significant-Gravitas/AutoGPT"
    ],
    "verifiedAgainst": "2026-08-21: Fetched PR #12990 metadata via `gh pr view` (title, state, mergedAt, full body) and full diff via `gh pr diff` this session; confirmed the body quote verbatim, confirmed merge state/date, and confirmed the RateLimitUnavailable exception, 503+Retry-After mapping, and fail-open/fail-closed tables in the actual diff of routes.py and rate_limit.py.",
    "stars": 186720,
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "FAIL",
      "why": "For the specific subset of requests that flip from allowed (old fail-open bug) to denied (new fail-closed behavior), the change is not 'zero request change': previously the model was invoked and processed the prompt; now it returns 503 before the LLM is ever called — zero tokens read, zero thinking happens, where nonzero happened before. That is a direct hit on the mechanism lens's own third clause ('does a different amount of thinking happen'). The entry's reasoning leans on a phrase — 'outright blocking (not degrading)' — as if it were a carve-out written into PASS_ABSOLUTE, but no such carve-out appears in the bar definition supplied here; 'pure accounting, procurement, client-side correction' describes fixes that leave behavior identical for all requests, not a policy change that newly blocks calls the system previously allowed through. This is a security/billing-guard fix with a genuine behavioral effect on denied traffic, not a caching-preservation technique with zero request change. Downgrade to FAIL."
    }
  },
  {
    "id": 321,
    "name": "prompt_cache_key scoped by chat/agent metadata instead of user+model",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "Re-fetched PR #14627 body/state directly. Verbatim: \"The final key sent to OpenAI is deterministic and scoped by agent + topic: lobe_{agentId}_{topicId}\" and \"Existing custom `prompt_cache_key` values from provider payload handlers are preserved and not overwritten.\" Quote checks out exactly as cited. Mechanistically this is a clean PASS_METADATA candidate — prompt_cache_key is an OpenAI routing field never tokenized/read by the model, so recomputing how the string is derived cannot change what tokens the model reads. BUT `gh pr view 14627 --repo lobehub/lobehub --json state` returns state=OPEN — this fix is unmerged. An open PR is a proposal, not a verified shipped behavior of the 81,908-star repo; it can be closed, revised, or abandoned before landing, and there is no evidence in main that this scoping is actually active today. The candidate itself disclosed the OPEN status. Capping at INSUFFICIENT_EVIDENCE rather than crediting a pass for a technique not confirmed live in production.",
    "savings": "Not quantified by submitter — framed as fixing cache-bucket ambiguity across concurrent delegated agents, not a dollar or percentage figure.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 81908 stars): 2026-08-21: Re-fetched via `gh pr view 14627 --repo lobehub/lobehub --json state,title,body,url` — confirmed OPEN/unmerged, quote verbatim present. Re-verified star count via `gh repo view lobehub/lobehub --json stargazerCount` = 81,908, exact match to claim.",
    "telemetrySignal": "None provided in the PR description; behavior is described narratively with manual test scenarios, no before/after measurement.",
    "providers": [
      "lobehub/lobehub"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched via `gh pr view 14627 --repo lobehub/lobehub --json state,title,body,url` — confirmed OPEN/unmerged, quote verbatim present. Re-verified star count via `gh repo view lobehub/lobehub --json stargazerCount` = 81,908, exact match to claim.",
    "stars": 81908
  },
  {
    "id": 322,
    "name": "tool-loop usage summation inflates stored context size, triggers premature compaction",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched issue #27031 body — verbatim quote confirmed present: \"5. Continue the chat: compaction fires far below the real threshold because `_exceeds_token_threshold` reads the inflated value.\" and the ~40k/80k/120k -> ~240k summation math, matching the candidate's citation exactly. The described mechanism triggers this register's automatic-FAIL category (context compaction/summarization firing on turns that never needed it, causing the model to read a shorter/lossily-summarized history than it otherwise would). Note on the candidate's own evidence field: it cites the issue as '(OPEN)' but `gh issue view 27031` shows state=CLOSED, stateReason=COMPLETED — a real discrepancy in the submission. However I chased this down: both PRs that claimed to fix it, #27032 and #27425 ('fix: context compaction reads last-call input, not additive usage total', fixes #27031), were each closed with mergedAt:null — never merged. Direct inspection of current HEAD (backend/open_webui/utils/response.py, middleware.py, context_compaction.py) confirms merge_usage() still additively sums input_tokens/output_tokens/total_tokens via USAGE_SUMMABLE_KEYS at four call sites in middleware.py, and _exceeds_token_threshold() still reads that same summed usage.get('input_tokens') value; no last_input_tokens/last_output_tokens carry-through field exists anywhere in normalize_usage(). So despite GitHub's issue-tracker metadata saying closed/completed, the bug is verified still live in current main — a stronger, more current finding than the candidate's own citation implied.",
    "savings": "None claimed — this is a metering bug that increases cost/degrades quality via unwanted compaction, not a savings technique; correctly self-identified by the submitter as the register's FAIL bucket.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 149488 stars): 2026-08-21: Fetched issue #27031 via `gh issue view 27031 --repo open-webui/open-webui --json title,state,stateReason,body,closedAt` (state CLOSED/COMPLETED, contradicting candidate's OPEN label). Fetched linked PRs #27032 and #27425 (`gh pr view ... --json state,mergedAt,body`) — both CLOSED, mergedAt:null. Fetched current backend/open_webui/utils/response.py, middleware.py, context_compaction.py via `gh api repos/.../contents/...` — confirmed additive summing and inflated-threshold-read code is unchanged/still present. Star count re-verified via `gh repo view` = 149,491 vs claimed 149,488 (normal live-count drift, not a discrepancy).",
    "telemetrySignal": "Reporter supplies exact summation arithmetic and a manual reproduction (enable compaction, 2-3 native tool calls, moderate context) rather than aggregate production metrics.",
    "providers": [
      "open-webui/open-webui"
    ],
    "verifiedAgainst": "2026-08-21: Fetched issue #27031 via `gh issue view 27031 --repo open-webui/open-webui --json title,state,stateReason,body,closedAt` (state CLOSED/COMPLETED, contradicting candidate's OPEN label). Fetched linked PRs #27032 and #27425 (`gh pr view ... --json state,mergedAt,body`) — both CLOSED, mergedAt:null. Fetched current backend/open_webui/utils/response.py, middleware.py, context_compaction.py via `gh api repos/.../contents/...` — confirmed additive summing and inflated-threshold-read code is unchanged/still present. Star count re-verified via `gh repo view` = 149,491 vs claimed 149,488 (normal live-count drift, not a discrepancy).",
    "stars": 149488
  },
  {
    "id": 323,
    "name": "langchain CacheBackedEmbeddings — hash-of-text ByteStore gates every embed_documents() call",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched libs/langchain/langchain_classic/embeddings/cache.py in full. Lines 178-190 match verbatim: \"vectors: list[list[float] | None] = self.document_embedding_store.mget(\\n    texts,\\n)\\nall_missing_indices: list[int] = [\\n    i for i, vector in enumerate(vectors) if vector is None\\n]\\n\\nfor missing_indices in batch_iterate(self.batch_size, all_missing_indices):\\n    missing_texts = [texts[i] for i in missing_indices]\\n    missing_vectors = self.underlying_embeddings.embed_documents(missing_texts)\". Line 63 confirmed verbatim: \"return f\\\"{namespace}{_sha1_hash_to_uuid(key)}\\\"\" (sha256/blake2b/sha512 alternatives also present at lines 64-69). On a hash hit, texts[i] never enters missing_texts, so the underlying embedding model is never called at all for that exact string — a byte-identical-content match gating a full skip. This satisfies PASS_REPLAY cleanly: the model reads nothing on a hit, and the gate key is derived from the literal request text, not an approximation.",
    "savings": "Not numerically quantified in-repo; mechanism is a full skip (100% of embedding cost) on any exact repeat of input text.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 144725 stars): 2026-08-21: Fetched full file via `gh api repos/langchain-ai/langchain/contents/libs/langchain/langchain_classic/embeddings/cache.py` and read lines 1-70 and 165-200 directly — both cited excerpts match verbatim including exact line numbers. Star count re-verified via `gh repo view` = 144,726 vs claimed 144,725 (drift of 1, immaterial).",
    "telemetrySignal": "None instrumented in the library — purely a client-side cache-hit/miss code path with no built-in metrics.",
    "providers": [
      "langchain-ai/langchain"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full file via `gh api repos/langchain-ai/langchain/contents/libs/langchain/langchain_classic/embeddings/cache.py` and read lines 1-70 and 165-200 directly — both cited excerpts match verbatim including exact line numbers. Star count re-verified via `gh repo view` = 144,726 vs claimed 144,725 (drift of 1, immaterial).",
    "stars": 144725,
    "crosscheckOverride": {
      "from": "PASS_REPLAY",
      "to": "FAIL",
      "why": "Re-fetched and read the file directly. The cache key is `f\"{namespace}{hash(text)}\"` and `namespace` defaults to the empty string (`from_bytes_store(..., namespace: str = \"\", ...)`, line 293) — it is NOT auto-derived from `underlying_embeddings.model`; the docstring only *recommends* callers pass `namespace=underlying_embedder.model` (lines 130-131), it doesn't enforce it. `_make_default_key_encoder`'s `_key_encoder` (lines 60-71) hashes only the text `key` — model identity never enters the hash under any algorithm (sha1/blake2b/sha256/sha512). Consequence: a `ByteStore` reused across two different `underlying_embeddings` (an entirely ordinary model upgrade or provider swap, using the default namespace) will report a hit on identical text and return the OLD model's stored vector instead of ever invoking the new model. The gate hashes a slice of the request (text only), not 'the full request' (model + text) as PASS_REPLAY requires, so a genuinely different request (same text, different model) collides with and silently replays a different model's stale answer — the automatic-FAIL condition 'a different model answers,' reachable via ordinary, non-adversarial use, not just a documented-but-ignored edge case."
    }
  },
  {
    "id": 324,
    "name": "llama_index IngestionPipeline — sha256(node content + transform config) gates the transform (embedding) step, plus docstore hash-diff skips re-embedding unchanged documents",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched llama-index-core/llama_index/core/ingestion/pipeline.py in full. Lines 100-108 confirmed verbatim: \"for transform in transformations_with_progress:\\n    if cache is not None:\\n        hash = get_transformation_hash(nodes, transform)\\n        cached_nodes = cache.get(hash, collection=cache_collection)\\n        if cached_nodes is not None:\\n            nodes = cached_nodes\\n        else:\\n            nodes = transform(nodes, **kwargs)\\n            cache.put(hash, nodes, collection=cache_collection)\". Lines 481-493 confirmed verbatim, including \"else:\\n    continue  # document exists and is unchanged, so skip it\". Line 596 confirmed: \"nodes_to_run = self._handle_upserts(input_nodes)\" feeds directly into the transform step. Also directly verified get_transformation_hash() (lines 58-69): \"return sha256((nodes_str + transform_string).encode(\\\"utf-8\\\")).hexdigest()\" where nodes_str is node.get_content(metadata_mode=MetadataMode.ALL) — confirming the hash covers actual node content plus transform config, exactly as claimed. Also independently confirmed BaseEmbedding(TransformComponent, ...) in base/embeddings/base.py, so embedding models genuinely flow through this exact gated code path, not a hypothetical. Both gates are exact content-hash matches; on a hit, the embedding call function is never invoked. Clean PASS_REPLAY.",
    "savings": "Not numerically quantified in-repo; full skip of the embedding call on unchanged content at either the IngestionCache or docstore-diff layer.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 51790 stars): 2026-08-21: Fetched full file via `gh api repos/run-llama/llama_index/contents/llama-index-core/llama_index/core/ingestion/pipeline.py` and read lines 58-113, 470-500, and 585-605 directly — all cited excerpts and line numbers match verbatim. Also fetched base/embeddings/base.py confirming BaseEmbedding extends TransformComponent. Star count re-verified via `gh repo view` = 51,790, exact match.",
    "telemetrySignal": "None built in beyond the cache hit/miss control flow itself.",
    "providers": [
      "run-llama/llama_index"
    ],
    "verifiedAgainst": "2026-08-21: Fetched full file via `gh api repos/run-llama/llama_index/contents/llama-index-core/llama_index/core/ingestion/pipeline.py` and read lines 58-113, 470-500, and 585-605 directly — all cited excerpts and line numbers match verbatim. Also fetched base/embeddings/base.py confirming BaseEmbedding extends TransformComponent. Star count re-verified via `gh repo view` = 51,790, exact match.",
    "stars": 51790,
    "crosscheckOverride": {
      "from": "PASS_REPLAY",
      "to": "FAIL",
      "why": "Re-fetched and read the file directly, including the previously-uncited `run()` (lines 538-620), `_handle_upserts` (451-507) and `_handle_duplicates` (451-467). The claim bundles two mechanisms but only one hashes the full request. `get_transformation_hash` (used inside `run_transformations`/`IngestionCache`, lines 58-69) does include the transform's serialized config and would be fine in isolation. But the claim also credits the docstore hash-diff as part of what gates 're-embedding unchanged documents' — and that gate runs on `input_nodes` before any transformation executes: `run()` line 596/602 calls `nodes_to_run = self._handle_upserts(input_nodes)` (or `_handle_duplicates`) strictly ahead of the `run_transformations` call that would consult `get_transformation_hash`. Because this comparison (`existing_hash != node.hash`, line 485; `continue # document exists and is unchanged, so skip it`, line 493) happens pre-transform, `node.hash` structurally cannot encode transform/model config — it hasn't run yet. In the common docstore+vector_store deployment (`DocstoreStrategy.UPSERTS`), changing the embed model or chunker while document content is unchanged makes `existing_hash == node.hash`, so the node is dropped from `nodes_to_run` and the new model is never called at all — the vector store keeps the old model's embedding under the new config's identity. This upstream gate hashes content only, not the full request, so the overall claimed mechanism (as the entry itself describes it, crediting both layers) fails PASS_REPLAY even though the downstream IngestionCache layer alone would have passed."
    }
  },
  {
    "id": 325,
    "name": "AnythingLLM vector-cache — path-hash gate skips EmbedderEngine.embedChunks() across the chroma/qdrant/weaviate/pgvector providers",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched server/utils/files/index.js and server/utils/vectorDbProviders/chroma/index.js in full; every cited line matches verbatim, including \"const digest = uuidv5(filename, uuidv5.URL);\", the log line \"Cached vectorized results of ${filename} found! Using cached data to save on embed costs.\", and in chroma/index.js the early `return { vectorized: true, error: null };` inside `if (cacheResult.exists)` that occurs before the file reaches `const vectorValues = await EmbedderEngine.embedChunks(textChunks);` later in the same function — confirmed embedChunks is genuinely unreachable on the hit branch. Overriding the candidate's own proposed INSUFFICIENT_EVIDENCE: the mechanism is fully verified, not under-evidenced — `cachedVectorInformation(filename)` hashes only the file path/name (`uuidv5(filename, uuidv5.URL)`); the document's actual content is never part of the digest input anywhere in this function. The register's bar defines PASS_REPLAY as replay 'ONLY on a byte-identical hash match of the full request' — here the thing being matched (a filesystem path) is not the thing being sent to the embedder (chunk text), so a file overwritten/edited at the same path would silently replay stale embeddings for content the embedder never saw. That is a proxy-identity cache, not a content-identity cache — it does not meet the standard for any PASS tier, so it settles as FAIL rather than an unresolved question.",
    "savings": "In-repo claim only, no percentage: log line states caching exists 'to save on embed costs.'",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 65017 stars): 2026-08-21: Fetched server/utils/files/index.js and server/utils/vectorDbProviders/chroma/index.js via `gh api repos/Mintplex-Labs/anything-llm/contents/...` and read the full cachedVectorInformation function (lines 266-281) and the chroma addDocumentToNamespace cache-branch (lines 210-279) directly — confirmed path-only hash key and the pre-embedChunks early return. Did not independently re-verify the qdrant/weaviate/pgvector provider files beyond the candidate's own gh-search claim. Star count re-verified via `gh repo view` = 65,017, exact match.",
    "telemetrySignal": "A console.log fires on every cache hit; no structured metric or counter.",
    "providers": [
      "Mintplex-Labs/anything-llm"
    ],
    "verifiedAgainst": "2026-08-21: Fetched server/utils/files/index.js and server/utils/vectorDbProviders/chroma/index.js via `gh api repos/Mintplex-Labs/anything-llm/contents/...` and read the full cachedVectorInformation function (lines 266-281) and the chroma addDocumentToNamespace cache-branch (lines 210-279) directly — confirmed path-only hash key and the pre-embedChunks early return. Did not independently re-verify the qdrant/weaviate/pgvector provider files beyond the candidate's own gh-search claim. Star count re-verified via `gh repo view` = 65,017, exact match.",
    "stars": 65017
  },
  {
    "id": 326,
    "name": "khoj MD5(compiled-chunk-text) set-difference gates embed_documents() before writing to its pgvector-backed Entry table",
    "strictVerdict": "FAIL",
    "reasoning": "Re-fetched src/khoj/processor/content/text_to_entries.py in full. Line 37 confirmed verbatim: \"return lambda entry: hashlib.md5(bytes(getattr(entry, key), encoding=\\\"utf-8\\\")).hexdigest()\", with key defaulting to \"compiled\" (line 148: `key=\"compiled\"`). Lines 174-183 confirmed verbatim: \"existing_entry_hashes = set([entry.hashed_value for entry in existing_entries])\\nhashes_to_process |= hashes_for_file - existing_entry_hashes\" through \"embeddings += self.embeddings_model[model.name].embed_documents(data_to_embed)\", with data_to_embed built strictly from entries_to_process, which is itself built strictly from hashes_to_process (the set difference). pgvector backing independently confirmed: database/models/__init__.py imports `from pgvector.django import VectorField` and defines `embeddings = VectorField(dimensions=None)`; docker-compose.yml uses image `docker.io/pgvector/pgvector:pg15`. The hash is over the actual chunk content being embedded (the compiled field), and only chunks whose exact md5 isn't already present in the pgvector-backed table are passed to embed_documents() — a byte-identical content match gating a full skip. Clean PASS_REPLAY, and a stronger instance than the AnythingLLM candidate because the key is unambiguously content-derived rather than a path proxy.",
    "savings": "Not numerically quantified in-repo; full skip of the embedding call for any exact repeat chunk across a user's corpus.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 36625 stars): 2026-08-21: Fetched src/khoj/processor/content/text_to_entries.py, src/khoj/database/models/__init__.py, and docker-compose.yml via `gh api repos/khoj-ai/khoj/contents/...` and read lines 25-45 and 142-186 of text_to_entries.py directly — all cited excerpts and line numbers match verbatim; pgvector VectorField and docker image confirmed by grep on decoded content. Star count re-verified via `gh repo view` = 36,625, exact match.",
    "telemetrySignal": "None instrumented beyond the hash-diff control flow itself; `timer(...)` context managers log elapsed time for the surrounding steps but do not report skip counts.",
    "providers": [
      "khoj-ai/khoj"
    ],
    "verifiedAgainst": "2026-08-21: Fetched src/khoj/processor/content/text_to_entries.py, src/khoj/database/models/__init__.py, and docker-compose.yml via `gh api repos/khoj-ai/khoj/contents/...` and read lines 25-45 and 142-186 of text_to_entries.py directly — all cited excerpts and line numbers match verbatim; pgvector VectorField and docker image confirmed by grep on decoded content. Star count re-verified via `gh repo view` = 36,625, exact match.",
    "stars": 36625,
    "crosscheckOverride": {
      "from": "PASS_REPLAY",
      "to": "FAIL",
      "why": "The cited quotes (line 37, line 148 key=\"compiled\", lines 174-183) all match verbatim exactly as claimed, and I independently confirmed the pgvector/Entry-table claims (database/models/__init__.py:14 'from pgvector.django import VectorField', :787 'embeddings = VectorField(dimensions=None)' inside class Entry, docker-compose.yml:3 'docker.io/pgvector/pgvector:pg15'). Evidence isn't forged. But going one level past what the entry checked, I found the dedup query itself is broken as a 'full request' hash: DbEntry.objects.filter(user=user, hashed_value__in=hashes_for_file, file_type=file_type) at lines 171-173 has NO search_model filter, even though Entry rows have a search_model FK (set only at creation, line 223, from get_default_search_model()) and that default search model is itself a DB-configurable SearchModelConfig record (adapters/__init__.py:560-567) that self-hosted khoj admins can and do change. I checked all 9 call sites (docx/pdf/image/plaintext/notion/org/github/markdown/text_to_entries.py) — none pass model identity into the dedup query. Net effect: hashed_value = MD5(compiled text) ONLY, with model identity completely excluded from the gate despite living on the very same row. So after an admin switches the configured embedding model, any content whose compiled text is unchanged is treated as 'already exists' and embed_documents() is never called with the new model — the system silently keeps serving the OLD model's stale embeddings from the pgvector table indefinitely, with no recomputation path short of deleting the row or the raw text changing. That is a genuine 'a different model should answer, and does not' failure — the hash gates content only, not 'the full request' (which necessarily includes which model is meant to answer it). Unlike the langchain entry (explicit, documented, user-controlled namespace parameter) or the llama_index entry (IngestionCache hash automatically includes transform/model config via to_dict()), this is khoj's ENTIRE and ONLY described caching mechanism — there is no independent, model-aware fallback layer to fall back on. Downgrading to FAIL. | Re-read lines 142-183 of the fetched file directly. The existing-entry lookup that determines `hashes_to_process` is `DbEntry.objects.filter(user=user, hashed_value__in=hashes_for_file, file_type=file_type)` (lines 171-173) — filtered by user, content hash, and file_type only. `search_model` is never part of that filter, even though `DbEntry` stores a `search_model` field on every write (line 223: `search_model=model`) and `model = get_default_search_model()` is re-read fresh on every call (line 178) and can change (admin/config edit swaps the configured embedding model). If the configured search model changes but a chunk's compiled text is byte-identical to a chunk already indexed under the old model, `hashes_to_process = hashes_for_file - existing_entry_hashes` (line 175) excludes it, `embed_documents()` is never called for that chunk (lines 180-183), and the old model's stored vector is retained in the pgvector table as if it were current. The hash gate covers chunk text only, not 'the full request' (text + model); a model change is a different request that collides with the old hash and is silently skipped rather than replayed on a byte-identical full-request match."
    }
  },
  {
    "id": 327,
    "name": "promptfoo `retry <evalId>` rescopes reruns to ERROR-only rows, leaving graded rows untouched",
    "strictVerdict": "PASS_ABSOLUTE",
    "reasoning": "Independently fetched src/node/retry.ts@main and confirmed the exact mechanism: `getErrorResultIds()` (lines 130-141) filters strictly on `eq(evalResultsTable.failureReason, ResultFailureReason.ERROR)`, and `retryCommand()` logs the verbatim string `Found ${errorResultIds.length} ERROR results to retry` (line 354) before calling `evaluate()` only on those rows via resume mode (`cliState.resume = true; cliState.retryMode = true`, lines 372-373). Also confirmed the delete-after-success safety design via the code comment at lines 359-364: 'CRITICAL: We do NOT delete ERROR results here anymore! ... Now we delete AFTER successful retry, so if retry fails, ERROR results are preserved.' Applying the bar to the never-retried rows (the majority of a typical suite): zero tokens are read, no model answers, no thinking happens — nothing is sent at all, versus the naive CI alternative of re-running the whole suite. The genuinely-errored rows do get fresh, necessary calls (no valid prior output exists to replay), which is outside the savings claim, not a violation of it.",
    "savings": "No first-party benchmark published; savings scale with (untouched rows / total rows) per retry, which is typically the large majority of a suite that had only transient provider errors. Eliminates full-suite CI re-runs on partial failure.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 24441 stars): 2026-08-21: Fetched src/node/retry.ts at ref=main via `gh api repos/promptfoo/promptfoo/contents/src/node/retry.ts`; confirmed lines 130-141 (getErrorResultIds query), line 354 (log quote, verbatim match), and lines 359-364 (delete-after-success design). Re-verified star count via `gh repo view` = 24441 (exact match to claim).",
    "telemetrySignal": "DB query pattern in getErrorResultIds() scoped by failureReason=ERROR, and the eval-count log line 'Found N ERROR results to retry' where N is far smaller than total suite size, are directly observable in CLI/CI logs.",
    "providers": [
      "promptfoo/promptfoo"
    ],
    "verifiedAgainst": "2026-08-21: Fetched src/node/retry.ts at ref=main via `gh api repos/promptfoo/promptfoo/contents/src/node/retry.ts`; confirmed lines 130-141 (getErrorResultIds query), line 354 (log quote, verbatim match), and lines 359-364 (delete-after-success design). Re-verified star count via `gh repo view` = 24441 (exact match to claim).",
    "stars": 24441,
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 328,
    "name": "langsmith-sdk LANGSMITH_TEST_CACHE: VCR-cassette HTTP replay for @pytest.mark.langsmith eval tests, matched on request body",
    "strictVerdict": "PASS_REPLAY",
    "reasoning": "Fetched python/langsmith/utils.py@main and confirmed `with_cache()` (lines 602-617) constructs `vcr.VCR(record_mode=\"new_episodes\", match_on=[\"uri\", \"method\", \"path\", \"body\"], filter_headers=[\"authorization\", \"Set-Cookie\"])` — exact match to the cited quote — with no custom body matcher registered anywhere in the file, so vcrpy's default exact-equality body matcher applies (no semantic/fuzzy JSON matching). Fetched python/langsmith/testing/_internal.py@main and confirmed the docstring at lines 166-169 verbatim: 'LLM calls are expensive! Cache requests by setting `LANGSMITH_TEST_CACHE=path/to/cache`. Check in these files to speed up CI/CD pipelines, so your results only change when your prompt or requested model changes.' `record_mode=\"new_episodes\"` means non-matching (i.e., non-byte-identical) requests are executed for real and recorded, while identical ones are replayed — the only excluded fields are auth/cookie headers, which the model never reads. This satisfies PASS_REPLAY exactly.",
    "savings": "Avoids re-invoking the LLM for eval/CI pytest reruns whenever the request (method+URI+path+body, which includes model/params/messages) is byte-identical to a committed cassette. No first-party benchmark number in the docs; savings scale with how often CI reruns unchanged prompts.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 1027 stars): 2026-08-21: Fetched python/langsmith/utils.py and python/langsmith/testing/_internal.py at ref=main via `gh api repos/langchain-ai/langsmith-sdk/contents/...`; confirmed lines 602-617 (VCR config, exact match_on/filter_headers) and lines 166-169 (docstring, exact quote). Re-verified star count via `gh repo view` = 1027 (exact match to claim).",
    "telemetrySignal": "vcrpy cassette file diffs / cassette hit-vs-record events are directly observable in test run output; a 'new_episodes' recording event fires only on a non-matching request.",
    "providers": [
      "langchain-ai/langsmith-sdk"
    ],
    "verifiedAgainst": "2026-08-21: Fetched python/langsmith/utils.py and python/langsmith/testing/_internal.py at ref=main via `gh api repos/langchain-ai/langsmith-sdk/contents/...`; confirmed lines 602-617 (VCR config, exact match_on/filter_headers) and lines 166-169 (docstring, exact quote). Re-verified star count via `gh repo view` = 1027 (exact match to claim).",
    "stars": 1027,
    "adversarialNote": "survived both skeptic lenses"
  },
  {
    "id": 329,
    "name": "lm-evaluation-harness response cache omits model identity — cross-model cache poisoning",
    "strictVerdict": "FAIL",
    "reasoning": "Fetched lm_eval/api/model.py@main and confirmed `hash_args` at lines 230-232 verbatim: 'def hash_args(attr: str, args: Iterable[Any]) -> str: dat = json.dumps([attr] + list(args)); return hashlib.sha256(dat.encode(\"utf-8\")).hexdigest()' — no model identifier parameter or reference anywhere in the function, confirming the bug is still live on current main (unpatched). Fetched issue #2715 via `gh issue view`: state is OPEN, reproduction with Qwen2.5-3B vs. Llama-3.2-3B on hellaswag matches the cited body text, and maintainer baberabb's comment is verbatim: 'the current implementation simply checks if the inputs exist in the provided cache file, and does not take the model into account.' This is an automatic FAIL under 'does a different model answer' — a second model's evaluation run silently returns the first model's stored generations as its own results whenever prompts match (guaranteed for standard shared few-shot benchmark prompts).",
    "savings": "N/A — this is a correctness/data-integrity defect, not a savings technique. It happens to reduce API calls, but by silently serving the wrong model's answers, so it does not qualify for the register regardless of any apparent cost reduction.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 13736 stars): 2026-08-21: Fetched lm_eval/api/model.py at ref=main via `gh api repos/EleutherAI/lm-evaluation-harness/contents/lm_eval/api/model.py`, confirmed hash_args (lines 230-232) has no model_id param. Fetched issue #2715 via `gh issue view --repo EleutherAI/lm-evaluation-harness`, confirmed OPEN state and maintainer quote verbatim. Re-verified star count via `gh repo view` = 13736 (exact match to claim).",
    "telemetrySignal": "Running two different --model_args values against the same --use_cache db path and observing identical eval metrics for both is directly reproducible and was independently confirmed by the maintainer.",
    "providers": [
      "EleutherAI/lm-evaluation-harness"
    ],
    "verifiedAgainst": "2026-08-21: Fetched lm_eval/api/model.py at ref=main via `gh api repos/EleutherAI/lm-evaluation-harness/contents/lm_eval/api/model.py`, confirmed hash_args (lines 230-232) has no model_id param. Fetched issue #2715 via `gh issue view --repo EleutherAI/lm-evaluation-harness`, confirmed OPEN state and maintainer quote verbatim. Re-verified star count via `gh repo view` = 13736 (exact match to claim).",
    "stars": 13736
  },
  {
    "id": 330,
    "name": "lm-evaluation-harness request-instance cache omits generation_kwargs — silently reuses stale sampling settings",
    "strictVerdict": "FAIL",
    "reasoning": "Fetched lm_eval/api/task.py@main and confirmed `build_all_requests`'s cache_key composition (lines 288-296) verbatim: 'cache_key = f\"requests-{self._config.task}-{self.config.num_fewshot}shot-rank{rank}-world_size{world_size}\"' followed only by chat_template/fewshot_as_multiturn/system_prompt_hash/tokenizer_name suffixes — no generation_kwargs term anywhere in the function signature or key construction, confirming the bug is live on current main. Also confirmed at line 1408 that `arguments = (ctx, deepcopy(self.config.generation_kwargs))` — generation_kwargs (temperature, do_sample, top_p, max_gen_toks, until — all of which flow into the actual generation request sent to the model) is baked into Instance.arguments at build time but excluded from the cache key that gates whether those Instances get reused. Fetched issue #3881 via `gh issue view`: state OPEN, body quote confirmed verbatim: 'an evaluator who runs the same task at `temperature=0.7` after a cached `temperature=0` run gets `temperature=0` results with no warning,' and a follow-up comment confirms two competing fix PRs (#3891, #3951) are still open/unmerged. This is an automatic FAIL — the model reads a different generation configuration (and produces different tokens) than the run actually requested, silently.",
    "savings": "N/A — correctness/reproducibility defect, not a savings technique.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 13736 stars): 2026-08-21: Fetched lm_eval/api/task.py at ref=main via `gh api repos/EleutherAI/lm-evaluation-harness/contents/lm_eval/api/task.py`, confirmed lines 288-296 (cache_key, no generation_kwargs term) and line 1408 (generation_kwargs baked into arguments). Fetched issue #3881 via `gh issue view --repo EleutherAI/lm-evaluation-harness`, confirmed OPEN state, quote verbatim, and two open/unmerged fix PRs. Re-verified star count via `gh repo view` = 13736 (exact match to claim).",
    "telemetrySignal": "Comparing the generation_kwargs the user passed on the CLI for a --cache_requests run against the generation_kwargs actually baked into the served cached Instance.arguments (inspectable via the cache-request pickle/json artifact) would show a mismatch whenever a prior run used different sampling settings under the same task/fewshot/rank/tokenizer combination.",
    "providers": [
      "EleutherAI/lm-evaluation-harness"
    ],
    "verifiedAgainst": "2026-08-21: Fetched lm_eval/api/task.py at ref=main via `gh api repos/EleutherAI/lm-evaluation-harness/contents/lm_eval/api/task.py`, confirmed lines 288-296 (cache_key, no generation_kwargs term) and line 1408 (generation_kwargs baked into arguments). Fetched issue #3881 via `gh issue view --repo EleutherAI/lm-evaluation-harness`, confirmed OPEN state, quote verbatim, and two open/unmerged fix PRs. Re-verified star count via `gh repo view` = 13736 (exact match to claim).",
    "stars": 13736
  },
  {
    "id": 331,
    "name": "Codex Guardian forked-review prompt_cache_key used child thread ID instead of the shared reuse key",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "Fetched PR #24803 via `gh pr view`: state MERGED (2026-05-28T10:55:08Z), author jif-oai (is_bot: false, i.e. a human codex-team contributor, not an automated/bot merge). Body quote confirmed verbatim: 'Guardian review sessions are reusable across forks when their `GuardianReviewSessionReuseKey` is unchanged, but the underlying Responses request was still using the child thread ID as `prompt_cache_key`. That meant forked Guardian reviews that should share cache context produced different cache keys, reducing prompt cache reuse and weakening the reuse invariant.' The 'What Changed' section confirms the fix computes `guardian:<sha1(parent_thread_id:reuse_key)>` scoped to parent thread + reuse-sensitive config. Applying the bar: (1) different tokens read — no, `prompt_cache_key` is a Responses-API envelope/routing hint the model never tokenizes or reads; the actual system/tool/history content per fork is unchanged; (2) different model — no; (3) different thinking — no. This is a pure cache-key-placement fix, squarely PASS_METADATA, and it is a distinct mechanism (cross-fork key derivation for a sub-agent) from generic prompt_cache_key pinning entries.",
    "savings": "PR does not quantify a token/dollar figure. Directionally: restores server-side prompt-cache hit rate for forked Guardian review sessions sharing the same parent thread + reuse key, which previously each paid a full fresh-prefix cost on every fork instead of reusing the shared prefix.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 111129 stars): 2026-08-21: Fetched PR #24803 via `gh pr view 24803 --repo openai/codex`, confirmed MERGED state, non-bot human author (jif-oai), merge timestamp, and body quote verbatim. Re-verified star count via `gh repo view` = 111129 (candidate claimed 111114; close, consistent with organic star growth since claim was written — not a discrepancy worth flagging).",
    "telemetrySignal": "OpenAI Responses API's cached_tokens field in the usage payload, compared across sibling Guardian forks before/after the fix — pre-fix near-zero cache hits across forks, post-fix hits matching the shared parent-session prefix. PR description states test coverage asserts forked reviews now send the same prompt_cache_key.",
    "providers": [
      "openai/codex"
    ],
    "verifiedAgainst": "2026-08-21: Fetched PR #24803 via `gh pr view 24803 --repo openai/codex`, confirmed MERGED state, non-bot human author (jif-oai), merge timestamp, and body quote verbatim. Re-verified star count via `gh repo view` = 111129 (candidate claimed 111114; close, consistent with organic star growth since claim was written — not a discrepancy worth flagging).",
    "stars": 111129,
    "crosscheckOverride": {
      "from": "PASS_METADATA",
      "to": "INSUFFICIENT_EVIDENCE",
      "why": "The PR body quote is real (confirmed verbatim), but the entry never checked what the PR actually shipped -- and it shipped nothing. Merge commit 3307240195ece4ef772b45e5323c683f4181f1e5 (oid on PR #24803) returns stats {additions:0, deletions:0, total:0}, files:[], and its tree SHA (c4ceea88...) is byte-identical to its parent's tree SHA -- verified three independent ways (JSON stats, /compare API, and the raw .diff media type, which returns zero bytes). This is a true no-op merge, not an API artifact (compare confirmed it IS on main's ancestry, just empty). Tracing git history for the same file turned up the real source: PR #24893 'Add Guardian review prompt cache key', merged 19 minutes earlier (10:36:26Z vs 10:55:08Z), body reading 'Split from the Guardian prompt cache key change... expected to rely on the companion split PRs' -- a classic stacked-PR race where #24803's diff had already landed via its sibling before it merged. Worse: the CURRENT live code (codex-rs/core/src/guardian/review_session.rs:297-308) does not match the entry's cited mechanism at all -- prompt_cache_key_override_for_review_session() takes only parent_thread_id (no reuse_key parameter exists) and returns plain `guardian:{parent_thread_id}`, with no sha1 and no incorporation of GuardianReviewSessionReuseKey into the key string. The entry's 'What Changed' quote (`guardian:<sha1(parent_thread_id:reuse_key)>`) describes code that was never actually merged under this PR and does not exist in the repository today. The underlying phenomenon (cache key keyed by parent thread instead of child) is real and shipped, but via different code, a different PR, and a materially simpler formula than claimed -- the entry's specific evidentiary chain is broken and needs to be rebuilt from #24893, not #24803. Star count re-verified: 111133 (claimed 111114/111129 in prior passes -- organic drift, not itself a flag). | Attack that lands: the entire evidence chain is `gh pr view` — PR title/body text only. No diff was pulled (`gh pr diff`, or contents API before/after the touched file) the way the two source-verified entries in this batch did, quoting actual line ranges from real files. The specific mechanism claimed (`guardian:<sha1(parent_thread_id:reuse_key)>`) is lifted verbatim from the PR author's own 'What Changed' prose, not independently confirmed against code. There is also real semantic ambiguity in the source material itself: 'forked Guardian reviews that should share cache context' could describe pure routing-key metadata (safe, PASS_METADATA) or could describe actually-reused prior review content across forks (which would put content, not just a key, in scope) — a self-reported PR description can't settle which. Downgrading: the claim is plausible but not code-confirmed."
    }
  },
  {
    "id": 332,
    "name": "OpenAI Agents SDK: billed responses.compact calls never entered into run token totals",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "Fetched issue #4434 via `gh issue view`: state CLOSED, body quote confirmed verbatim: 'In a scripted 12-turn run with one compaction pass, the run reports requests=12 and total_tokens=1800 while the compaction pass itself consumed 192,000 tokens. The reported total is under one percent of what was billed.' The issue body also independently states 'Only `compacted.output` is read... the string `usage` does not appear anywhere in that module,' matching the mechanism claim. Fetched PR #4446 via `gh pr view`: state MERGED (2026-08-15T23:12:30Z), body states 'Fixes #4434,' author seratch (is_bot: false; cross-checked via `gh api search/issues?q=repo:openai/openai-agents-python+author:seratch+type:pr+is:merged` returning 512 merged PRs by this author in the repo, strong evidence of maintainer/core-contributor status, not a drive-by or bot). The bug and fix are entirely about whether the SDK's client-side usage accumulator adds numbers already returned by the API into its own running total — the actual `responses.compact` call, its billed tokens, and the model's output are byte-for-byte identical before and after the fix. All three bar clauses are trivially NO (nothing about the request or response changes), so this is squarely PASS_ABSOLUTE, reported per the brief's explicit allowance for maintainer-confirmed usage/cost accounting bugs.",
    "savings": "N/A / inverted — this is an under-counting bug fix, not a savings technique. Pre-fix, RunResult.usage reported under 1% of actually-billed tokens for any run containing a compaction pass; the fix makes reported usage match what OpenAI already billed, with zero change to what is sent or billed.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 28836 stars): 2026-08-21: Fetched issue #4434 via `gh issue view 4434 --repo openai/openai-agents-python`, confirmed CLOSED state and body quote verbatim. Fetched PR #4446 via `gh pr view 4446 --repo openai/openai-agents-python`, confirmed MERGED state, 'Fixes #4434' linkage, and author. Cross-checked author's maintainer standing via `gh api search/issues` (512 merged PRs in-repo). Re-verified star count via `gh repo view` = 28836 (candidate claimed 28835; trivial organic drift, not a discrepancy worth flagging).",
    "telemetrySignal": "context_wrapper.usage.total_tokens (SDK-reported) vs. the OpenAI dashboard's actual billed token count for the same API key over the same window — pre-fix these diverge sharply on any run containing compaction; post-fix they reconcile.",
    "providers": [
      "openai/openai-agents-python"
    ],
    "verifiedAgainst": "2026-08-21: Fetched issue #4434 via `gh issue view 4434 --repo openai/openai-agents-python`, confirmed CLOSED state and body quote verbatim. Fetched PR #4446 via `gh pr view 4446 --repo openai/openai-agents-python`, confirmed MERGED state, 'Fixes #4434' linkage, and author. Cross-checked author's maintainer standing via `gh api search/issues` (512 merged PRs in-repo). Re-verified star count via `gh repo view` = 28836 (candidate claimed 28835; trivial organic drift, not a discrepancy worth flagging).",
    "stars": 28836,
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "INSUFFICIENT_EVIDENCE",
      "why": "Attack that lands: same evidentiary gap as the Guardian entry — verification was `gh issue view` + `gh pr view` (prose from the bug reporter and PR author), not the actual source diff. The specific code-level claim ('the string `usage` does not appear anywhere in that module') and the headline numbers (reported total_tokens=1800 vs. 192,000 actually billed) are both quoted from the issue body's own narrative, never independently grepped against the real accumulator code or confirmed via a reproduced run. Author standing (512 prior merged PRs) and the 'Fixes #4434' linkage are genuine corroborating signals but establish process/social legitimacy, not verified code content, and don't rule out the fix also touching request-affecting logic. Given this batch demonstrably contains two entries verified against real fetched source (promptfoo, langsmith), this one falls short of that same standard for a claim resting entirely on self-reported before/after numbers. Downgrading for unverified mechanism, not for any positive evidence of a bar violation."
    }
  },
  {
    "id": 333,
    "name": "OpenHands agent-server: Pydantic re-validation on LLM registry silently deletes the stats-streaming telemetry callback",
    "strictVerdict": "FAIL",
    "reasoning": "Deciding quote, re-fetched directly from issue #14039 comment by VascoSch92 (authorAssociation MEMBER, self-verified): 'The metrics themselves are accumulated correctly on the shared Metrics object — the bug is purely that the streaming-callback link is severed by Pydantic re-validation.' And: 'the previous _telemetry (which held our _stats_update_callback) is discarded and replaced with a fresh Telemetry whose _stats_update_callback is None. From that point on, every Telemetry.on_response() skips the callback, and no key=\"stats\" events are ever emitted.' Nothing about the request sent to the model changes: this is an internal SDK object (Telemetry) losing a wired callback during Pydantic re-validation, so a downstream API field (accumulated_token_usage) reports empty while true billing/metrics accumulation (on the shared Metrics object) is confirmed intact by the maintainer's own trace. All three bar clauses are NO. Matches PASS_ABSOLUTE's definition ('pure accounting, procurement, client-side correction; zero request change... measurement or structural argument') precisely. Caveat noted for the register: this is a bug report, not a technique anyone deploys to save cost — it satisfies the letter of the 3-clause bar but isn't a savings mechanism at all.",
    "savings": "None — this is not a cost-saving technique. It's a telemetry/observability defect that makes a cost-reporting API field (accumulated_token_usage) go empty; actual billed cost and the underlying Metrics accumulation are unaffected, per the maintainer's own confirmation.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 84720 stars): 2026-08-21: Re-fetched OpenHands/OpenHands issue #14039 via `gh issue view` — confirmed VascoSch92's full root-cause comment verbatim (Root cause / Minimal reproduction / Fix sections), authorAssociation MEMBER. Re-fetched software-agent-sdk PR #2999 via `gh pr view` — confirmed title, state MERGED, mergedBy VascoSch92, body closing #14039. Re-verified star counts via `gh repo view`: OpenHands/OpenHands 84,720; OpenHands/software-agent-sdk 1,016.",
    "telemetrySignal": "Maintainer (MEMBER) root-cause trace with reproduction snippet, plus a merged fix PR (software-agent-sdk#2999, title 'fix(llm): preserve telemetry callbacks across LLM re-validation', merged by the same maintainer, body states 'Closes OpenHands/OpenHands#14039'). Strong, first-party, structural evidence — not a doc sentence.",
    "providers": [
      "provider-agnostic (SDK/client-side bug, reproduced via litellm_proxy in the reporting user's setup)",
      "OpenHands/OpenHands"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched OpenHands/OpenHands issue #14039 via `gh issue view` — confirmed VascoSch92's full root-cause comment verbatim (Root cause / Minimal reproduction / Fix sections), authorAssociation MEMBER. Re-fetched software-agent-sdk PR #2999 via `gh pr view` — confirmed title, state MERGED, mergedBy VascoSch92, body closing #14039. Re-verified star counts via `gh repo view`: OpenHands/OpenHands 84,720; OpenHands/software-agent-sdk 1,016.",
    "stars": 84720,
    "trap": "Reads like a plausible register entry because it cleanly satisfies the 3-clause bar, but it's a category error dressed as a cost technique — it's an observability bug that hides cost data, not a mechanism that reduces cost. Worth flagging to a human curator even though the verdict itself (PASS_ABSOLUTE) is technically correct.",
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "FAIL",
      "why": "Evidence-forgery check passes clean: re-fetched issue OpenHands/OpenHands#14039 and PR OpenHands/software-agent-sdk#2999 via gh, and both cited quotes exist verbatim (VascoSch92, authorAssociation MEMBER: 'The metrics themselves are accumulated correctly on the shared Metrics object — the bug is purely that the streaming-callback link is severed by Pydantic re-validation.' and the '_telemetry ... discarded and replaced with a fresh Telemetry whose _stats_update_callback is None ... no key=\"stats\" events are ever emitted' passage). PR #2999 is confirmed MERGED, mergedBy VascoSch92, body reads 'Closes OpenHands/OpenHands#14039.' Star counts confirmed exact: OpenHands/OpenHands 84,720, OpenHands/software-agent-sdk 1,016. So the technical trace is not fabricated. The problem is upstream of the bar: this is not a cost-saving technique at all, and the 3-clause test ('does the model read different tokens / does a different model answer / does a different amount of thinking happen') has nothing to apply itself to. The bug severs an internal Python callback that streams metrics-already-collected on the shared Metrics object out over a websocket/API field (accumulated_token_usage); nothing about a request, a response, billing, or model selection is described as changing at all, in either direction. PASS_ABSOLUTE is defined as 'pure accounting, procurement, client-side correction; zero request change' as the resolution of a technique being graded — there is no technique here to certify as request-neutral, only a telemetry pipe that was unintentionally unplugged and then re-plugged in a bugfix. The entry's own reasoning field concedes exactly this ('this is a bug report, not a technique anyone deploys to save cost ... isn't a savings mechanism at all'), which is a self-admission that the entry is out of scope for a register whose pass column is supposed to represent verified cost/token-neutral techniques. Assigning it the strongest passing tier (PASS_ABSOLUTE) manufactures a pass for an item with no savings claim to test, inflating the pass count on a category error rather than a verified technique. Given the directive to default to downgrading under uncertainty, and that none of the concrete verdict definitions (including FAIL's specific automatic-fail list, none of which describes what happened here) actually fit an entry with no technique to evaluate, the defensible disposition is that this cannot be settled as a pass under the register's own bar and should be flagged to a human curator for removal/reclassification rather than counted as PASS_ABSOLUTE. | Independently re-verified the facts via `gh issue view 14039 --repo OpenHands/OpenHands` and `gh pr view 2999 --repo OpenHands/software-agent-sdk`: VascoSch92's root-cause comment, the RegistryEvent(llm=llm) re-validation trace, and the merged fix PR (mergedBy VascoSch92, closes #14039) all quote verbatim as claimed. The factual chain is solid. But the verdict itself is a category error, and the entry's own text admits it: savings='None — this is not a cost-saving technique', and the reasoning field itself flags 'this is a bug report... isn't a savings mechanism at all.' PASS_ABSOLUTE is defined for a *technique* — a procurement/accounting/client-side-correction mechanism — verified to leave the wire request untouched. This entry has no technique to check: it's an observability bug where a stats-streaming callback gets clobbered by Pydantic re-validation, so an API field (accumulated_token_usage) goes empty while the underlying Metrics object keeps accumulating correctly. Every bug in existence that doesn't touch request construction trivially satisfies 'nothing on the wire changed' — that's not evidence of a verified zero-request-change savings mechanism, it's the null case of not having one at all. Certifying it as PASS_ABSOLUTE launders a bug report into the register's passing column, which is exactly the kind of scope-creep that would inflate a pass count that's already proven to be majority-false on audit (71->47). Since there is no technique under test, it cannot be credited with passing the bar's three clauses in any meaningful sense; the correct disposition is to reject the passing label outright, not extend it on a technicality. Recommend removal from the register entirely rather than any PASS_* label — flagging FAIL only because the enum offers no 'not applicable/out of scope' option, and this does not clear the bar for a certifiable pass."
    }
  },
  {
    "id": 334,
    "name": "Codex maps response.incomplete (a billed, completed-but-truncated turn) to a retryable stream error, resending an already-answered request",
    "strictVerdict": "FAIL",
    "reasoning": "Deciding quotes, re-fetched by me directly from raw.githubusercontent.com/openai/codex/main (not from the candidate's paraphrase): codex-rs/codex-api/src/sse/responses.rs line 453-462: `\"response.incomplete\" => { let reason = event.response.as_ref()... let message = format!(\"Incomplete response returned, reason: {reason}\"); return Err(ResponsesEventError::Api(ApiError::Stream(message))); }`. codex-rs/protocol/src/error.rs line 390-400, inside `pub fn is_retryable(&self) -> bool`: `CodexErrorDetails::Stream(..) | CodexErrorDetails::Timeout | ... => true`, grouped with ConnectionFailed/Io/InternalServerError — i.e. treated identically to an actual dropped connection. This directly answers the bar's third clause: on a response.incomplete event (a semantic, billed, terminal status the provider sends after fully computing and truncating a response, e.g. reason=max_output_tokens), Codex's retry machinery re-invokes the model on the same turn — a different (extra) amount of thinking demonstrably happens versus the no-bug baseline. That is source-verified fact, not speculation, and is sufficient on its own to fail the bar regardless of whether a human maintainer has weighed in.",
    "savings": "Negative. This is not a savings mechanism — it is a defect that increases cost via unnecessary retries (and, after retry-budget exhaustion, a WebSocket-to-HTTPS transport fallback) on turns that were already fully answered and billed. It belongs in the register, if at all, as a documented anti-pattern/trap, not as a PASS entry.",
    "provenance": "Primary source re-fetched during adjudication (2026-08-21 high-star GitHub sweep; repo 111129 stars): 2026-08-21: Re-fetched codex-rs/codex-api/src/sse/responses.rs and codex-rs/protocol/src/error.rs directly from GitHub raw (main branch) and located/confirmed both quoted code fragments verbatim myself. Re-fetched openai/codex issue #38831 via `gh issue view` — confirmed OPEN, 1 comment, authorAssociation NONE, no maintainer response. Re-verified star count via `gh repo view`: 111,129.",
    "telemetrySignal": "Source-code-verified misclassification (response.incomplete -> ApiError::Stream -> is_retryable()=true) confirmed by my own independent fetch of both files on main. Issue #38831 is OPEN with exactly one comment, author `jdcodes1`, authorAssociation NONE — no maintainer reply, no merged fix, so no first-party confirmation exists.",
    "providers": [
      "OpenAI (Responses API)",
      "openai/codex"
    ],
    "verifiedAgainst": "2026-08-21: Re-fetched codex-rs/codex-api/src/sse/responses.rs and codex-rs/protocol/src/error.rs directly from GitHub raw (main branch) and located/confirmed both quoted code fragments verbatim myself. Re-fetched openai/codex issue #38831 via `gh issue view` — confirmed OPEN, 1 comment, authorAssociation NONE, no maintainer response. Re-verified star count via `gh repo view`: 111,129.",
    "stars": 111129,
    "trap": "The candidate downgrades an otherwise clearly-decidable question (does this trigger model reinvocation on an already-answered turn? — yes, verified straight from source) to INSUFFICIENT_EVIDENCE by demanding proof of things the bar doesn't require: maintainer sign-off and confirmation that a duplicate call physically reaches the billing meter. The wire-level mechanism itself is fully source-verifiable and settles the bar; treat 'no maintainer replied yet' as irrelevant to the verdict once the code path is independently confirmed."
  }
]