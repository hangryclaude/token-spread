[
  {
    "drop": false,
    "name": "Groq automatic prompt caching (exact-prefix, no extra fee, 50% discount)",
    "class": "prompt-caching",
    "providers": [
      "Groq"
    ],
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "Mechanistically this is exact-prefix KV-cache reuse: the token sequence Groq's model reads is byte-identical whether the prefix hits cache or not, the same model answers, and nothing about the requester's scheduling changes — the wire content is untouched, which is as clean as caching designs get. But whether the cached-path *output* is truly bit-identical to a fresh compute is asserted only by Groq itself: \"The actual model inference and response generation occur normally, maintaining identical output quality whether caching is used or not.\" Per the register rule, a vendor doc sentence asserting output identity caps the verdict at CONTRACTUAL_ONLY — it is not independent proof of model-invisibility, since Groq's custom LPU batching/scheduling around a cache hit vs. miss path is undocumented and untested by any third party. Confirms the miner's proposed verdict; not overridden.",
    "verifiedAgainst": "https://console.groq.com/docs/prompt-caching (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "Auditable per-request: Groq's chat-completion response exposes usage.cached_tokens alongside usage.prompt_tokens (confirmed in the docs' worked example), so a spend audit can compute a real per-request/per-account cache-hit ratio directly from API responses — it does not have to infer caching from an unexplained invoice delta the way some vendors' schemes require.",
    "savings": "50% discount on cached input tokens with no separate cache-write or storage fee (contrast Anthropic's cache-write premium) — per the source: \"Prompt caching is provided at no additional cost. There is a 50% discount for cached input tokens.\" Account-level dollar impact is UNQUANTIFIED: it depends on prefix hit-rate, which depends on request volume/ordering since the cache is recency-based with only a 2-hour TTL, and is currently scoped to just 3 models (GPT-OSS 20B/120B/Safeguard-20B) per \"We're starting with a limited selection of models and will roll out prompt caching to more models soon\" — not yet Groq's flagship Llama or Kimi lines.",
    "trap": "Reads at first pass as just another routine prompt-caching entry, and its mechanism (exact-prefix KV reuse, wire content unchanged) is genuinely the cleanest possible design for this category — automatic, free, cannot even be disabled. That cleanliness makes it tempting to wave through as PASS_ABSOLUTE. But the output-identity claim is still sourced entirely from Groq's own FAQ, not from any independently verifiable determinism guarantee, so the vendor-assertion rule still caps it at CONTRACTUAL_ONLY.",
    "whoActsOnIt": "Any Groq customer sending repeated system-prompt/tool-definition prefixes on GPT-OSS-20B/120B/Safeguard-20B gets the discount automatically today with zero code change and no opt-in step; the only actionable lever is prompt structuring (static content first, variable content last) since cache hits require an exact-prefix match.",
    "estimatedReach": "Narrow today by Groq's own admission — limited to 3 GPT-OSS models, explicitly not yet extended to Groq's flagship Llama-3.3/4, Kimi K2, or Qwen offerings; reach widens automatically as Groq expands model coverage, per the docs' own forward-looking statement.",
    "implementationNote": "Zero implementation effort required — fully automatic, no cache_control breakpoints or opt-in flag (unlike Anthropic/OpenAI explicit prompt caching), and cannot be manually disabled. The only engineering lever available is prompt structuring: place static content (system prompt, few-shot examples, tool/schema definitions) first and variable content (user query, timestamps, session/user IDs) last, since any variable token placed before the static block breaks exact-prefix matching for everything downstream of it."
  },
  {
    "drop": false,
    "name": "Fireworks AI prompt caching (default-on, exact-prefix)",
    "class": "prompt-caching",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "providers": [
      "Fireworks AI"
    ],
    "reasoning": "Verified verbatim on docs.fireworks.ai/guides/prompt-caching: \"Prompt caching doesn't alter the result generated by the model. The response you receive will be identical to what you would get if prompt caching was not used. Each generation is sampled from the model independently on each request and is not cached for future usage.\" Also independently confirmed on the same page: \"Prompt caching is enabled by default for all Fireworks models and deployments\" (no opt-in string found anywhere on the page), \"The default discount is 50%, but the exact discount varies by model,\" and \"Cached tokens on dedicated deployments are close to free for you, because they affect context length but do not need extra processing.\" Mechanistically this is exact-prefix KV-cache reuse: the model reads the identical token sequence and samples independently each call, so nothing should change on the wire. But the sole evidence for that behavioral identity is Fireworks' own documentation sentence — we did not run an independent byte-for-byte replay/diff test ourselves — so per the adjudication bar a vendor-asserted identity claim caps the verdict at CONTRACTUAL_ONLY, not any PASS_* tier.",
    "verifiedAgainst": "https://docs.fireworks.ai/guides/prompt-caching (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "On dedicated deployments, response headers fireworks-prompt-tokens and fireworks-cached-prompt-tokens report per-request cache-hit counts, and aggregated cache metrics appear in the Fireworks usage dashboard — this mechanism is directly confirmed in the docs. For serverless, the 50%-discounted cached-token rate implies billing line items should distinguish cached vs. regular prompt tokens, but line-item granularity for serverless billing was not independently confirmed in this pass.",
    "savings": "50% default discount on cached serverless tokens, varies by model (Model Library has per-model figures); on dedicated deployments cached tokens add no extra compute charge, described as \"close to free\" — both figures confirmed verbatim on the primary source.",
    "trap": "Vendor-docs identity claim (byte-identical-output assertion) — no independent replay test was performed, so this is capped at CONTRACTUAL_ONLY per the adjudication bar. Repo-star and PR-merge-ancestry traps are not applicable since the source is a docs page, not a repo or PR. Mechanism refuter note, preserved: batch-nondeterminism (Thinking Machines Lab) means a cache hit can land the request in a different batch shape than a cold run, so byte-identical OUTPUT is a stronger claim than the docs can carry — true of this whole verdict class, which is why the class caps at the provider's word.",
    "whoActsOnIt": "Any engineer sending Fireworks serverless or dedicated requests with a stable prompt prefix (system prompt, tool schema, RAG context) — no opt-in needed since caching is default-on; savings accrue automatically when prefixes repeat within the cache TTL (minutes, up to hours depending on model/load).",
    "implementationNote": "No caller-side implementation is required — caching is default-on across all Fireworks serverless and dedicated deployments. To maximize hit rate, order prompts with static/shared content first and variable content last, per standard exact-prefix caching practice.",
    "estimatedReach": "All Fireworks AI customers (serverless and dedicated) running repeated-prefix workloads — agents with long system prompts, tool-calling schemas, RAG contexts, or multi-turn chat.",
    "crosscheckOverride": {
      "from": "FAIL",
      "to": "CONTRACTUAL_ONLY",
      "why": "Maintainer resolution: the mechanism refuter lowered this to FAIL citing Thinking Machines Lab batch-nondeterminism — cached-path vs cold-path batching can change numerics, so the vendor identity sentence may be false in practice. That argument is real, but it applies with identical force to every provider prompt cache the register grades CONTRACTUAL_ONLY (Anthropic, OpenAI, Bedrock, Groq in this same sweep); grading one vendor FAIL on it would make the category incoherent. Precedent controls: vendor-asserted identity caps at CONTRACTUAL_ONLY, and the batching tension is recorded here and in the trap field rather than as the verdict."
    }
  },
  {
    "drop": false,
    "name": "Fireworks AI On-Demand deployments: per-GPU-second billing with no cold-start charge",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "class": "FACT",
    "reasoning": "Verbatim quote confirmed live via raw curl of fireworks.ai/pricing (not markdown-converted, to dodge the icon-cell trap): \"Pay per GPU second, with no extra charges for start-up times\" sits as the subhead of the page's \"On-Demand Pricing\" section, directly above the \"On demand deployments\" GPU-rate table (H100 $7-8/hr, B200 $10-13/hr, etc., billed per-second per the docs). I independently pulled a second Fireworks source, docs.fireworks.ai/faq/deployment/ondemand/billing-scaling, which corroborates the mechanism in different words: \"Charges start when the server begins accepting requests\" / \"Billed by GPU-second for each active instance\" — meaning the boot/model-loading window before a replica is ready is described as unbilled, consistent with fireworks.ai/docs/guides/ondemand-deployments noting \"cold-start latency (up to a few minutes)\" for scale-from-zero. On the bar's core question, this is a trivial NO: a billing-metering boundary on dedicated GPU rental never touches the token stream, model choice, or amount of thinking — nothing about what the model reads changes. But per this register's own SCHEMA.md rule, \"A documentation sentence is the provider's word, however clearly it reads, and caps at CONTRACTUAL_ONLY. Only a measurement or a structural argument ... supports PASS_ABSOLUTE.\" Both corroborating sources here are Fireworks' own documentation about their own internal billing engine; neither the miner nor this adjudication captured an actual invoice/usage-log diff across a real cold-start event proving the pre-ready window carries zero charge. That is exactly the pattern that got the structurally-identical entry id 92 (OpenAI previous_response_id billing-parity claim) demoted from pass to CONTRACTUAL_ONLY on adversarial audit despite an equally clean verbatim quote. Same cap applies here.",
    "savings": "UNQUANTIFIED — avoids billing for the \"up to a few minutes\" scale-from-zero cold-start window Fireworks' own on-demand deployment guide describes. Back-of-envelope using the pricing table's own on-demand rates (H100/H200 $7-8/hr, B200 $10-13/hr, B300 $12-15/hr as of Aug 2026): a 2-minute avoided cold-start charge is roughly $0.23-$0.50 per scale-up event depending on GPU tier — real value depends entirely on how often a given deployment scales from zero, which the source does not quantify.",
    "provenance": "primary-doc",
    "telemetrySignal": "A Fireworks customer could self-verify from their own account: pull deployment \"ready\" timestamps from the dashboard/status API and diff them against invoiced GPU-second line items to see whether the pre-ready boot window is excluded from the bill. That check requires a live paid account and an actual cold-start event; neither the miner's citation nor this adjudication performed it, so from outside the account this claim is not independently observable — it rests entirely on the vendor's documentation matching its actual billing engine.",
    "providers": [
      "Fireworks AI"
    ],
    "verifiedAgainst": "https://fireworks.ai/pricing (read 2026-08-30); corroborated by https://docs.fireworks.ai/faq/deployment/ondemand/billing-scaling (read 2026-08-30) and https://fireworks.ai/docs/guides/ondemand-deployments (read 2026-08-30)",
    "whoActsOnIt": "Infra/procurement engineers choosing between Fireworks' serverless (per-token) and On-Demand (per-GPU-second, autoscale-to-zero) deployment tiers, and budgeting for scale-from-zero cost when sizing dedicated deployments.",
    "estimatedReach": "NICHE — applies only to customers running Fireworks On-Demand dedicated GPU deployments with autoscale-to-zero enabled, not the higher-volume serverless per-token customer base.",
    "implementationNote": "Do not present this as a verified savings figure — present it as a documented billing policy pending independent confirmation. If cited publicly, pair it with the caveat that no invoice/usage-log measurement backs it yet.",
    "trap": "Textbook instance of the register's own cap rule: a pure infra-billing claim with zero token/model impact looks like an obvious PASS_ABSOLUTE candidate because it's \"orthogonal to the wire\" — but orthogonality to tokens doesn't grant PASS status when the only evidence for the claim itself is the vendor's documentation. Compare id 92 (OpenAI previous_response_id billing parity), demoted from pass to CONTRACTUAL_ONLY on exactly this reasoning despite an equally clean verbatim quote.",
    "settlingExperiment": "Spin up a real Fireworks On-Demand deployment with scale-to-zero, force a cold start (send a request after the scale-to-zero window elapses), capture the deployment's \"ready\" timestamp from the API/dashboard, then pull the actual GPU-second usage/invoice line for that period and confirm the pre-ready boot window is billed at zero. That measurement — not another doc quote — is what would move this from CONTRACTUAL_ONLY to PASS_ABSOLUTE."
  },
  {
    "drop": false,
    "name": "Replicate \"fast booting fine-tunes\" idle-billing carve-out for private models",
    "class": "provider-billing-carveout",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "providers": [
      "Replicate"
    ],
    "verifiedAgainst": "https://replicate.com/pricing (read 2026-08-30)",
    "provenance": "primary-doc",
    "reasoning": "Verified verbatim on replicate.com/pricing (raw HTML, not markdown-converted, per the icon/markdown-drop trap): \"For fast booting fine-tunes you'll only be billed for the time the model is active and processing your requests, so you won't pay for idle time like with other private models. Fast booting fine-tunes are labeled as such in the model's version list.\" I additionally fetched the linked billing docs page (replicate.com/docs/topics/billing#fast-booting-fine-tunes), a second, independent location that repeats the claim and adds the mechanism: \"This works by using a common, shared pool of hardware running a base model. In these cases, we only ever charge you for the time the model is active and processing your requests.\" Applying the bar: this does not change the sequence of tokens sent, does not substitute a different model or weights (same fine-tune, applied to a shared base-model pool instead of a dedicated instance), and does not alter how much \"thinking\" happens — the request/response contract on the wire is identical either way; only which backend hardware class serves it, and thus what Replicate meters, differs. So on the narrow technical question the answer is NO throughout. But the entire claim — both the economics (idle seconds truly go unbilled) and the mechanism (shared-hardware serving is output-equivalent to a dedicated instance) — rests solely on Replicate's own pricing and docs prose. There is no independently checkable telemetry, sample invoice, or serving source code confirming either half, and per the adjudication rule a documentation assertion like this caps at CONTRACTUAL_ONLY rather than any PASS_* tier, even when the quote itself checks out cleanly and is cross-confirmed on two of Replicate's own pages.",
    "savings": "UNQUANTIFIED — Replicate gives no percentage or dollar figure. Magnitude depends entirely on how much idle/setup time a workload would otherwise accrue on a dedicated private-model instance: a bursty, low-QPS deployment where the instance sits idle most of the time could see the bill fall dramatically (idle+setup time can dominate a low-traffic dedicated instance's uptime); a steady, high-QPS deployment that's rarely idle would see little to no benefit since it was barely paying for idle time anyway.",
    "telemetrySignal": "A spend audit COULD observe this: Replicate's dashboard/API itemizes predictions and instance time by model version and by second, so comparing per-second billed line items for a version labeled \"fast booting\" against a standard private-model deployment of similar size/traffic would show whether idle/setup seconds are billed at $0 for the labeled version and non-zero for the unlabeled one. I did not run this audit (no paid Replicate account/invoice access), so this states what is observable, not what was observed — the pass/fail here rests on vendor prose alone.",
    "implementationNote": "No code or API change is required to get the carve-out — it is automatic and entirely provider-controlled, tied to whether Replicate itself can serve a given private fine-tune from its shared base-model pool. It shows up only as a \"fast booting\" label in that fine-tune's version list; it is not a flag or parameter the customer sets. Practical action: before assuming idle time is free, check the model's version list for the label. Any private model or deployment WITHOUT that label — which is the default and covers most custom Cog-packaged private models on dedicated hardware — is billed for setup + idle + active uptime exactly as before.",
    "whoActsOnIt": "Replicate customers who train and deploy private fine-tuned models that Replicate's infra can serve from a shared base-model pool (per the docs, this is a specific optimization Replicate applies to certain fine-tunes, not a general private-model option). There is no configuration to act on beyond checking the label — the customer cannot opt a given model into \"fast booting\" status; Replicate decides eligibility.",
    "estimatedReach": "Narrow and provider-gated. Only fine-tunes Replicate itself is able to serve from a shared, common base-model pool qualify (the docs suggest this is tied to Replicate's own training pipeline for specific supported base models, most plausibly LoRA-style image-model fine-tunes, not arbitrary custom Cog containers). The much larger population of general private models and \"deployments\" on dedicated hardware — explicitly contrasted in the same docs paragraph — remains billed for full setup+idle+active uptime by default and is unaffected by this carve-out.",
    "trap": "Fetched both replicate.com/pricing and replicate.com/docs/topics/billing as raw HTML via curl -sL (trap #3 — markdown conversion can silently drop content) and located the exact quote via string search rather than trusting a summary. No GitHub repo is involved, so the >=500-star check and PR-merge-ancestry checks (traps 1, 2, 5) do not apply. The trap that DOES apply, and is the deciding factor: a vendor pricing/docs sentence — even one cross-confirmed on two separate pages and enriched with a plausible mechanism (\"shared pool of hardware running a base model\") — is still only the provider's word on both the billing behavior and the output-equivalence of shared-vs-dedicated serving, so it cannot be pushed above CONTRACTUAL_ONLY without independent invoice or telemetry evidence.",
    "settlingExperiment": "To move this from CONTRACTUAL_ONLY toward a PASS_* tier: (1) create a paid Replicate account, train/use a model version labeled \"fast booting\" and one standard private-model deployment of comparable size, send identical requests with deliberate idle gaps to both, then pull itemized per-second billing via the Replicate API/dashboard to confirm idle seconds are actually $0-billed on the labeled version and non-zero on the other; (2) send the same prompt/input to both and hash-compare outputs to confirm the shared-base-model serving path is output-identical to the dedicated-instance path, ruling out quantization/batching drift from multi-tenant serving."
  },
  {
    "drop": false,
    "name": "Hugging Face Inference Endpoints: automatic scale-to-zero after 15 minutes idle",
    "class": "infra-idle-shutdown / dedicated-endpoint-provisioning",
    "providers": [
      "Hugging Face"
    ],
    "strictVerdict": "PASS_ABSOLUTE",
    "provenance": "primary-doc",
    "verifiedAgainst": "https://huggingface.co/docs/inference-endpoints/en/autoscaling (read 2026-08-30); cross-verified against https://huggingface.co/docs/inference-endpoints/en/pricing (read 2026-08-30)",
    "reasoning": "Fetched the cited page raw (curl -sL) and confirmed the quote verbatim, unedited, in the live \"Scaling to 0\" section: \"When an endpoint remains idle without receiving any requests for over 15 minutes, the system automatically scales down the endpoint to 0 replicas.\" Cross-checked the separate HF pricing doc raw HTML, which states billing accrues only \"while successfully deployed Endpoints (ready to serve) are initializing and in a running state\" — a 0-replica endpoint is neither, so idle time beyond 15 minutes is genuinely unbilled compute, confirming the claim from a second independent primary page rather than trusting one sentence. This is a pure capacity/billing toggle: enabling it never changes the input tokens sent, never swaps the model or model revision serving the endpoint, and never alters how much computation a given request does once a replica is up — the model that answers after a cold start is the identical deployed weights. The only observable side effect is a 502 during the cold-start window on the first post-idle request (a latency/availability cost, not a token-sequence, model-identity, or thinking-budget change), so it clears the bar cleanly. One wrinkle resolved during verification: the pricing page separately notes \"Scaled to Zero\" endpoints still count against instance \"quota\" (a resource-limit concept) even though they are not billed — that quota clause does not contradict the billing-halt claim, it is a different accounting axis (allowed concurrent endpoints vs. dollars charged), so it does not downgrade the verdict.",
    "telemetrySignal": "Directly observable in HF's own billing/invoice line items or the endpoint dashboard: per-instance GPU/CPU-hour charges drop to $0 for any idle span exceeding the 15-minute grace window while the endpoint shows 0 replicas, and charges resume only when the next request's cold start (visible client-side as a burst of 502 Bad Gateway responses until the replica is ready) brings a replica back up.",
    "savings": "100% of the per-instance hourly rate is avoided for every idle minute beyond the 15-minute window. HF's own pricing page lists instance rates from $0.032/hr (small CPU) up past $20-40+/hr for large multi-GPU instances, billed to the minute; a dev/staging or low-traffic endpoint idle nights/weekends can plausibly cut 60-90%+ of its compute bill this way. Cost is a cold-start delay on the next request (duration model-size-dependent; doc gives no fixed number) plus, per the pricing page, the endpoint still occupies an instance \"quota\" slot unless explicitly paused.",
    "estimatedReach": "Users of Hugging Face's dedicated (paid) Inference Endpoints product specifically — not the shared/serverless HF Inference API, and not other providers. Most relevant to endpoints with bursty or low/off-hours traffic (dev, staging, internal tools, low-QPS production models) where 15+ minute idle gaps are common; low value for endpoints under constant load.",
    "whoActsOnIt": "The endpoint owner/operator, who must explicitly opt in via the endpoint's Settings page under \"Automatic Scale-to-Zero\" (or the equivalent API/IaC field) — it is not default-on.",
    "implementationNote": "Opt-in per endpoint (Settings > Automatic Scale-to-Zero). No built-in request queue — HF's own docs recommend building a client-side retry/queue to absorb the 502s during cold start. Scale-down check runs on a ~2-minute cadence with a 300-second stabilization window per the autoscaling doc, so effective idle-to-zero timing is 15 min plus that cadence, not a hard 15:00 cutoff.",
    "trap": "Almost mis-scored via CONTRACTUAL_ONLY: at first glance this looks like \"a documentation sentence asserting identity\" (the kind that caps at CONTRACTUAL_ONLY), but re-reading shows it asserts an infra mechanism (replica count business a billing state), not an identity claim about model output — so the cap doesn't apply. Also nearly flagged the pricing page's \"quota\" language as contradicting the billing-halt claim; raw-HTML re-read showed quota (instance-count limit) and dollar billing (compute-hour charge) are stated as two distinct things on that same page, so no actual conflict."
  },
  {
    "name": "Together AI Provisioned Throughput (PTU): reserved per-minute capacity vs per-token serverless",
    "class": "reserved-capacity-billing",
    "providers": [
      "Together AI"
    ],
    "drop": false,
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "Verbatim quote confirmed on together.ai/pricing via raw curl fetch: \"Reserve dedicated capacity in throughput units (PTUs). Each PTU represents fixed capacity. The tokens-per-minute it delivers depends on the model and the token type.\" Together's blog additionally states \"The inference API for Provisioned Throughput, Dedicated Inference and Serverless Inference is identical\" -- same weights, same tokens, same model answering, only the billing unit changes (per-minute reserved capacity vs per-token metered). That satisfies the bar's identity question with a NO, but identity rests solely on the vendor's own assertion, so per the rules this caps at CONTRACTUAL_ONLY and can never reach PASS_*. Independent finding beyond what the miner reported: reading the page's own calculator JS, PTU capacity-per-unit is DERIVED from serverless per-token price via `TPM per PTU = RATE / (pricePer1M/1e6)` where RATE=$0.05/PTU-minute (a real, published, fixed rate -- contradicting the candidate's \"no published fixed $/PTU rate\" framing). Algebraically this means PTU cost equals same-model serverless cost at 100% utilization and exceeds it at any lower utilization -- reserved capacity cannot beat metered pricing for the identical model/workload. The \"83% lower\" headline figure is only reachable because the calculator's \"Compare vs.\" dropdown is hard-wired to a separate closed-source-competitor CMS collection (Claude/Gemini/GPT-family models), not to Together's own serverless price for the same open-weight model -- so the marketed saving is really \"switch to a cheaper open model,\" misattributed to the PTU billing mechanism.",
    "verifiedAgainst": "https://www.together.ai/pricing (read 2026-08-30); corroborated at https://www.together.ai/blog/provisioned-throughput (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit can observe this directly: reserved PTU capacity bills as a flat recurring line item (e.g. $2,160/PTU/30-day-month) distinct from metered per-token line items. Divide actual tokens processed in the billing period by the flat spend to get an effective $/token, then compare to Together's published serverless per-token price for the same model. Because the vendor's own capacity formula pegs PTU cost to equal serverless cost at 100% utilization, any effective rate above the serverless sticker price is direct proof the reservation is currently a net loss versus just paying per-token.",
    "savings": "None for a like-for-like (same model, same workload) comparison -- Together's own formula pegs PTU cost to serverless cost at full utilization and makes it worse below full utilization. UNQUANTIFIED for the marketed \"83% lower\" / \"$21,600/mo\" figures: these are self-reported marketing-calculator defaults comparing a cheap open-weight model under PTU billing against an unrelated, pricier closed-source competitor model under serverless billing -- not an audited PTU-vs-serverless delta for the same model. Real value of PTU is an uptime SLA and guaranteed throughput, not a cost discount.",
    "estimatedReach": "Narrow. Limited to three Together-hosted open-weight models (MiniMax M3, Kimi K3, GLM-5.2) as of the read date, one-month minimum commitment (~$2,160/PTU/mo baseline), and only economically defensible for teams with steady, high, predictable sustained throughput on one of those specific models who value an uptime SLA over cost savings.",
    "implementationNote": "Before reserving, compute sustained (not peak) req/sec and compare the resulting effective $/token against Together's serverless price for the same model -- per the vendor's own capacity formula, PTU only breaks even at 100% utilization and costs more below it. Treat PTU as an SLA/latency-guarantee purchase, not a cost-optimization technique, and never use the on-page calculator's default \"Compare vs.\" competitor-model baseline as evidence of PTU savings on your own model.",
    "trap": "The calculator's \"Compare vs.\" dropdown only ever pulls from a closed-source-competitor CMS collection (data-models=\"prop\"), so it structurally cannot show PTU-vs-serverless for the same open-weight model -- the one comparison that would actually test whether reserved billing saves money. The headline savings figure is a model-swap benefit relabeled as a billing-mechanism benefit.",
    "whoActsOnIt": "FinOps/infra teams evaluating Together AI for high-volume production inference on MiniMax M3, Kimi K3, or GLM-5.2 who need throughput/uptime guarantees, and who should independently verify utilization economics rather than trusting the calculator's cross-model comparison."
  },
  {
    "drop": false,
    "strictVerdict": "CONTRACTUAL_ONLY",
    "name": "Resumable Streams (Vercel AI SDK `useChat({resume:true})` + resumable-stream/Redis): client reconnect re-attaches to the in-flight generation instead of re-invoking the model",
    "class": "invocation-deduplication / stream-transport-continuity",
    "providers": [
      "Vercel AI SDK",
      "resumable-stream (Vercel, npm/GitHub)"
    ],
    "reasoning": "Confirmed the quote is verbatim on the cited page, then went past it — a doc sentence asserting identity normally caps at CONTRACTUAL_ONLY, so I cloned github.com/vercel/resumable-stream (569 stars, re-checked via `gh repo view` at read time) and read src/runtime.ts directly. `createNewResumableStream` gates the user-supplied `makeStream()` (the closure that actually drives the LLM `streamText` call) behind a single atomic Redis INCR per streamId; every later call for that streamId — including client reconnects — routes through `resumeExistingStream`/`resumeStream`, which take no `makeStream` argument at all and structurally cannot re-invoke the model. The producer's own read loop is kept alive by `waitUntil` (Next.js `after()`) independent of any specific client Response object, which is exactly the mechanism behind the docs' claim: \"client-side aborts are treated as disconnects... Closing a tab, refreshing the page, or calling stop() only closes the current HTTP connection and should not cancel the underlying generation.\" Because the model is invoked exactly once per streamId no matter how many times a client detaches and re-subscribes, and nothing about the request payload or generation parameters ever differs based on reconnect count, this clears PASS_ABSOLUTE on verified mechanism, not on vendor assertion alone.",
    "verifiedAgainst": "https://ai-sdk.dev/docs/ai-sdk-ui/chatbot-resume-streams (read 2026-08-30); cross-checked against github.com/vercel/resumable-stream source (src/runtime.ts, README \"How it works\") (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit could watch provider-side request logs for one-to-one correspondence between chat turns and completions/messages calls: with resumable streams correctly wired, the number of model API invocations for a given chat message stays at exactly 1 regardless of how many GET /resume (reconnect) requests hit the app after a disconnect. On the Redis side, the sentinel key for a streamId is INCRemented exactly once past the producer-creation threshold; all subsequent listener attaches are visible as pubsub SUBSCRIBEs with no corresponding new INCR-to-1 transition or new upstream model call.",
    "savings": "UNQUANTIFIED by Vercel — no aggregate percentage or dollar figure published anywhere I found. The prize is proportional to (mid-stream client disconnect rate) x (average generation cost): it displaces the common naive chat-app fallback where a lost connection forces the user to resend the same turn, paying for two full generations instead of one. Real-world size depends entirely on an app's own disconnect telemetry (mobile network drops, accidental tab closes, page refresh mid-stream), which Vercel does not report in aggregate.",
    "whoActsOnIt": "Application engineers running production chat UIs on the Vercel AI SDK (`ai` package: ~23.6M npm downloads/week as of 2026-08-30) who opt in to `useChat({resume:true})`, provision a Redis instance, and implement the POST(create)/GET(resume) endpoint split or the idempotent `resumableStream` API from the `resumable-stream` package (~422K npm downloads/week as of 2026-08-30, so real adoption beyond the reference demo). It is an opt-in engineering pattern, not a default platform behavior or a knob a non-developer could flip.",
    "implementationNote": "Requires the `resumable-stream` npm package, a Redis (or Redis-compatible) instance, `waitUntil`/Next.js `after()` to keep the serverless invocation alive past the client response, and app-side persistence mapping chatId to the active streamId. The dedup guarantee lives in the library's INCR-gated producer/consumer split (verified in src/runtime.ts) — a hand-rolled reimplementation that calls its own generation function on every GET instead of using `resumeExistingStream`/`resumeStream` would silently lose the property this candidate claims, so the win is contingent on using the documented API surface as shipped.",
    "trap": "This is close kin to the \"doc sentence asserting identity caps at CONTRACTUAL_ONLY\" trap — the quoted sentence is precisely that kind of self-serving identity claim. It only escapes the cap because independent inspection of the actual open-source mechanism (not just the prose) confirms the single-invocation guarantee at the code level, satisfying the register's \"re-verify everything\" mandate rather than resting on Vercel's word.",
    "estimatedReach": "Real and growing but opt-in: the `ai` SDK core package sees ~23.6M weekly npm downloads (checked via npm downloads API, 2026-08-30) and `resumable-stream` itself sees ~422K weekly downloads — well beyond a toy/demo package — but the feature requires deliberate Redis provisioning and route restructuring, so actual production usage is a fraction of total AI SDK deployments, not universal.",
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "CONTRACTUAL_ONLY",
      "why": "Re-cloned github.com/vercel/resumable-stream myself (gh repo view confirms 569 stars, matching the entry) and read src/runtime.ts line-by-line — the entry's central mechanistic claim does not survive contact with the code it cites. The entry asserts: \"`createNewResumableStream` gates the user-supplied `makeStream()` ... behind a single atomic Redis INCR per streamId.\" That is false. `createNewResumableStream` (runtime.ts:57-73, 127-219) never calls `.incr()` — it calls a plain `ctx.publisher.set(sentinel, \"1\", {EX:86400})` (an unconditional overwrite, not a compare-and-set/NX), and the internal implementation unconditionally executes `const stream = makeStream()` (line 169) on EVERY invocation with zero check for an already-running producer. The code's own comment admits this: \"This is ultimately racy if two requests for the same ID come at the same time. But this library is for the case where that would not happen\" (runtime.ts:143-144) — the library disclaims a structural guarantee here. The atomic `incr()`-gated dedup logic the entry describes is real, but lives in a DIFFERENT function entirely: internal `createResumableStream` / public `resumableStream()` (the README's \"Idempotent API\"), via `incrOrDone()` at runtime.ts:236 and :325-333.\n\nI independently fetched https://ai-sdk.dev/docs/ai-sdk-ui/chatbot-resume-streams (the entry's own cited source) and confirmed its POST handler calls `createNewResumableStream` (the ungated, always-invokes-makeStream function) and its GET handler calls `resumeExistingStream` (correctly gated, no makeStream arg) — i.e. exactly the README's \"Usage with explicit resumption\" split, NOT the INCR-atomic \"Idempotent API\" path the entry attributes the guarantee to.\n\nSo in the actually-documented, actually-cited integration, the single-invocation property rests entirely on an application/client-level contract — the POST route firing once per submitted turn, and `useChat({resume:true})` choosing GET over re-POST on reconnect — not on any Redis-atomic structure preventing a second model call. If a client retried the POST after a dropped connection (precisely the failure mode this feature targets), `createNewResumableStream` would run again, `makeStream()` would fire again, and the model would be invoked twice; nothing in the code stops it.\n\nThis is exactly the trap the entry itself named (\"doc sentence asserting identity caps at CONTRACTUAL_ONLY\") — it just inspected the wrong function and concluded the escape succeeded, when for the code path its own citation uses, it doesn't. Peripheral numbers do check out (422,127 weekly resumable-stream downloads, 23,560,904 weekly `ai` downloads, both re-verified live via the npm downloads API on 2026-08-30, matching the entry's 422K/23.6M), so this isn't fabrication — it's a real atomic-gated function conflated with a different, ungated sibling under the same claim. Downgrade to CONTRACTUAL_ONLY: the no-double-invocation property for the documented/cited usage is a behavioral contract between client and correctly-wired routes, not a verified structural/atomic guarantee."
    }
  },
  {
    "drop": false,
    "name": "Azure Foundry PTU Reservation \"Shared Scope\" (Cross-Subscription Billing Allocation)",
    "class": "reservation-billing-scope",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "provenance": "primary-doc",
    "providers": [
      "Microsoft Azure / Microsoft Foundry (Azure OpenAI)"
    ],
    "verifiedAgainst": "https://learn.microsoft.com/en-us/azure/cost-management-billing/reservations/microsoft-foundry (read 2026-08-30)",
    "reasoning": "Fetched the cited page raw (curl -sL, 69,714 bytes) and confirmed the page is titled \"Save costs with Microsoft Foundry Provisioned Throughput Reservations\" and is specifically about PTU capacity reservations (not generic boilerplate misapplied). The verbatim quote is present word-for-word: \"Shared scope - Applies the reservation discount to matching resources in eligible subscriptions that are in the billing context... The reservation shared scope would include multiple Microsoft Entra tenants in an enrollment.\" Applying the bar: this is purely a billing/accounting-allocation setting that controls which subscription's PTU hourly usage a pre-purchased reservation's discount is applied against during Azure's cost-reconciliation process. It has zero effect on the model-serving path — the deployment, the request payload, the model version, and the amount of inference compute performed are identical whether the reservation is scoped to one subscription or shared across an EA enrollment. So the bar question (\"does the model read a different sequence of tokens / different model / different thinking\") answers NO on its face. But per the register rule, that \"no change\" conclusion rests entirely on trusting Microsoft's own account of how their internal billing engine reconciles hourly PTU meter usage against reservation entitlements — an opaque, non-verifiable-from-outside process. A spend audit or API caller cannot observe the wire-level request to confirm nothing changed; they can only observe the resulting invoice line items. That is exactly the \"documentation sentence asserting identity\" case the rules say caps at CONTRACTUAL_ONLY, never a PASS_* tier, regardless of how mechanically plausible the non-interference claim is. This is also not a re-skin of prompt-cache TTL, batch tier, or semantic caching — it is a distinct capacity-commitment billing-scope lever (analogous to AWS Reserved Instance / Savings Plan billing-family scoping, but specific to Foundry PTU), so it is not a duplicate concept.",
    "telemetrySignal": "Cannot be observed at the API/wire level at all — no request header, token, or model-selection field changes. It is only visible in Azure Cost Management / EA reconciliation reports: the effective hourly rate charged against a given subscription's PTU deployment (discounted reservation rate vs. full pay-as-you-go PTU rate) and which reservation SKU the usage was applied against. A FinOps audit comparing per-subscription invoiced PTU rates before/after switching a reservation from Single-subscription to Shared scope is the only way to see this technique's effect.",
    "savings": "UNQUANTIFIED. No dollar figure is given anywhere in the source. The claimed prize is avoiding a specific waste scenario — one subscription's PTU deployment sitting idle under a paid-for reservation while an identical deployment in a sibling subscription (or Entra tenant) within the same EA enrollment pays full on-demand PTU pricing — but the doc gives no example of the delta this closes.",
    "estimatedReach": "Narrow. Applies only to Azure EA or Microsoft Customer Agreement enterprise customers who (a) have purchased a Microsoft Foundry Provisioned Throughput reservation and (b) run matching PTU deployments across more than one subscription or Entra tenant within the same billing context. Irrelevant to pay-as-you-go PTU users on a single subscription and irrelevant to any non-PTU (token-metered) Azure OpenAI usage.",
    "implementationNote": "Pure billing configuration, no code or request changes: at reservation purchase time (or via Exchange after purchase) in the Azure Portal Reservations blade, set the reservation's Scope to \"Shared\" instead of \"Single subscription\" or \"Single resource group.\" Requires EA enrollment or MCA billing-profile admin rights; for EA program only admin/sales agents can purchase the reservation at all per the same page.",
    "whoActsOnIt": "Cloud FinOps / billing administrators managing a multi-subscription or multi-tenant Azure EA enrollment that runs Microsoft Foundry PTU deployments — not application developers and not anyone touching the inference request path.",
    "trap": "The tempting misreading is to treat \"reservation\" language as implying something about capacity routing or request handling for the model itself; it is entirely a downstream billing-reconciliation setting. Also had to verify this section text is Foundry-PTU-specific and not generic Azure reservation-scope boilerplate pasted onto an unrelated product page — confirmed by fetching raw HTML and checking the page title/surrounding PTU-specific prose, not just string-matching the quote in isolation."
  },
  {
    "drop": false,
    "name": "Azure Foundry PTU reservation coverage is prorated by the minute, not rounded to the hour",
    "class": "finops-billing-mechanics",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "The deciding question is whether a different sequence of tokens is read, a different model answers, or a different amount of thinking happens — this candidate fails to even touch that axis, since it is purely an accounting rule for how Azure computes reservation-discount coverage against deployed PTU-minutes within an hour. Verbatim confirmed by raw curl fetch of the cited Microsoft Learn page (matched exactly, line 887): \"PTUs for partial-hour deployments are pro-rated based on the number of minutes the deployment exists during the hour... a 100 PTU deployment that exists for only 15 minutes of an hour period is considered as a 25 PTU deployment.\" Because the entire fact rests on Microsoft's own documentation describing its own billing system's internal rounding behavior, with nothing independently observable on the wire, it caps at CONTRACTUAL_ONLY per the register rule that a doc sentence never reaches PASS_*.",
    "provenance": "primary-doc",
    "providers": [
      "Microsoft Azure / Microsoft Foundry (Azure OpenAI)"
    ],
    "verifiedAgainst": "https://learn.microsoft.com/en-us/azure/cost-management-billing/reservations/microsoft-foundry (read 2026-08-30)",
    "savings": "UNQUANTIFIED — the mechanic is billing-neutral in isolation (partial-hour PTU deployments get precise pro-rata reservation credit rather than being rounded down to zero or up to full-hour credit). The only exploitable prize is avoiding wasted engineering effort or mistimed spend from a false assumption that Azure rounds PTU reservation application to whole hours (as some other clouds' reserved-capacity products do); no dollar figure is derivable without a specific customer's deployment create/delete churn pattern.",
    "telemetrySignal": "A spend audit could observe this in the Azure Cost Management reservation-utilization export: hours in which a PTU deployment was created or deleted mid-hour would show fractional PTU-hour quantities (e.g., 25 of 100 PTUs credited) rather than either full-hour rounding. It is visible only in billing/usage export data, never in model-request or token telemetry — there is nothing on the request side to observe.",
    "trap": "No repo/PR involved so the star-count and merge-status traps don't apply. Source was fetched raw via curl -sL (not markdown-converted) specifically to avoid the icon-cell-drop trap, though this passage is prose, not a table, so the risk was moot. The only trap that mattered — verbatim-quote existence at the cited URL — was checked directly and passed exactly, no paraphrase drift.",
    "whoActsOnIt": "FinOps/cloud-cost engineers managing Azure Foundry PTU reservations who might otherwise mistime deployment create/delete operations around hour boundaries under a false assumption of hour-level rounding waste. Not a prompt-engineering or application-developer concern — it never reaches request content.",
    "implementationNote": "No action is needed to capture this — Azure already prorates PTU reservation application by the minute, so there is no benefit to aligning PTU deployment start/stop timestamps to whole-hour boundaries. The only actionable use is correcting a FinOps misconception (imported from other clouds' reserved-instance products) that partial-hour usage is rounded rather than metered precisely.",
    "estimatedReach": "Narrow — limited to enterprise Azure OpenAI/Foundry customers who have purchased Provisioned Throughput reservations and who create/delete/resize PTU deployments mid-hour; a small, high-spend subset of Azure OpenAI users."
  },
  {
    "drop": false,
    "name": "FOCUS 1.4 Commitment Program Eligibility Details",
    "class": "billing-schema / commitment-eligibility-reporting (post-hoc invoice metadata, not a request-path technique)",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "The bar question is \"does the model read different tokens, does a different model answer, or does a different amount of thinking happen\" — the answer is trivially NO because this field is computed and written into the Cost and Usage Report AFTER a charge has already been metered and billed; it never touches the inference request or response. Verbatim from the raw HTML of the cited page: \"A new Commitment Program Eligibility Details column on Cost and Usage, paired with a supported feature of the same name. The column contains a JSON object with a CommitmentPrograms array, where each entry identifies a ProgramType the charge qualifies for, regardless of whether a commitment is currently applied.\" That last clause is the tell: eligibility is annotated identically whether or not any commitment action was taken, i.e. it is pure downstream reporting metadata over an already-issued charge, not a mechanism that alters or replays any API call. Unlike a \"the two responses are identical\" vendor claim, there is no behavioral-identity assertion here that needs trusting (so this is not the CONTRACTUAL_ONLY trap) — it is structurally impossible for an invoice-export schema field to affect a model call that already completed before the CUR row was generated.",
    "verifiedAgainst": "https://www.finops.org/insights/introducing-focus-1-4/ (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "FinOps Foundation (FOCUS spec, cross-provider) — applicable to any conformant cloud/AI provider's Cost and Usage Report export, including Bedrock/Vertex AI/Azure OpenAI provisioned-throughput and reserved-capacity line items"
    ],
    "telemetrySignal": "A spend audit could, in principle, diff rows in a FOCUS-1.4-conformant CUR export where CommitmentProgramEligibilityDetails is non-empty against rows where a commitment was actually applied, to size uncovered-but-eligible spend per provider. In practice this cannot be observed yet: the spec was only ratified 2026-06-04 and the column is an optional \"supported feature\" — no provider's billing export has been confirmed to populate it. So today a spend audit would see nothing; this is a future telemetry surface, not a present one.",
    "savings": "UNQUANTIFIED. The field only exposes eligibility for a commitment program on an already-issued charge — it does not itself apply a discount, buy a reservation, or reduce any bill. Any dollar savings depend entirely on a human/team separately purchasing the flagged reservation/savings-plan/PTU afterward, which FOCUS does not model or automate. No dollar figure or case study is given in the source.",
    "estimatedReach": "Currently near-zero in practice: spec ratified 2026-06-04, field is an optional supported feature, and no cloud/AI provider (AWS/Bedrock, Azure/Azure OpenAI, GCP/Vertex AI) has been confirmed to populate it in their CUR exports yet. Longer-term reach depends on provider adoption of FOCUS 1.4 conformance.",
    "implementationNote": "Not an LLM-request-path optimization at all — it is a FinOps billing-schema addition consumed entirely out-of-band from inference. To use it once providers implement it: ingest a FOCUS-1.4-conformant CUR, filter rows where CommitmentProgramEligibilityDetails is non-empty and no commitment is currently applied, then make a separate, human-driven purchasing decision about a reservation/savings-plan/PTU. The column itself saves nothing on its own; it only removes the \"reverse-engineering eligibility rules from provider documentation\" step the source names as the problem it solves.",
    "trap": "Checked trap #3 (raw HTML vs markdown) by curling the page directly rather than trusting a converted quote — confirmed byte-level match including the JSON-array field description. Checked the CONTRACTUAL_ONLY trap explicitly and ruled it out: this isn't a vendor asserting two request paths behave identically (which would require trust), it's a schema field that by construction exists only after billing, so PASS_ABSOLUTE is warranted rather than capped. Repo-star and bot-closed-PR/Spring-flow traps are not applicable (this is a standards-org announcement page, not a repo or PR).",
    "whoActsOnIt": "FinOps/procurement/finance teams doing invoice reconciliation and forward-looking commitment purchasing decisions — never anyone or anything in the live model-inference request path.",
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "INSUFFICIENT_EVIDENCE",
      "why": "Verbatim quote and June 4, 2026 ratification date both check out byte-for-byte against the raw HTML of the cited page, and the core mechanism (CommitmentProgramEligibilityDetails is a post-charge CUR column) genuinely never touches an inference request — the narrow bar question is correctly answered NO. But the entry's claim of LLM/AI relevance is not supported by, and is directly contradicted by, its own cited source. The 'providers' field asserts applicability to 'Bedrock/Vertex AI/Azure OpenAI provisioned-throughput and reserved-capacity line items,' yet grepping the source for bedrock|vertex|azure openai|provisioned|token|inference|llm turns up zero connection between FOCUS 1.4's commitment-eligibility feature and any AI provider. The only AI/token-billing content on the page explicitly scopes that work to FOCUS 1.5, described as future and unreleased: 'these updates lay the vital groundwork for FOCUS 1.5, which will bring unit and token economics into view... tracking inference value at the AI frontier' and 'Native AI support. FOCUS 1.5 is scoped to surface AI model identity and token consumption... in the Cost and Usage dataset.' CommitmentPrograms/ProgramType in 1.4 is generic cross-cloud commitment vocabulary (Flexible Spend Plans, Resource Reservations, Advance Resource Commitments) with no AI-specific instance named anywhere in the source, and the entry itself concedes no provider has confirmed populating the column and reach is 'near-zero.' Since a register of LLM cost techniques requires actual (or at least source-supported) applicability to LLM/inference spend, and the only evidence available affirmatively places that capability in an unreleased future spec version rather than the ratified 1.4 being reviewed, the entry's premise for inclusion is unsubstantiated — this downgrades from PASS_ABSOLUTE to INSUFFICIENT_EVIDENCE on the LLM-relevance question, even though the mechanical request-path claim itself was verified accurate."
    },
    "settlingExperiment": "Re-run the refuter check that lowered this: Verbatim quote and June 4, 2026 ratification date both check out byte-for-byte against the raw HTML of the cited page, and the core mechanism (CommitmentProgramEligibilityDetails is a post-charge CUR "
  },
  {
    "drop": false,
    "name": "OpenCost 1.121.0 llm-d inference cost metering (per-token GPU cost allocation for self-hosted vLLM)",
    "class": "finops-observability / cost-attribution (read-only metering, not a request-path intervention)",
    "strictVerdict": "PASS_ABSOLUTE",
    "reasoning": "Confirmed the cited OpenCost blog post (fetched raw HTML directly, not markdown) contains the verbatim quote unchanged: \"The integration uses metrics already present in an llm-d deployment: token throughput from vLLM (vllm:prompt_tokens_total, vllm:generation_tokens_total), GPU costs from OpenCost's existing allocation engine, and processing-time metrics that support separate cost calculations for input and output tokens.\" The post (dated 2026-07-21, authors Sima Nadler/IBM Research and Alex Meijer/OpenCost maintainer) describes a strictly read-only feature: it scrapes counters vLLM already emits to Prometheus and combines them with OpenCost's pre-existing GPU cost-allocation engine to publish a derived $/million-token figure (metric llm_total_hourly_cost, exposed via OpenCost's REST API). I found no mention of any required vLLM server flag, sampling/batching change, or proxy/gateway inserted into the inference path — the only \"gateway\" reference is about attributing a cost-share of an existing infra gateway, not intercepting requests. Because the mechanism never touches the request or response, the model reads the identical token sequence, the identical model answers, and identical thinking occurs, so it clears PASS_ABSOLUTE. One caveat verified and excluded from scope: the post also floats a future \"smart router — which llm-d is developing — [that] can use the REST API to factor per-token cost into routing decisions\"; this is explicitly framed as roadmap work by a separate project (llm-d), not part of what shipped in OpenCost 1.121.0 (\"This feature is available in OpenCost 1.121.0!\" refers to the metering/API only), so it does not contaminate this candidate's verdict — if/when that router ships and consumes cost data to change routing, it would need its own separate adjudication.",
    "verifiedAgainst": "https://opencost.io/blog/opencost-llmd-inference-cost/ (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit would see zero change in the actual inference traffic — same prompts, same vLLM sampling params, same token counts consumed. What it WOULD see: new Prometheus series (llm_total_hourly_cost and related metrics) scraped from OpenCost, and new fields in OpenCost's REST API responses carrying allocation-based vs usage-based $/M-token figures per model/deployment. Because this is purely additive instrumentation with no dependency in the inference call path, an audit diffing raw request/response payloads before and after enabling the feature would find no difference at all — the only observable delta is in the cost-monitoring stack, not the model-serving stack.",
    "savings": "UNQUANTIFIED as a direct saving, and the miner's own framing concedes this: \"the metering itself saves nothing on its own.\" The worked example in the source (self-hosted at 25% utilization: $4.00/M tokens allocation-based vs $1.00/M usage-based, vs a $2.00/M external API price) sizes the VISIBILITY gap the tool exposes, not a saving it produces — realizing any saving requires a separate human/automated action (rightsizing, consolidation, or a future router deprioritizing the underutilized model) that this candidate does not itself perform.",
    "providers": [
      "Self-hosted LLM inference on Kubernetes via vLLM + the llm-d project (model-agnostic — works for any model served through vLLM/llm-d, not tied to a specific model provider)"
    ],
    "estimatedReach": "Narrow. Applies only to organizations self-hosting inference on Kubernetes specifically via the llm-d project (a young, GPU-cluster-scale serving stack) with OpenCost >=1.121.0 installed; does not apply to any hosted-API consumer (OpenAI/Anthropic/etc.) and doesn't apply to self-hosted deployments outside llm-d/vLLM+Prometheus.",
    "implementationNote": "No inference-path changes needed: OpenCost reads vLLM's pre-existing Prometheus counters (vllm:prompt_tokens_total, vllm:generation_tokens_total) plus its own GPU allocation engine; ships as a metrics/REST-API addition in OpenCost 1.121.0, validated by the authors on a 109-GPU / 30-model cluster per the source. Repo star count re-verified at adjudication time via `gh repo view opencost/opencost`: 6,715 stars (>=500 threshold cleared; matches miner's claimed 6715 exactly).",
    "trap": "Checked and cleared: (5) repo stars re-verified live via gh CLI (6715, not trusted from miner) — pass; (3) fetched raw HTML via curl, not a markdown conversion, so no icon-only-cell or formatting loss risk (the page is prose/tables, not an icon matrix, but raw fetch was still used per policy); (4) checked for a pinned dependency rebuilding the outbound inference request (e.g., a proxy/gateway/router in the path) — found none; the only routing/gateway mentions are either cost-share accounting for an existing gateway or an explicitly future, separate llm-d \"smart router\" not shipped in this feature; (6) both citation and repo check carry the 2026-08-30 read date. Not a duplicate of prompt-cache TTL / batch tier / semantic caching — this is a distinct FinOps cost-attribution/observability concept.",
    "whoActsOnIt": "Platform/FinOps engineers or SRE teams operating self-hosted GPU inference clusters on Kubernetes via llm-d, who consume the exposed $/M-token figures to manually (today) or eventually semi-automatically (via the roadmapped llm-d smart router) rightsize, consolidate, or decommission underutilized self-hosted models — the savings lever is entirely downstream of this candidate."
  },
  {
    "drop": false,
    "name": "Vertex AI (Gemini Enterprise Agent Platform) dedicated online-prediction billing splits into a CUD-eligible Compute Engine SKU plus a separate, non-discounted Agent Platform Management Fee SKU",
    "class": "commitment-discount-passthrough / billing-sku-split",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "Verified verbatim against the live doc fetched today: 'When consuming from a reservation or spot capacity, billing is spread across two SKUs: the Compute Engine SKU with the label goog-vertex-ai-product:vertex-ai-online-prediction and the Agent Platform Management Fee SKU. This enables you to use your Committed Use Discounts (CUDs) in Agent Platform.' The page (title 'Use reservations with online inference | Gemini Enterprise Agent Platform') sizes reservations in A3 (H100 GPU) VMs, confirming this covers real dedicated-resource LLM serving, not just generic ML. Nothing about the outbound request, model, or token sequence changes — it is purely an accounting split so pre-existing general-purpose Compute Engine CUD tiers discount the infrastructure SKU while the co-billed management-fee SKU stays full price. Per the bar, a documentation sentence asserting this billing-identity caps the verdict at CONTRACTUAL_ONLY, never PASS_*, since the mechanism is only verifiable via the provider's own billing docs, not observable on the wire.",
    "verifiedAgainst": "https://docs.cloud.google.com/gemini-enterprise-agent-platform/machine-learning/predictions/use-reservations (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A customer's Cloud Billing detailed/BigQuery export for the same online-prediction deployment would show two separate SKU line items: one under goog-vertex-ai-product:vertex-ai-online-prediction (Compute Engine) carrying a CUD credit, and one under the Agent Platform Management Fee SKU with zero CUD credit — this split is directly auditable from the billing export without trusting any vendor claim. What the export alone cannot tell you is the blended savings rate, since that depends on the customer's specific CUD commitment size and machine/GPU series, which isn't published per-SKU.",
    "savings": "UNQUANTIFIED as a blended figure — the primary doc gives no percentage. Google's general Compute Engine CUD tables (secondary source, read 2026-08-30) show tiers of roughly 16-28% at 1-year and 42-63% at 3-year depending on commitment type and machine/GPU series (e.g. standard flexible CUDs 28%/46%; GPU-attached G2/G4 flexible CUDs only 16-21%/42-43%; select resource-based CUDs up to 55-63% at 3yr) — applied only to the Compute Engine infrastructure SKU. The co-billed Agent Platform Management Fee SKU gets 0% discount, so the true blended savings on total deployment spend is always lower than the raw CUD tier and scales with the infra-vs-management-fee cost ratio, which Google does not publish.",
    "providers": [
      "Google Cloud Vertex AI / Gemini Enterprise Agent Platform — dedicated/custom-model online-prediction deployments on Compute Engine reservations, not the pay-per-token Gemini foundation-model API"
    ],
    "estimatedReach": "Narrow: enterprise GCP customers who both (a) self-deploy custom or open models (or Model Garden models) as dedicated Vertex AI online-prediction endpoints on GPU/CPU VMs, and (b) already hold or are willing to buy Compute Engine CUDs/reservations covering that machine series. Does not reach customers using the standard pay-per-token Gemini API or Provisioned Throughput (GSU).",
    "implementationNote": "Purchase Compute Engine CUDs (or set up resource reservations) covering the exact machine/GPU series (e.g. A3/H100) the deployment will use, then deploy the Vertex AI online-prediction endpoint against that reservation (SPECIFIC_RESERVATION or ANY scope) so the goog-vertex-ai-product:vertex-ai-online-prediction SKU portion becomes CUD-eligible; confirm via the Cloud Billing SKU breakdown that the credit is actually landing on that line, separate from the un-discounted management-fee line.",
    "trap": "Two things could mislead a future adjudicator: (1) this is easily confused with Vertex's Generative AI Provisioned Throughput (GSU), already in the register — GSU is a token-metered commitment for the managed foundation-model API, whereas this is a Compute-Engine-reservation CUD passthrough for dedicated/custom online-prediction deployments; they are genuinely different products and different billing mechanics. (2) the eye-catching 'up to 55-63%' CUD headline applies only to select non-GPU machine series — GPU-attached flexible CUDs (the ones actually relevant to LLM-serving hosts like A3/H100 or G2/G4) are much lower (16-21% at 1yr, 42-43% at 3yr), so quoting the generic CUD percentage without checking the GPU-specific table overstates the real prize."
  },
  {
    "drop": false,
    "name": "CDR-to-invoice reconciliation transplanted as an independent LLM billing audit ledger",
    "class": "out-of-band billing audit / spend-integrity check — not a request-shaping or scheduling technique",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "The technique is defined as a self-logged request/response ledger compared against the provider's invoice line items strictly after the fact — an out-of-band accounting reconciliation, not a change to how requests are built or answered. By construction it cannot alter token sequence, model selection, or reasoning depth, since it runs entirely outside the inference call. Source quote verified verbatim by raw curl fetch and text search: \"Missing, delayed, or more than one CDR can lead to incorrect invoices and reporting... Billing reconciliation involves the comparison of customer invoices with the actual network use... every event that is chargeable is captured on an invoice and the amounts billed are proportional to the consumption of services.\" That establishes the telecom practice is real and matches the quote exactly. But the source (grepped in full) never mentions LLMs, tokens, GPT, Claude, OpenAI, or AI billing even once — the \"transplant\" to LLM spend is the miner's own unsourced inference, not something the cited article claims or that any practitioner is shown doing. Provenance for the LLM-specific half is therefore inferred, not primary-doc, and savings are correctly labeled UNQUANTIFIED since no case study of a caught LLM billing discrepancy is cited anywhere.",
    "verifiedAgainst": "https://www.neon-soft.com/blog/revenue-assurance-audit-a-telecom-operators-guide/ (read 2026-08-30)",
    "provenance": "inferred",
    "telemetrySignal": "A spend audit built this way could observe: per-request self-logged token counts, timestamps, and model IDs versus the provider's usage export/invoice totals — surfacing missing charges, duplicated line items, or a discount/cache-rate misapplication (e.g. cache-read pricing not honored). It categorically cannot observe anything about token content, model choice at inference time, or reasoning depth, because the check happens only after the response has already been returned and billed.",
    "savings": "UNQUANTIFIED",
    "providers": [
      "all"
    ],
    "trap": "No repo/PR/pricing-table trap applies here (no repo, no PR). The near-trap is provenance laundering: the source's silence on LLM billing was almost read as neutral rather than as zero evidence for the actual claim under review — the quote is 100% real but 100% about telecom, so it only supports half the candidate's claim.",
    "whoActsOnIt": "FinOps/platform engineering teams that already maintain an independent request/response log and want to cross-check it against vendor usage exports or invoices to catch billing drift.",
    "estimatedReach": "Narrow in practice: only teams that already self-log every LLM call at request granularity can run this at all; most teams rely solely on the provider's own dashboard, which is exactly the record this technique is meant to cross-check.",
    "implementationNote": "Feasibility depends on provider invoice/usage-export granularity — most major LLM providers expose daily or per-request usage data via a console/usage API, but a literal itemized \"invoice line item\" per request (the telecom-CDR analogue) is inconsistent across vendors, so full record-by-record reconciliation may only be partially achievable today.",
    "crosscheckOverride": {
      "from": "PASS_ABSOLUTE",
      "to": "INSUFFICIENT_EVIDENCE",
      "why": "Re-fetched the cited source raw (curl -sL to neon-soft.com/blog/revenue-assurance-audit-a-telecom-operators-guide/, 51KB) and grepped it for both quoted passages and for any LLM-domain vocabulary. Both quotes check out verbatim: \"Missing, delayed, or more than one CDR can lead to incorrect invoices and reporting\" (found at offset 3980) and \"Billing reconciliation involves the comparison of customer invoices with the actual network use... every event that is chargeable is captured on an invoice and the amounts billed are proportional to the consumption of services\" (offset 4216, exact match). So the telecom half of the entry is accurately sourced.\n\nBut a case-insensitive grep for LLM|GPT|Claude|OpenAI|Anthropic|token|AI billing|large language across the entire fetched page returns zero hits — confirmed independently, matching what the entry itself already admits (\"the source... never mentions LLMs, tokens, GPT, Claude, OpenAI, or AI billing even once\"). There is no second citation, case study, practitioner writeup, tool, or repo anywhere in the entry showing anyone actually runs CDR-style reconciliation against LLM provider invoices. The entry's own fields concede this directly: provenance is \"inferred,\" savings are \"UNQUANTIFIED,\" and the reasoning text states the LLM transplant is \"the miner's own unsourced inference, not something the cited article claims or that any practitioner is shown doing.\"\n\nThat is disqualifying on its own terms, independent of the wire-change question. This review's job is to grade whether a *documented technique* survives evidentiary scrutiny — here the sole citation supports only that telecom billing reconciliation exists, which nobody disputes; it supplies zero evidence that any team, tool, or provider performs an LLM-billing analogue. An entry cannot earn PASS_ABSOLUTE (or any PASS_* tier, which all presuppose a real technique whose mechanics you can inspect) when the domain-specific half of its own claim is admitted speculation with no primary source. The honest self-assessment inside the entry (provenance: inferred, savings: unquantified) already points to INSUFFICIENT_EVIDENCE; the strictVerdict of PASS_ABSOLUTE contradicts the entry's own evidence trail and should be downgraded, not accepted."
    },
    "settlingExperiment": "Re-run the refuter check that lowered this: Re-fetched the cited source raw (curl -sL to neon-soft.com/blog/revenue-assurance-audit-a-telecom-operators-guide/, 51KB) and grepped it for both quoted passages and for any LLM-domain vocabulary. Bot"
  },
  {
    "drop": false,
    "name": "Claude Code: output-style mid-session change is a silent no-op (cache-safe only because nothing changes)",
    "class": "prompt-caching",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "Fetched code.claude.com/docs/en/prompt-caching directly (WebFetch + raw curl, 509KB) and confirmed the #changing-output-style section contains the quote verbatim: \"Output style is part of the system prompt, which Claude Code reads once at session start. Changing it via /config or the outputStyle setting mid-session does not invalidate the cache, but the change also doesn't apply. Claude keeps using the style that was loaded at session start. The new style loads on the next /clear or restart.\" The citation is genuine, not fabricated, and the mechanism (system-prompt layer frozen at session start) is structurally identical to the doc's separate CLAUDE.md-mid-session-edit claim. However the miner's proposed PASS_ABSOLUTE fails the bar's explicit rule: this claim rests entirely on a single provider prose sentence asserting request-identity, with no independent verification performed (no captured request diff, no cache_creation_input_tokens/cache_read_input_tokens delta actually measured across a real /config toggle, no reproduced session). \"A documentation sentence asserting identity is THE PROVIDER'S WORD and caps at CONTRACTUAL_ONLY, never PASS_*\" — that is precisely this case, so the verdict is downgraded to CONTRACTUAL_ONLY rather than dropped, since the citation itself is sound.",
    "verifiedAgainst": "https://code.claude.com/docs/en/prompt-caching#changing-output-style (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "Anthropic"
    ],
    "telemetrySignal": "A spend audit COULD in principle observe this: run `claude -p ... --output-format json` or watch the OTel cache-token exporter / the /usage \"Prompt cache (main)\" line before and after a mid-session /config output-style change, and confirm cache_creation_input_tokens stays ~0 (no rebuild) while the response's style/behavior remains that of the ORIGINAL session-start setting, not the newly requested one. No such capture was performed for this adjudication — only the doc's own assertion was checked, which is why the verdict caps at CONTRACTUAL_ONLY rather than a PASS tier.",
    "savings": "UNQUANTIFIED, effectively None as a deliberate cost lever. The only \"avoided\" cost is a cache-invalidation event that never actually gets triggered, because Claude Code silently ignores the setting change rather than applying it — there is no scenario where a rational actor chooses this path to save money, since the alternative they actually wanted (the new style taking effect) requires /clear or restart, which is exactly the full cache-rebuild event this \"technique\" claims to avoid.",
    "trap": "Doc-sentence-as-proof: the entire claim is one paragraph of Anthropic's own prose asserting identity between pre- and post-toggle requests. No network capture, no reproduced Claude Code session, no OTel/--output-format json cache-token diff was run to independently confirm it. This is exactly the pattern the bar warns caps at CONTRACTUAL_ONLY, never PASS_*, regardless of how plausible or internally-consistent-with-neighboring-doc-sections the claim reads.",
    "whoActsOnIt": "Claude Code users/teams who monitor cache-hit ratios via /usage or the OTel exporter and would otherwise be confused by a 100% hit rate persisting through a /config output-style change; anyone advising teammates not to bother toggling output style mid-task since it silently won't apply until /clear or restart.",
    "estimatedReach": "Narrow — only Claude Code users who invoke /config or set outputStyle mid-session (most users set style once at session start or never touch it); not a broad-impact lever.",
    "implementationNote": "No implementation possible or needed — this is unavoidable default behavior of Claude Code's session-start-frozen system-prompt layer, not an opt-in optimization. The only actionable takeaway is procedural: to actually change output style, run /clear or restart rather than /config mid-session.",
    "settlingExperiment": "To promote past CONTRACTUAL_ONLY: start a real Claude Code session, note cache_read/cache_creation token counts via `claude -p \"hello\" --output-format json`, run /config to change outputStyle mid-session, send another turn, and independently confirm both (a) cache_creation_input_tokens stayed ~0 (wire-level identity) and (b) the response still exhibits the ORIGINAL style's formatting/behavior rather than the newly requested one (functional no-op) — rather than relying on the doc's prose alone."
  },
  {
    "drop": false,
    "name": "Claude Code: skill/command invocation appends rather than rewrites history (cached prefix survives)",
    "class": "prompt-caching-mechanics",
    "strictVerdict": "CONTRACTUAL_ONLY",
    "reasoning": "Fetched the cited page raw (curl -sL, 200 OK) and confirmed the exact anchor and verbatim sentence exist: \"Skills and commands inject their instructions as user messages at the point of invocation. Nothing earlier in the conversation changes.\" That sentence sits under an h2 titled \"Actions that keep the cache\" alongside similar assertions (permission-mode change, CLAUDE.md edits, output-style change), all sourced only to Anthropic's own prose about its internal message-construction behavior — no independent request-body trace, no cache_read_input_tokens/cache_creation_input_tokens comparison across a real skill-invocation turn was supplied by the miner or found elsewhere. Per the adjudication bar, a documentation sentence asserting identity of internal behavior is THE PROVIDER'S WORD and caps at CONTRACTUAL_ONLY, never PASS_*, regardless of plausibility. It also doesn't structurally fit a PASS_* archetype: invoking a skill genuinely appends new tokens the model reads (the skill's instructions) — this is not a no-op, metadata-only, or scheduling-only change; the narrower claim (that the pre-existing prefix stays byte-identical and keeps serving from cache) is exactly the sort of assertion that needs a wire-level capture, not vendor documentation, to verify.",
    "verifiedAgainst": "https://code.claude.com/docs/en/prompt-caching#invoking-skills-and-commands (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "Anthropic"
    ],
    "telemetrySignal": "A spend audit COULD observe this, but the miner didn't: run Claude Code with request/usage logging (--debug, or a local proxy, or Anthropic's usage field) and inspect cache_read_input_tokens vs cache_creation_input_tokens on the API turn immediately following a skill or command invocation. If the claim holds, cache_read_input_tokens should equal (or closely track) the token length of everything before the invocation, with only the injected skill content plus new output priced as fresh/creation tokens. This session did not capture that trace — the finding rests entirely on doc prose.",
    "savings": "UNQUANTIFIED — the docs imply the entire pre-invocation prefix (system prompt + prior turns) keeps reading at the ~10% cache-hit rate rather than being rebilled at full input price, but no measured before/after usage numbers exist to size this; magnitude scales with how much context has accumulated before the skill is invoked.",
    "settlingExperiment": "Instrument a real Claude Code session (via --debug output or a local MITM proxy on the Anthropic API traffic) to capture the outgoing message array and the returned usage object both immediately before and immediately after invoking a Skill or slash-command. Confirm (a) the token sequence up to the invocation point is byte-identical across both requests, and (b) the post-invocation request's cache_read_input_tokens matches the prior turn's total input length rather than falling to near-zero (which would indicate a cache miss / history rewrite).",
    "trap": "Provider-documentation-assertion trap: the only evidence is Anthropic's own doc sentence describing its internal request-construction behavior, not an independently reproduced wire-level trace — per the adjudication rule this caps at CONTRACTUAL_ONLY no matter how mechanically plausible the claim is (and it is plausible — Claude Code has to append skill content as a new message for the skill to have any effect at all, so the interesting/unverified part is narrowly whether the prior prefix is left untouched).",
    "whoActsOnIt": "Claude Code users and agent-harness builders relying on Skills/slash-commands to inject extra instructions mid-session without wanting to eat a full cache-invalidation penalty — relevant when deciding to prefer Skills/commands over context-rewriting operations like /compact or live CLAUDE.md edits, which the same doc page states DO break the cache.",
    "estimatedReach": "All Claude Code users who invoke Skills or slash-commands during a session with an already-populated (and therefore cache-eligible) conversation prefix — a large fraction of regular Claude Code usage, but unquantified here.",
    "implementationNote": "No configuration required — this is asserted to be Claude Code's default, inherent message-construction behavior, not an opt-in. To actually verify it (rather than trust the doc), capture real request/usage logs via `claude --debug` or a local proxy across a skill-invocation turn and diff cache_read_input_tokens before/after, per the settling experiment above."
  },
  {
    "drop": false,
    "name": "Claude Code /rewind: truncates to an already-cached prefix instead of rebuilding one, unlike /compact",
    "class": "context-checkpoint-cache-reuse",
    "strictVerdict": "FAIL",
    "provenance": "primary-doc",
    "verifiedAgainst": "https://code.claude.com/docs/en/prompt-caching#rewinding-the-conversation (read 2026-08-30)",
    "reasoning": "Fetched the page raw via curl -sL (509KB HTML, not the markdown conversion) and confirmed the exact quote is present verbatim: \"/rewind truncates your conversation back to an earlier turn. The remaining history is the same content the cache was built from at that point, and the system prompt and project context layers are unchanged, so the next request hits the earlier cache entry.\" That sentence is an identity assertion by the provider about their own closed-source CLI's internal request construction — not an independently observed byte-diff of two real Anthropic API requests. Worse, the same page's own \"Cache scope\" section (checked for caveats) admits the system prompt is dynamically built per session — it embeds working directory, platform, shell, OS version, auto-memory paths, and a git-status snapshot taken at session startup — so \"system prompt unchanged\" is a contingent runtime fact, not a static guarantee, and nothing here rules out CLAUDE.md edits or a newly-attached MCP server between the original turn and the rewind breaking the exact prefix match. Per the register's own rule, a documentation sentence asserting content/request identity caps at CONTRACTUAL_ONLY, never PASS_*, however plausible the mechanism sounds.",
    "telemetrySignal": "A spend audit could compare cache_read_input_tokens vs cache_creation_input_tokens in the Anthropic API usage stats (or Claude Code's own /cost and session transcript token accounting) on the request immediately following a /rewind — near-full cache_read with ~0 cache_creation would corroborate the claim for that session. It cannot currently be confirmed as a byte-for-byte wire fact from outside: Claude Code does not expose raw outbound request bodies by default (a --debug/--debug-file flag exists but was not exercised in this adjudication).",
    "savings": "UNQUANTIFIED — the doc gives no token or dollar figures. The prize is avoiding one cache-write-priced pass over the abandoned-path prefix (paid once by /compact's new summary) in favor of one cache-read-priced pass (/rewind); magnitude scales with how many tokens sit between the rewind point and the current turn, which is per-session and not stated anywhere in the source.",
    "providers": [
      "Anthropic"
    ],
    "estimatedReach": "Narrow: only Claude Code CLI users who choose /rewind over /compact specifically to abandon an exploratory path mid-session — a behavioral subset of an already Claude-Code-specific feature, not applicable to raw Anthropic API integrators.",
    "whoActsOnIt": "Claude Code users deciding, at the moment they want to abandon a dead-end path, whether to run /rewind (cheap, per this doc) or /compact (rebuilds the cache, per the same doc's explicit contrast). Not actionable for anyone calling the Messages API directly.",
    "implementationNote": "No code to ship — this is existing Claude Code REPL behavior (/rewind vs /compact). At most it's a workflow habit to document: \"when you want to abandon a path entirely, rewind instead of compact.\" Related feature docs: https://code.claude.com/docs/en/checkpointing.",
    "trap": "The doc's own \"Cache scope\" section undercuts the blanket \"system prompt... unchanged\" claim by describing exactly how much dynamic, session-specific material (cwd, platform, shell, OS version, git-status-at-startup) is baked into that system prompt — treating the rewind claim as PASS_ABSOLUTE would launder a provider assertion about internal request bytes into a fact nobody outside Anthropic can currently verify.",
    "settlingExperiment": "Run `claude --debug-file` (or equivalent request logging) across one real session: capture the raw outbound request body for the turn that originally built the cache at point N, then /rewind to N and capture the very next outbound request body; diff them byte-for-byte (or diff cache_creation_input_tokens/cache_read_input_tokens across the two API responses) to confirm the post-rewind prefix is truly byte-identical rather than merely doc-asserted to be. That would move this from CONTRACTUAL_ONLY to PASS_ABSOLUTE (or reveal it as FAIL if any dynamic layer differs).",
    "crosscheckOverride": {
      "from": "CONTRACTUAL_ONLY",
      "to": "FAIL",
      "why": "Re-fetched code.claude.com/docs/en/prompt-caching raw (509,313 bytes, matches entry's claimed size) and confirmed the quoted sentence is genuinely verbatim in the 'Rewinding the conversation' section, and that the entry's own cited 'Cache scope' caveat (cwd/platform/git-status baked into system prompt) is real but actually irrelevant to the within-session rewind claim, since the doc elsewhere establishes the system prompt is fixed once at session start and held in memory for the whole session (Changing output style / Editing CLAUDE.md sections) — so the entry's own 'trap' argument is weaker than presented. However, `gh search issues` on anthropics/claude-code surfaced concrete, reproduced, currently-OPEN counter-evidence to the specific quoted claim ('system prompt and project context layers are unchanged'): issue #85455 (CLI 2.1.226) shows /rewind to before the first user prompt — the exact 'abandon this path entirely' use case the register entry describes — drops the entire ~120-item skills listing and replays stale SessionStart hook output, leaving post-rewind context 'smaller than a fresh hello session.' That is a materially different, degraded token sequence, not an unchanged one. Corroborating: #55042 (independently confirmed by a second commenter, never fixed, only stale-bot-closed) shows Claude Code's Read-tool dedup cache survives rewind and returns stub responses pointing at tool_results erased by the truncation — silent content loss. #87893 (OPEN) shows rewind can be silently reverted on resume, further undermining the reliability of the 'next request hits the earlier cache entry' guarantee. Per the register's own bar ('any yes = FAIL', no carve-out for degenerate rewind targets), a claim that is not merely unverified but actively falsified by reproduced user reports for the technique's own headline use case does not get to sit at CONTRACTUAL_ONLY ('plausible, unverified provider prose') — it fails the mechanism test outright. Also flagging that the entry's proposed telemetrySignal (cache_read vs cache_creation token ratio) would not have caught the #85455 failure mode, since a hit on a shorter/degraded cached prefix still looks like a favorable cache ratio while silently dropping content."
    }
  },
  {
    "drop": false,
    "name": "Claude Code SessionStart resume-cost fields (context_tokens, prompt_cache_likely_expired, estimated_cache_write_usd)",
    "class": "hook-telemetry / cost-observability",
    "strictVerdict": "PASS_METADATA",
    "reasoning": "Verified verbatim against the live doc: \"SessionStart hooks also receive the four fields below. Your hook can use them to report what resuming a stale conversation costs before the first request, for example in a systemMessage.\" The doc's own schema draws an explicit fork the miner didn't cite but that settles the case: additionalContext is defined as the field \"added to Claude's context,\" while systemMessage (the natural sink for these cost numbers) is defined as surfacing \"to the user\" only, with some events discarding it entirely. The four cost fields (context_tokens, prompt_cache_likely_expired, estimated_cache_write_usd, seconds_since_last_response) arrive solely via hook stdin JSON, a channel with no route into the model's prompt unless a hook author manually re-injects them through additionalContext — an optional, separate act outside this candidate's scope. They are arithmetic on data Claude Code already has locally (transcript token count x published per-model price), not a model computation. No token sequence, model choice, or thinking budget changes as a result of these fields existing or being read.",
    "verifiedAgainst": "https://code.claude.com/docs/en/hooks#sessionstart-input (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "Anthropic"
    ],
    "savings": "None — explicitly not a savings mechanism. It is a pre-request cost estimate (in USD) shown to a human before they trigger a resume/fork, so any savings are indirect and behavioral (a user might choose not to resume a stale session after seeing estimated_cache_write_usd), not measurable as a wire or billing change.",
    "telemetrySignal": "A spend audit watching Anthropic API request/response bodies or usage/billing records will never see these four fields — they exist only in the hook subprocess's stdin JSON on the local machine. The only way to observe them is to instrument the hook itself (log its stdin) or read Claude Code's own debug log; they carry no billing signal and correspond to no line item.",
    "whoActsOnIt": "The human operator (via a custom SessionStart hook script) or nobody, if no hook is configured — never the model, never Claude Code's request-construction path, and never Anthropic's billing pipeline.",
    "implementationNote": "Requires Claude Code v2.1.251+. A hook author reads context_tokens / prompt_cache_likely_expired / estimated_cache_write_usd from SessionStart's stdin JSON when source is \"resume\" or \"fork,\" then returns a systemMessage (UI-only, per the doc's own systemMessage definition) warning the user of the resume cost before the first request goes out. No decision-control field here blocks or alters the resumed request itself.",
    "trap": "The tempting misread is conflating \"field with _usd in its name inside a cost-flavored JSON blob\" with \"affects billing.\" It doesn't — it's a locally computed estimate for human consumption, delivered on a channel (hook stdin) the doc elsewhere explicitly contrasts with the one field (additionalContext) that actually reaches the model. Also worth flagging for the register maintainer: this is telemetry/observability, not a spend-reduction technique, so it may belong in a different bucket than techniques that actually move dollars.",
    "estimatedReach": "Narrow and conditional: only Claude Code users on v2.1.251+ who (a) configure a SessionStart hook and (b) resume or fork a session with at least one prior Claude response in the transcript."
  },
  {
    "drop": false,
    "strictVerdict": "FAIL",
    "name": "LLMLingua prompt compression (Microsoft, EMNLP 2023)",
    "class": "prompt-compression / lossy-preprocessing",
    "reasoning": "FAIL: the litmus question is whether the model reads a different token sequence than the user's original prompt, and LLMLingua answers yes by design -- it removes and rewrites prompt tokens via a small model before the target LLM ever sees them. The paper's own abstract, verified verbatim at the cited source, admits this is lossy, not lossless: \"...allows for up to 20x compression with little performance loss.\" \"Little\" is explicitly non-zero, which rules out PASS_ABSOLUTE and PASS_REPLAY (no byte-identical wire payload survives), PASS_METADATA (the changed content is the literal prompt body the model conditions its reasoning on, not an inert field it never reads), and PASS_SCHEDULING (nothing about timing changed). It also fails to even reach CONTRACTUAL_ONLY, because that cap exists for a vendor asserting semantic-identity intent despite an implementation change -- here the authors make no such claim; they affirmatively state that performance loss occurs. A different, shorter, algorithmically-rewritten sequence of tokens is what the target LLM actually answers from -- textbook FAIL.",
    "verifiedAgainst": "https://arxiv.org/abs/2310.05736 (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit could observe a drop in billed input tokens to the target LLM API (the compressed prompt is genuinely shorter on the wire), but that signal alone is misleading in isolation -- it conflates real cost reduction with silent context degradation. A trustworthy audit would need a paired downstream-quality metric (task accuracy / eval score delta) run alongside the token-count delta to distinguish \"cheaper because compressed intelligently\" from \"cheaper because the model got less information.\" Token-identity on the wire cannot be observed as preserved, because it provably isn't.",
    "savings": "Up to 20x prompt-token reduction is the paper's own self-reported headline figure -- EMNLP 2023 peer review validates the compression method's design, not independent reproduction of this exact ratio across arbitrary production prompts. The same sentence concedes \"little performance loss,\" an explicitly non-zero, unquantified accuracy cost, so net realizable savings after accounting for quality degradation is UNQUANTIFIED.",
    "providers": [
      "Microsoft Research / microsoft/LLMLingua (open-source; verified 6,611 GitHub stars via `gh repo view` on 2026-08-30, well above the register's 500-star floor) -- not a hosted-API vendor feature; it is an application-layer preprocessing step a caller must run themselves before hitting any target LLM's API, so no provider ships this as a customer-facing toggle."
    ],
    "estimatedReach": "UNQUANTIFIED -- 6.6k GitHub stars signals real developer interest in the open-source repo, but there is no data here on production deployment volume or what share of LLM-calling pipelines insert this (or an equivalent compressor) ahead of a billed API call.",
    "implementationNote": "If a caller wants shorter/cheaper prompts and is willing to accept an unquantified accuracy tax, LLMLingua runs client-side, upstream of the target model call. It does not belong in a \"the model saw the same input\" register bucket -- if tracked at all, it should be filed as a distinct lossy-optimization pattern, never as a spend-neutral technique.",
    "trap": "Not one of the six standard adjudication traps (no bot-closed PR, no Spring-flow merge, no icon-dropped HTML table, no pass-through-looking dependency, no repo-star inflation, no missing date). It is its own trap-shape worth naming for the register: \"compression/summarization marketed as an optimization\" -- any technique that computes a NEW prompt via an intermediate model before the target model sees it is definitionally not pass-through, no matter how favorable the compression-ratio headline is.",
    "whoActsOnIt": "Any engineering team building its own LLM call pipeline that chooses to insert LLMLingua (or a similar compressor) between their app and the target LLM API. The adoption decision and the accuracy risk sit entirely with that caller -- it is not something a hosted API vendor turns on by default."
  },
  {
    "drop": false,
    "name": "CacheGen KV-cache compression and streaming (SIGCOMM 2024)",
    "class": "infrastructure-technique (self-hosted serving, not a billing lever)",
    "strictVerdict": "FAIL",
    "reasoning": "Fetched arxiv.org/abs/2310.07240 raw and confirmed the quote verbatim: \"CacheGen reduces the KV cache size by 3.5-4.3x and the total delay in fetching and processing contexts by 3.2-3.7x with negligible impact on the LLM response quality.\" \"Negligible\" is explicitly not \"zero\" or \"identical\" -- the paper is admitting, in its own abstract, that the lossy-decoded KV tensors fed into attention differ from a full-precision recompute of the same context. That means the model's actual computation over the cached tokens changes -- the bar question (\"does a different amount of thinking happen / does the model read something different\") resolves to YES on the source's own words, not on any inference I had to make. This is not a documentation-assertion-of-bit-identity case that would cap at CONTRACTUAL_ONLY; it is the inverse -- an admitted non-identity, which is a direct FAIL. Additionally, CacheGen operates inside a self-hosted inference-serving stack below any request/response boundary, so it has no visibility to a customer auditing hosted-API spend, and its reported wins (3.5-4.3x, 3.2-3.7x) are self-benchmarked infra-latency/storage-size ratios, not an independently reproduced or billed $/token figure.",
    "verifiedAgainst": "https://arxiv.org/abs/2310.07240 (read 2026-08-30)",
    "provenance": "peer-reviewed (SIGCOMM 2024 paper; quote confirmed against the arxiv abstract directly, not taken on the miner's word)",
    "telemetrySignal": "None available. CacheGen's compression/decompression happens inside a self-hosted serving pipeline's KV-cache transport layer, entirely below the request/response boundary any API customer or spend-audit tool can observe. No hosted inference provider exposes a per-token KV-compression-rate or cache-transfer-bytes metric on a bill; the bandwidth/storage bytes it saves never surface as a line item, so there is nothing a $/token audit could point to as evidence this ran.",
    "savings": "3.5-4.3x KV-cache size reduction and 3.2-3.7x reduction in context-fetch-and-processing delay, per the paper's own self-reported benchmarks (not independently reproduced this session). This is an infra latency/storage-footprint metric internal to a self-hosted serving stack -- not a $/token or billed-spend figure, and not applicable to anyone consuming a hosted API.",
    "providers": [
      "Research only (University of Chicago-affiliated authors, SIGCOMM 2024); reference implementation github.com/UChi-JCL/CacheGen has 168 GitHub stars as of 2026-08-30 -- well under the register's 500-star reach bar and confirming no evidence of commercial hosted-API adoption"
    ],
    "estimatedReach": "None confirmed. Reference repo sits at 168 stars (checked via gh repo view at adjudication time, 2026-08-30) -- far below the 500-star threshold this register uses as a reach signal for repo-based techniques, though this candidate's cited source is the paper, not the repo. No hosted inference provider is known to have shipped this. Relevant only to teams running their own LLM-serving infrastructure who control the KV-cache transport layer directly.",
    "whoActsOnIt": "Infra/ML-systems engineers building or operating a self-hosted LLM serving stack with a disaggregated KV-cache store (e.g., prefix caching across a fleet). Not actionable by any customer of a hosted API -- there is no lever to pull, flag to set, or vendor to request this from.",
    "trap": "Checked for the \"documentation sentence asserting identity caps at CONTRACTUAL_ONLY\" trap and it does not apply here -- the source does the opposite of asserting identity; it explicitly concedes \"negligible impact,\" i.e., non-identity, so this is a direct primary-source-confirmed FAIL rather than a capped claim.",
    "implementationNote": "Not implementable as a customer-facing switch on any hosted API; would require deploying the CacheGen research codebase as part of a self-run inference-serving stack (e.g., alongside vLLM-style prefix caching) and accepting lossy KV reconstruction in exchange for lower network/storage cost."
  },
  {
    "drop": false,
    "name": "CacheBlend selective-recompute KV fusion (EuroSys 2025)",
    "class": "approximate-kv-cache-fusion",
    "strictVerdict": "FAIL",
    "reasoning": "Quote verified verbatim in the arxiv abstract (fetched live): \"reuses the precomputed KV caches, regardless prefix or not, and selectively recomputes the KV values of a small subset of tokens to partially update each reused KV cache.\" That sentence is itself the failure: a genuine full prefill computes every token's KV values against the true full-context attention; CacheBlend deliberately recomputes only a top-k \"small subset\" (selected in layer 2 by largest V-value discrepancy) and leaves the rest of the sequence's KV values as they were computed in isolation per-chunk, i.e. NOT reflecting real cross-chunk attention. That is a different amount of \"thinking\" happening for the majority of tokens by explicit construction, not an implementation detail invisible to the model's computation. The paper's own framing concedes this: it reports \"negligible quality drop\" (perplexity/F1/ROUGE deltas versus full recompute), not byte-identical or logit-identical output -- if the outputs were provably identical there would be no quality metric to report at all. This is the same shape of failure the register already logged for Prompt Cache (id 52): a paper-level \"without compromising generation quality\" claim that full-text/mechanism review shows is an approximation claim, not an equivalence claim. Per the bar (\"does a different amount of thinking happen? Any yes = FAIL\"), this is a clean FAIL, and it isn't eligible for CONTRACTUAL_ONLY either since no vendor is contractually asserting output identity -- this is an academic serving-optimization paper reporting its own measured quality degradation.",
    "verifiedAgainst": "https://arxiv.org/abs/2405.16444 (read 2026-08-30)",
    "provenance": "peer-reviewed",
    "telemetrySignal": "Cannot be observed from billed API telemetry at all -- this operates inside a self-hosted inference engine's attention kernel, below anything a token-count or cost line-item would surface. The only audit that could catch it is an output-equivalence check: run the same RAG prompt through (a) full prefill and (b) CacheBlend, hash both completions, and see them diverge on some fraction of runs -- which is exactly what the paper's own perplexity/F1 quality-drop numbers already show happens, just averaged into a metric instead of reported as a raw diff rate.",
    "savings": "2.2-3.3x TTFT reduction and 2.8-5x throughput increase vs. full KV recompute, per the paper's own benchmarks across three open-source LLMs and four RAG datasets. This is a serving-infrastructure latency/throughput metric, not a customer-billed dollar figure -- no confirmed hosted commercial API bills less because of it, so from a spend-audit standpoint the customer-facing savings are UNQUANTIFIED.",
    "providers": [
      "Research origin: University of Chicago (CacheBlend, EuroSys 2025 Best Paper). Code released via the LMCache open-source project (github.com/LMCache/LMCache, 11,575 stars as of 2026-08-30 per gh repo view) for self-hosted vLLM-style stacks. No confirmed commercial hosted-API provider (OpenAI/Anthropic/Google/etc.) has adopted the selective-recompute mechanism specifically."
    ],
    "estimatedReach": "Self-hosted / OSS inference operators running LMCache on their own RAG pipelines; not reachable or observable by ordinary API-billed customers since no hosted commercial endpoint exposes it.",
    "whoActsOnIt": "Inference-platform engineers who operate their own vLLM/LMCache serving stack for RAG workloads -- not applicable to someone auditing spend against a hosted API, since the technique lives entirely inside self-managed serving infrastructure.",
    "trap": "Same trap as Prompt Cache (id 52): an abstract-level \"without compromising generation quality\" claim that, read past the abstract, is explicitly an approximation (\"small subset of tokens\" recomputed, rest left stale) with a measured non-zero quality delta -- not a proof of output identity. Don't let \"best paper award\" or a 2.2-3.3x headline number substitute for checking whether the token sequence / attention computation is actually unchanged.",
    "implementationNote": "Mechanism per the paper: fully recompute layer 1, partially recompute layer 2 to get an approximate QKV, compare the resulting V-values against the precomputed (isolated per-chunk) V-values, pick the top-k tokens with largest discrepancy, and only recompute KV for that top-k subset at deeper layers -- every other token keeps the KV values it had when its chunk was encoded alone, which never saw the other retrieved chunks in its attention context."
  },
  {
    "drop": false,
    "strictVerdict": "FAIL",
    "name": "RECOMP: compressing retrieved documents before in-context integration (ICLR 2024)",
    "class": "context-compression / RAG-input-modification (not an infra/pricing optimization)",
    "reasoning": "Fetched the arXiv abstract directly (curl -sL and WebFetch, both independently) and confirmed the verbatim quote exists exactly as cited: \"We achieve a compression rate of as low as 6% with minimal loss in performance for both tasks, significantly outperforming the off-the-shelf summarization models.\" The paper's own abstract states the mechanism plainly: retrieved documents are run through a trained extractive or abstractive compressor, and the resulting SYNTHESIZED SUMMARY -- not the original retrieved text -- is what gets prepended to the LM's input context. This is a deliberate, self-admitted rewrite of the token sequence the model reads: the model never sees the raw retrieved passages at all when the compressor triggers, and can even receive an empty string under \"selective augmentation\" when the compressor judges the documents irrelevant. Applying the bar's single test -- \"does the model read a different sequence of tokens\" -- the answer is an unambiguous yes, by design and by the authors' own framing (it is literally the paper's contribution, not an incidental side effect). This is not a caching, batching, scheduling, or metadata technique; it is a content-generation step that sits upstream of the LLM call and changes what gets asked. It also is not a hosted provider's documentation claim of identity, so it does not even qualify for the CONTRACTUAL_ONLY ceiling -- there is no assertion of equivalence to cap; the paper openly reports a \"minimal loss in performance,\" meaning outputs are not identical to the uncompressed baseline. Straightforward FAIL, not a close call.",
    "verifiedAgainst": "https://arxiv.org/abs/2310.04408 (read 2026-08-30)",
    "provenance": "peer-reviewed (ICLR 2024 conference paper; claims are the authors' self-reported benchmark results, not independently reproduced this session)",
    "providers": [
      "Research paper (Xu, Shi, Choi -- UMass Amherst / University of Washington / Meta AI-affiliated); not a hosted provider feature, a RAG-pipeline preprocessing technique a caller would build/train and run themselves"
    ],
    "savings": "Authors report compression to as low as 6% of original retrieved-document length \"with minimal loss in performance\" on language-modeling and open-domain-QA benchmarks -- the performance delta itself is not numerically quantified beyond that phrase in the abstract. Self-reported by the paper, not independently reproduced. Irrelevant to sizing since the technique fails the bar (it changes model input, not just cost).",
    "telemetrySignal": "A spend audit could observe a drop in prompt/input token counts per request when RECOMP-compressed summaries replace raw retrieved passages (shorter context -> fewer billed input tokens) -- but that signal would be confounded with a content change, since the audit cannot distinguish \"same request, cheaper transport\" from \"different, shorter question being asked.\" The token-count reduction is real but it is evidence of the disqualifying change, not evidence of a pass-eligible optimization.",
    "trap": "None of the six standard traps apply here (no PR/merge history, no repo/star count, no pricing table, no pinned-dependency wrapper) -- this one is disqualified on the front page of the abstract itself. The risk this candidate tests is a different failure mode: conflating \"reduces token cost\" with \"doesn't change the tokens,\" which the register's bar exists specifically to catch. It is a legitimate, well-cited ML technique for shrinking RAG context, just not a member of this register's category (wire-identical infra/pricing optimizations).",
    "whoActsOnIt": "Nobody, for register purposes -- this is out of scope for a cost-technique register that requires the wire request to be unchanged. It belongs in a \"prompt/context engineering\" or \"RAG architecture\" catalog instead, where changing what the model reads is the entire point.",
    "estimatedReach": "N/A -- disqualified, not tracked for adoption/reach.",
    "implementationNote": "N/A for this register. If it were being cataloged elsewhere: requires training/fine-tuning a dedicated compressor model per task and adds a summarization inference step before every LLM call, which is itself extra compute cost to weigh against the input-token savings -- the paper does not net these two costs against each other."
  },
  {
    "name": "StreamingLLM attention sinks (Xiao et al., ICLR 2024)",
    "class": "kv-cache-eviction / context-window-truncation",
    "drop": false,
    "strictVerdict": "FAIL",
    "reasoning": "The paper's own abstract states: \"Window attention, where only the most recent KVs are cached, is a natural approach -- but we show that it fails when the text length surpasses the cache size,\" and that attention sinks merely \"largely recover\" (not preserve) that lossy scheme's performance. This matches the candidate's own admission that \"the model's forward pass no longer attends over the full original token sequence -- it attends over a truncated substitute.\" Once eviction triggers, tokens from the discarded middle of the conversation are permanently gone from the KV cache; the model literally reads a different, truncated sequence than the original conversation contained. That is a direct YES to \"does the model read a different sequence of tokens,\" which is an automatic FAIL under the bar regardless of the reported 22.2x self-hosted throughput win, which is a serving-efficiency metric describing how much faster a degraded computation runs, not evidence that the computation is unchanged.",
    "verifiedAgainst": "https://arxiv.org/abs/2309.17453 (read 2026-08-30)",
    "provenance": "peer-reviewed",
    "telemetrySignal": "On self-hosted serving stacks that implement SinkCache/StreamingLLM (e.g. HF `transformers`), a spend/quality audit could observe cache-eviction events once a session exceeds the configured window, and could detect the effect indirectly via degraded recall of mid-conversation facts in long-running sessions versus a full-context baseline -- i.e., the \"savings\" show up as an accuracy/recall regression on stale context, not as a line item on a bill. No major hosted-API vendor (OpenAI, Anthropic, Google) exposes this as a customer-visible billed feature, so on a hosted-API spend audit there is nothing to observe at all -- the mechanism, if used, is invisible infrastructure on the vendor's side.",
    "savings": "Up to 22.2x throughput speedup vs. a sliding-window-recomputation baseline, measured in the paper's own self-hosted streaming-inference benchmark (Llama-2/MPT/Falcon/Pythia scaled to millions of tokens). This is a serving-throughput metric on self-hosted infrastructure, not a $/token billing figure, and is UNQUANTIFIED as a hosted-API cost lever since no confirmed major vendor exposes it as a customer-facing or opt-out mechanism.",
    "providers": [
      "Research origin: MIT Han Lab / Meta AI / CMU-affiliated authors (Xiao, Tian, Chen, Han, Lewis)",
      "Open-source uptake confirmed: HuggingFace `transformers` shipped a SinkCache/StreamingLLM cache implementation (integrated ~Dec 2023, per github.com/huggingface/transformers PR #26681 and related issues, confirmed via web search 2026-08-30)",
      "No confirmed major hosted-API vendor (OpenAI, Anthropic, Google, etc.) exposes attention-sink KV eviction as a customer-visible or opt-out billed mechanism"
    ],
    "estimatedReach": "Narrow. Confined to self-hosted/open-weight long-context serving stacks (e.g. teams running Llama/MPT/Falcon/Pythia via `transformers` or similar) that choose to enable bounded-memory streaming inference. Zero reach into hosted-API billing since no major vendor exposes it to customers.",
    "whoActsOnIt": "Self-hosted LLM inference/serving engineers optimizing memory and throughput for long-running multi-turn sessions on their own GPU fleets -- not applicable to anyone paying a per-token API bill from a hosted vendor.",
    "trap": "Reads like a caching optimization (the word \"cache\" is right there in \"KV cache\") but it is a lossy approximation technique, not a cache in the register's sense of \"store and replay an identical result.\" The paper is explicit that plain window attention \"fails\" once the budget is exceeded and that sinks only \"largely recover\" -- language of approximation, not identity. Anyone tempted to file this next to prompt-cache TTL or semantic caching should note those preserve exact byte-for-byte reuse of untouched content; this technique instead permanently discards conversation history the model would otherwise have read. Also: the 22.2x number is a serving-throughput multiplier against a recomputation baseline, easy to mistake for a $/token savings figure -- it isn't one, and it isn't billed anywhere.",
    "implementationNote": "Not applicable for a hosted-API cost register entry -- there is nothing for an API customer to configure or opt into; this lives entirely in self-hosted serving infrastructure (e.g. `transformers` SinkCache, `attention_sinks` package) and changes the model's effective input the moment eviction triggers."
  },
  {
    "drop": false,
    "name": "Context distillation via fine-tuning (Snell et al., \"Learning by Distilling Context\", 2022)",
    "class": "training-time-fine-tuning / weight-distillation (not a serving-side technique)",
    "strictVerdict": "FAIL",
    "reasoning": "Quote verified verbatim against the arXiv abstract (curl-fetched raw HTML, matched word-for-word including the nested-quote structure) at https://arxiv.org/abs/2209.15189, read 2026-08-30. The method fine-tunes \"the same model to predict its own '[final answer]' conditioned on the '[task-input]', without seeing the '[instructions]' or using the '[scratch-pad]'\" -- meaning it trains new weights and then serves that new checkpoint on a shorter token sequence than the original context-laden prompt. That is a double failure of the bar: (a) \"a different model answers\" -- the deployed checkpoint has different parameters than the one that originally produced the reference output, confirmed by the paper's own framing that this lets researchers \"iteratively update the model parameters with new instructions and overwrite old ones\"; (b) \"the model reads a different sequence of tokens\" -- instructions and scratchpad are deliberately stripped from the input at serving time. Neither condition is a documentation-identity claim to be capped at CONTRACTUAL_ONLY; it fails on mechanism, not on an unverified equivalence assertion, so it is a flat FAIL.",
    "verifiedAgainst": "https://arxiv.org/abs/2209.15189 (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "Not observable by a downstream spend audit. This is a pre-deployment, training-time technique run by whoever owns the model weights; an API customer cannot invoke, toggle, or detect \"context distillation\" occurring server-side, and no request header, response field, or cache-hit signal on the wire would distinguish a distilled checkpoint from the original base model.",
    "savings": "UNQUANTIFIED for serving cost. The only number in the paper is a training-time accuracy delta (fine-tuning via context distillation outperforms directly learning with gradient descent by 9% on the SPIDER Text-to-SQL dataset) -- a self-reported quality metric, not a $/token or latency serving-cost measurement. Any downstream serving savings from a shorter deployed prompt are asserted as motivation, never measured, in this paper.",
    "providers": [
      "Academic research: UC Berkeley NLP group. Authors per the arXiv listing are Charlie Snell, Dan Klein, and Ruiqi Zhong -- Dan Klein is a UC Berkeley professor; no Anthropic affiliation is evident anywhere in the abstract or author list. This corrects the miner's unverified claim that the paper is 'Anthropic-adjacent alignment research' -- that appears to be speculation not supported by the source.",
      "A training-pipeline method a model-owning lab would run before deployment, not an API a downstream customer can call or verify"
    ],
    "estimatedReach": "None as a register technique -- not invocable by API customers of a hosted LLM at all (requires weight access + a training pipeline); 2022 preprint, no evidence cited of production adoption by any lab.",
    "implementationNote": "Requires direct access to the base model's weights and a fine-tuning pipeline. This alone would put it outside the register's implicit scope of 'techniques a spend-conscious API customer can apply,' independent of the FAIL verdict on the identity-of-response bar.",
    "whoActsOnIt": "N/A -- verdict is FAIL. Were this ever reframed as a PASS-eligible technique, only the model-owning lab (not an API consumer) could act on it, since it requires retraining and redeploying weights.",
    "trap": "None of the six standard traps apply mechanically here (no repo/PR/pricing-table/pass-through-wrapper involved). The live risk in this submission was an unverified provenance claim in the miner's own 'providers' field (see providers note) -- a reminder that miner-asserted author affiliation needs the same re-verification as everything else, not just the pull-quote.",
    "settlingExperiment": ""
  },
  {
    "drop": false,
    "name": "Lookahead Decoding: exact parallel decoding without a draft model (Fu et al., ICML 2024)",
    "class": "self-hosted inference-serving decoding algorithm (compute reorganization on the target model itself, no draft model, no metered-billing surface)",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "provenance": "primary-doc",
    "verifiedAgainst": "https://arxiv.org/abs/2402.02057 (read 2026-08-30)",
    "reasoning": "Quote confirmed verbatim on the live arXiv abstract page (read 2026-08-30): \"we introduce Lookahead decoding, an exact, parallel decoding algorithm that accelerates LLM decoding without needing auxiliary models or data stores.\" That \"exact\" is the authors' own primary-source claim, not an independently reproduced result -- it caps evidentiary weight the same way a vendor doc sentence would. The paper's own vLLM integration thread (github.com/vllm-project/vllm/issues/1742) has a project author stating exactness holds only for greedy decoding (\"Yes, it produces the exact same results that would be created by greedy decoding\") -- sampling, which is most real production traffic, was bolted on later via a separate PR whose equivalence guarantee is not established in that thread. Mechanically the algorithm verifies speculated n-grams against a Jacobi-window batched forward pass rather than the single-token sequential pass standard decoding uses; GPU matmul/attention reduction order is not guaranteed invariant across batch shapes (this is the same batch-invariance failure mode that made ChunkAttention/Hydragen/RadixAttention downgrade to INSUFFICIENT_EVIDENCE in this register), so \"exact\" is unverified at the bit level under real serving conditions. No amount-of-thinking or different-model concern applies -- only the target model runs -- but token-sequence identity under sampling, at scale, on real kernels, is asserted, not demonstrated.",
    "telemetrySignal": "A spend audit cannot observe this at all: it changes wall-clock latency and GPU-step count on infrastructure the operator already owns/pays for by the GPU-hour, not a per-token billing line. There is no $/token metric, no invoice field, and no hosted-API response header that would let anyone doing a billing audit detect whether Lookahead Decoding is in use -- it is invisible to spend telemetry by construction.",
    "savings": "Not a $/token figure. Paper reports up to 1.8x latency speedup on MT-bench and 4x with strong multi-GPU scaling on code-completion (self-reported, single-paper benchmark, read 2026-08-30). Real, but a self-hosted GPU-hour saving, not a billing/margin figure -- UNQUANTIFIED for the register's cost-technique purposes.",
    "providers": [
      "Hao AI Lab, UC San Diego -- open-source reference implementation (github.com/hao-ai-lab/LookaheadDecoding, 1342 stars, gh repo view confirmed 2026-08-30, meets the >=500-star bar)",
      "NVIDIA TensorRT-LLM -- CORRECTION to the miner's claim of zero hosted-API adoption: NVIDIA documents a production Lookahead Decoding implementation in TensorRT-LLM, deployable via NVIDIA NIM microservices (developer.nvidia.com blog on Qwen2.5-Coder + TensorRT-LLM Lookahead Decoding, read 2026-08-30) -- this is a real vendor SDK integration, not merely a research repo",
      "vLLM -- attempted integration (github.com/vllm-project/vllm/issues/1742) was NEVER merged: closed by a stale-bot cycle and then explicitly closed by a paper co-author (zhisbug) as 'no longer relevant,' confirming trap #1 (a closed thread with quoted-matching text is not a shipped fix)",
      "No major $/token commercial API provider (OpenAI, Anthropic, Together, Fireworks, etc.) advertises billing customers on this technique"
    ],
    "settlingExperiment": "Two checks would close this: (1) On identical hardware/seed/model, run several thousand prompts through standard sequential greedy decoding and through Lookahead Decoding at multiple Jacobi-window/batch sizes, hash-compare the full output token-ID sequences for byte-identical match -- this tests whether the batched-verification forward pass actually produces bit-identical logits/argmax to the sequential pass across realistic batch-shape variation, closing the floating-point reduction-order gap the paper's math proof doesn't cover on real kernels. (2) Confirm whether NVIDIA NIM's Lookahead-Decoding-enabled endpoints are used by any $/token-billed hosted deployment, and if so pull matched-prompt request/response pairs with and without it enabled to check for observable output divergence.",
    "trap": "Trap #1 (closed-thread-is-not-a-fix) applied to vLLM issue #1742: the presence of quoted \"exact same results\" testimony in that thread does not mean the feature shipped -- it was closed unmerged. Trap #5 (repo star re-verification) applied and passed: gh repo view confirms 1342 stars at adjudication time, not just trusted from the miner.",
    "implementationNote": "Mechanism: Lookahead Decoding runs two parallel branches per step against the SAME target model -- a Jacobi-iteration lookahead branch that generates candidate future n-grams from a fixed 2D window, and a verification branch that checks candidate n-grams against the model's own next-token distribution, accepting a token only if it matches what standard decoding would have produced. No draft model, no KV-cache compression, no token dropping -- but the verification happens inside a batched multi-token forward pass rather than vanilla single-token decode, which is the numerical-reproducibility crack the register has consistently flagged for this class of technique (ChunkAttention, Hydragen, RadixAttention precedent). Concur with the miner's proposed verdict of INSUFFICIENT_EVIDENCE, with the provider list corrected to reflect real (if non-billed) TensorRT-LLM/NIM productization the miner missed.",
    "estimatedReach": "Self-hosted/infra population only: operators running vLLM- or TensorRT-LLM-class serving stacks on owned or NIM-deployed GPUs for latency-sensitive workloads (coding assistants, agentic tool loops). Zero reach into $/token hosted-API consumers, since no billed API surfaces this as a customer-facing lever.",
    "whoActsOnIt": "ML infra / inference-platform engineers choosing a decoding algorithm for a self-hosted or NIM-deployed serving stack -- not available as a lever to someone simply calling a commercial LLM API and auditing their bill."
  },
  {
    "drop": false,
    "name": "Modal per-second, per-CPU-cycle billing with zero idle charge",
    "class": "infrastructure-billing-claim",
    "providers": [
      "Modal"
    ],
    "strictVerdict": "FAIL",
    "reasoning": "Quote confirmed verbatim via raw-HTML fetch of modal.com/pricing: \"With Modal, you always pay for what you use and nothing more. You never pay for idle resources — just actual compute time, by the CPU cycle.\" But Modal's own cold-start documentation directly contradicts that framing: \"Increasing the scaledown_window reduces the chance that subsequent requests will require a cold start, although you will be billed for any resources used while the container is idle (e.g., GPU reservation or residual memory occupancy).\" The exact configuration LLM-serving deployments need to avoid cold-start latency (min_containers/scaledown_window keeping a warm pool) is precisely the configuration where Modal bills for idle GPU/memory time — so \"zero idle charge\" is false for the deployment pattern the candidate's own \"orthogonal to token content\" framing implies. Independently, the mechanism is not LLM-specific: it is generic serverless billing, identical for any non-LLM workload run on Modal, orthogonal to tokens/model/thinking only because it never touches them at all — it never had standing as a distinct LLM cost technique versus ordinary vendor/config selection.",
    "verifiedAgainst": "https://modal.com/pricing (read 2026-08-30); https://modal.com/docs/guide/cold-start (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A Modal `billing` CLI/API report or Stripe invoice broken down by App/container would show non-zero charges accruing during intervals with zero inbound requests whenever scaledown_window &gt; default or min_containers &gt; 0 keeps a container warm — directly falsifying the \"never pay for idle\" claim for that deployment. For a pure scale-to-zero App with no warm-pool settings, the same report would show zero cost during idle gaps, matching the claim.",
    "savings": "Real only for pure scale-to-zero deployments with no warm pool; None/overstated for latency-sensitive configurations using scaledown_window or min_containers, where idle GPU reservation and residual memory occupancy are explicitly billed per Modal's own cold-start docs. Deployment-configuration-dependent, not the unconditional \"zero idle charge\" the pricing page markets.",
    "trap": "Provider's own glossy pricing page overstates a claim its own engineering docs (cold-start guide) directly qualify away — same word \"idle\" used to mean opposite things two pages apart. Classic case of citing marketing copy instead of the technical doc it silently contradicts.",
    "whoActsOnIt": "Teams evaluating Modal (or similar serverless GPU hosts) for self-hosted LLM inference and weighing it against reserved/dedicated GPU instances on the promise of \"zero idle cost\" — should read the scaledown_window/min_containers caveat before assuming warm-pool latency mitigation is free.",
    "implementationNote": "If this ever gets cited as a cost lever elsewhere, the caveat must travel with it: scaledown_window/min_containers (needed to kill cold-start latency for interactive endpoints) reintroduces idle billing. \"Zero idle charge\" only holds for pure scale-to-zero, bursty/batch workloads with no warm-pool configuration.",
    "estimatedReach": "N/A — claim fails verification against the provider's own technical docs; not a reliable cost technique to surface to adopters as-is."
  },
  {
    "drop": false,
    "name": "Baseten dedicated deployments: no charge for idle time between requests",
    "class": "infra-billing-granularity",
    "reasoning": "The verbatim FAQ quote is real (confirmed on baseten.co/pricing/, \"Do I pay for idle time on Baseten?\" accordion: \"No, you do not pay for idle time... you only pay for the time your model is... actively deploying, scaling up or down, or making predictions\"), but it is marketing copy, not the billing mechanism. Baseten's own autoscaling docs (docs.baseten.co/deploy/autoscaling) state plainly: \"Baseten bills each running replica by the minute\" and \"For production deployments, set min_replica to at least 2\" -- meaning any replica that is up, including one sitting idle between requests, is billed per-minute regardless of whether it is actively predicting at that instant. Even under the default min_replica=0 scale-to-zero setting, the default 900s \"scale-down delay\" keeps the last replica running (and billed) for up to 15 minutes of genuine idle time after the last request before it shuts down (\"Wait time before removing idle replicas\"). So the candidate's specific claim -- \"not while sitting idle waiting for traffic\" -- is false for the vast majority of real dedicated deployments (anything with min_replica>=1, which Baseten itself recommends for production) and only narrowly true for a fully scaled-to-zero replica once the shutdown delay has elapsed. This is a provider marketing sentence contradicted by the provider's own technical documentation on the same domain, not a verified cost technique.",
    "verifiedAgainst": "https://www.baseten.co/pricing/ and https://docs.baseten.co/deploy/autoscaling (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit could observe this directly: pull Baseten's usage/billing export (Organization > Billing and usage) and compare GPU-instance-minutes charged against inbound request timestamps for a given deployment. If instance-minutes accrue during gaps with zero inbound requests -- which is expected whenever min_replica>=1 (Baseten's own recommended production setting), or during the up-to-900s scale-down-delay window after the last request even under min_replica=0 -- the \"no idle charge\" claim is falsified for that deployment. Only a deployment held at min_replica=0 with traffic gaps exceeding the scale-down delay would show $0 during those windows.",
    "savings": "None as literally claimed -- production dedicated deployments (min_replica>=2, per Baseten's own recommendation) are billed continuously per running-replica-minute whether idle or predicting. A narrower, real saving exists only for min_replica=0 deployments during genuine zero-replica windows after the default 900s scale-down delay elapses -- UNQUANTIFIED, and inapplicable to Baseten's own recommended production configuration.",
    "strictVerdict": "FAIL",
    "providers": [
      "Baseten"
    ],
    "estimatedReach": "Narrow even where it holds: only Baseten dedicated-deployment customers running min_replica=0 with idle gaps longer than the ~900s default scale-down delay benefit. Baseten's own docs recommend min_replica>=2 for any production workload (to avoid cold starts and get redundancy), which gets zero benefit and is billed continuously regardless of traffic.",
    "trap": "Marketing FAQ (\"you do not pay for idle time\") on the pricing page is directly contradicted by Baseten's own technical autoscaling docs, which state billing is per running-replica-minute (\"Baseten bills each running replica by the minute\") and explicitly recommend min_replica>=2 -- continuous billing -- for production. A doc-vs-doc contradiction within the same provider's own site that the miner did not cross-check before citing the pricing page in isolation.",
    "whoActsOnIt": "FinOps / ML infra engineers evaluating Baseten dedicated deployments against serverless or per-token Model APIs on the premise that idle GPU capacity between requests is free.",
    "implementationNote": "If re-scoped narrowly to \"Baseten dedicated deployments support scale-to-zero autoscaling, so a fully idle deployment with min_replica=0 incurs zero GPU charges once the scale-down delay elapses,\" that narrower claim is directly supported by docs.baseten.co/deploy/autoscaling (\"A deployment at zero replicas incurs no GPU charges\") and would likely pass -- but that is standard scale-to-zero autoscaling billing, not the \"no charge for idle time between requests\" framing submitted, and materially different from what most dedicated-deployment customers (min_replica>=1) experience."
  },
  {
    "drop": false,
    "name": "Cloudflare AI Gateway custom cache key (cf-aig-cache-key)",
    "class": "caching-manual-key-override",
    "strictVerdict": "FAIL",
    "providers": [
      "Cloudflare AI Gateway"
    ],
    "provenance": "primary-doc",
    "verifiedAgainst": "https://developers.cloudflare.com/ai-gateway/configuration/caching/ (read 2026-08-30)",
    "reasoning": "FAIL. Cloudflare's own docs confirm the custom key is developer-chosen, not content-derived: \"The cf-aig-cache-key header lets you override the default cache key and opts the request into caching. Choose a custom key that groups only requests with equivalent responses. Requests with the same custom key share a cached response.\" On a hit the provider/model is never invoked (\"Subsequent requests with the same custom key value will return the cached response\") and Cloudflare performs no automated check that the new request's tokens match whatever produced the stored response — equivalence is asserted by the developer's label alone, never verified against the wire content. So on a cache hit a different sequence of tokens (whatever the second request actually contains) never reaches the model at all, which directly violates the bar. Contrast with the default SHA-256 mechanism this overrides (\"AI Gateway constructs the cache key by concatenating Provider, Endpoint, Model, auth header, and Full request body\" and hashing — exact match only): that at least ties the key to content. This feature deliberately severs that tie. Because the docs never even assert content identity between grouped requests, this doesn't rise to a CONTRACTUAL_ONLY claim to discount — there is no equivalence claim to evaluate in the first place, so it fails outright and more clearly than techniques whose docs assert sameness.",
    "telemetrySignal": "The gateway emits a per-response `cf-aig-cache-status: HIT|MISS` header, so a spend audit can count how many requests were served from cache at $0 upstream cost. But that header cannot show what was skipped: Cloudflare does not log or diff request bodies against the custom key, so nothing in the gateway's own telemetry reveals whether two requests sharing a `cf-aig-cache-key` actually had equivalent content. Detecting silent content-drift-under-shared-key requires the developer to independently capture and compare full request bodies per key value outside the gateway — the platform gives no signal for it.",
    "savings": "UNQUANTIFIED. Cloudflare's docs only assert a qualitative benefit — \"Cost Savings: Minimize the number of paid requests made to your AI provider, especially for frequently accessed or non-dynamic content\" — with no dollar figure, hit-rate, or traffic-share estimate. Actual savings scale with how aggressively (and how correctly, or incorrectly) developers group dissimilar prompts under one key, which cannot be sized from the documentation.",
    "whoActsOnIt": "The application developer who adds the `cf-aig-cache-key` header to their own outbound requests. The gateway takes no independent judgment — it purely trusts and honors whatever label the caller supplies; there is no reviewer, no similarity model, no hash on Cloudflare's side.",
    "implementationNote": "Send `cf-aig-cache-key: <any-string>` on a request to Cloudflare AI Gateway's provider-compatible endpoint. The first request bearing a given key is forwarded to the model and cached; every subsequent request bearing that identical string — regardless of its own messages/body — is served the stored response verbatim until the TTL expires (default 5 minutes absent `cf-aig-cache-ttl` or a gateway-level default).",
    "estimatedReach": "Available to every Cloudflare AI Gateway account, including free tier, the moment a caller adds the header — no separate opt-in flag. Cloudflare does not publish AI Gateway customer or request-volume figures, so absolute reach can't be sized; among existing AI Gateway users, uptake of this specific header (versus the default exact-match cache) is unmeasured and unpublished.",
    "trap": "Fetched the page as raw HTML via curl and stripped tags myself (not a markdown auto-conversion) per trap #3, to make sure the verbatim quote wasn't an artifact of lossy conversion — it matched exactly. No repo or PR is involved here so traps #1/#2/#4/#5 don't apply. Worth flagging for future adjudicators: don't lump \"arbitrary developer-labeled cache key\" in with content-hash caching or embedding-based semantic caching — it has neither a hash tie nor a similarity computation, just a human-asserted label, so it doesn't even qualify for the CONTRACTUAL_ONLY cap (there's no provider claim of identity to discount) — it's a harder, more direct FAIL than caches whose docs assert equivalence."
  },
  {
    "drop": false,
    "strictVerdict": "FAIL",
    "name": "Fastly AI Accelerator: semantic-similarity caching (embedding match, not exact byte match)",
    "class": "semantic-cache",
    "providers": [
      "Fastly"
    ],
    "reasoning": "Fetched docs.fastly.com/products/ai-accelerator raw and confirmed the miner's quote verbatim, then followed the linked cache-matching page and found the deciding detail: cache hits require only that the query embedding clear a configurable similarity threshold (default 0.75) against a stored query, matched via the x-semantic-cache-key header — Fastly's own docs state lowering the threshold \"increase[s] the likelihood of a cached response at the risk of returning a lower quality response.\" That sentence is the provider itself documenting non-identity: on a hit, the model never reads the current request's specific tokens at all — a response generated for a different, merely-similar prior prompt is replayed instead. This fails the one-question bar outright (a different token sequence is served) and doesn't even reach CONTRACTUAL_ONLY, because CONTRACTUAL_ONLY requires a vendor claim of identity to discount — here the vendor documents the opposite, an explicit quality/hit-rate tradeoff. Same technique category the register already rejected under id 222 (Kong/Apigee/TrueFoundry); Fastly is a genuinely separate vendor doing the identical thing, so this is legitimate new coverage, not a rename-to-relitigate attempt.",
    "verifiedAgainst": "https://docs.fastly.com/products/ai-accelerator (read 2026-08-30); https://www.fastly.com/documentation/guides/compute/ai-accelerator/about-the-ai-accelerator-page (read 2026-08-30, similarity-threshold/x-semantic-cache-key detail)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit can observe Fastly's reported cache-hit ratio and the resulting drop in metered LLM-origin calls (fewer requests forwarded upstream). It cannot observe, from billing/cost data alone, whether any given hit served the response the specific prompt actually warranted versus a merely-similar cached one — that requires output-correctness monitoring (evals/spot-checks against the similarity threshold setting), not spend data, and Fastly's docs don't expose a per-hit fidelity metric.",
    "savings": "Vendor-marketed only, no independently verified dollar figure. Fastly's product page claims to \"Reduce fees by making fewer direct queries to LLM services\" and \"Reduce user wait times between query and response\"; separate Dec 2024 launch materials claim \"9x faster\" response times. Actual $ savings depend entirely on the (unpublished) hit rate at whatever similarity threshold a customer runs, which trades directly against answer fidelity. UNQUANTIFIED.",
    "estimatedReach": "Narrow. Requires routing LLM API traffic through Fastly Compute specifically and opting into the AI Accelerator product — a small subset of Fastly's CDN customer base, smaller still relative to the broader universe of teams using direct API calls or API-gateway products for LLM traffic.",
    "implementationNote": "The tell is on the sub-page Fastly links from its own overview: cache hits are gated by an x-semantic-cache-key header and a numeric similarity threshold (default 0.75, configurable), not a hash of the request body. Any adjudicator re-checking this should fetch that sub-page raw (curl -sL), not just the overview — the overview's \"semantic similarity\" phrase alone reads almost like marketing copy; the threshold/quality-risk sentence is what removes any doubt.",
    "trap": "A cache \"hit\" doesn't require the incoming prompt to match anything byte-for-byte — only that its embedding lands within cosine-similarity distance of a previously cached query. Two different prompts can retrieve the same stored response, and Fastly's own docs concede this is a quality/hit-rate dial, not a correctness guarantee.",
    "whoActsOnIt": "Teams routing LLM API calls through Fastly Compute who enable AI Accelerator to cut per-call spend on repeated/similar queries — they would need to independently run evals against their chosen similarity threshold to know whether hits are silently answering a different question than the one asked, since Fastly's dashboards report hit rate, not answer fidelity."
  },
  {
    "drop": false,
    "name": "Vercel AI SDK \"Caching Middleware\" recipe: unscoped JSON.stringify(params) exact-match cache with no TTL",
    "class": "exact-match-cache (application-layer middleware, not edge/CDN — miner's \"edge-cdn\" territory tag is a mislabel)",
    "strictVerdict": "FAIL",
    "reasoning": "Fetched ai-sdk.dev/docs/advanced/caching and the mirrored ai-sdk.dev/cookbook/next/caching-middleware directly as raw HTML; both contain the verbatim quoted sentence 'The recommended approach to caching responses is using language model middleware and the simulateReadableStream function,' followed by code that sets const cacheKey = JSON.stringify(params) and then calls a bare redis.set(cacheKey, result) / redis.set(cacheKey, fullResponse) with no TTL, no expire call anywhere in that recipe, and no field gating on sampling intent — the word 'temperature' does not appear on the page at all. Any subsequent byte-identical request, such as a best-of-N or ensemble-voting call at temperature>0 that intentionally repeats identical params to get a fresh stochastic sample, is served from this cache the moment 'cached !== null', so the model is never invoked again and a different amount of thinking that should have happened does not. This is the exact unscoped-exact-match, no-TTL failure shape the register already used to FAIL Portkey (id 8) and Cloudflare's default hash (id 5), now confirmed at the source in Vercel's own official AI SDK documentation, duplicated verbatim across two separate pages — which is why it caps at documentation-asserted convenience and still fails the wire-level 'does a different amount of thinking happen' bar rather than earning any PASS_* tier.",
    "verifiedAgainst": "https://ai-sdk.dev/docs/advanced/caching and https://ai-sdk.dev/cookbook/next/caching-middleware (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "Vercel AI SDK"
    ],
    "savings": "UNQUANTIFIED — Vercel's docs describe the benefit only as avoiding \"redundant API calls\" for repeated identical requests, with no dollar figure or hit-rate estimate given; and because there is no TTL in the cited recipe, the cache never self-expires, so both any realized savings and the collision/staleness risk grow unbounded over the app's lifetime rather than settling to a steady state.",
    "telemetrySignal": "A spend audit cannot see this from the LLM provider's dashboard alone, since suppressed calls never reach the provider — it would need to diff app-side generateText/streamText invocation counts (or Redis GET/SET counts on the cache middleware) against provider-side completion counts; a gap where app-layer calls exceed provider-side completions, with zero output variance across calls that share identical params at temperature>0, is the tell. Absent that instrumentation, the missing model invocations are invisible.",
    "trap": "This reads like an ordinary, benign response cache (identical inputs really do produce a byte-identical wire request, which satisfies naive caching logic) and could be waved through on that basis. The actual failure is conflating \"the request bytes are identical\" with \"the caller wanted the same computation to happen again\" — false whenever temperature>0 sampling is being deliberately repeated for diversity (best-of-N, ensembles, evals). The docs never gate on this distinction; it's baked into the recommended recipe, not a downstream misuse.",
    "whoActsOnIt": "Any team that copy-pastes Vercel's official caching-middleware recipe verbatim into a production AI SDK app that also does best-of-N sampling, temperature-based ensembling, or repeated-prompt evaluation harnesses — they silently get duplicate cached outputs instead of fresh samples, with no error and (in this specific recipe) no TTL to ever age it out.",
    "estimatedReach": "High — ai-sdk.dev is the official Vercel AI SDK documentation, and the identical code block (same JSON.stringify(params) key, same bare redis.set with no TTL) is duplicated verbatim across both the \"Advanced: Caching\" page and the separate \"Caching Middleware\" cookbook recipe, making it the canonical caching example most AI SDK users would copy first.",
    "implementationNote": "The same doc page's separate \"Lifecycle Callbacks\" example (onEnd + redis.expire(key, 60*60)) does set a 1-hour TTL, but it is a different, non-middleware approach that the candidate did not cite and that still does not scope the cache key to sampling intent — it only bounds blast radius, not the underlying identity-vs-intent conflation."
  },
  {
    "drop": false,
    "name": "Three-party settlement audit for LLM gateway/reseller billing (interconnect-audit transplant)",
    "class": "billing-reconciliation",
    "reasoning": "Source quote confirmed verbatim via raw curl fetch of neon-soft.com: \"Wholesale carrier interconnect agreements with telecom operators entail large traffic volumes. Settlement, traffic, and partner invoices are audited to ensure that revenue sharing and cost recovery are accurate.\" That page is a generic telecom revenue-assurance listicle with zero mention of LLMs, gateways, or resellers — the transplant to LLM spend is entirely the miner's own invention with no corroborating source. The candidate self-describes as \"a downstream audit with no effect on the request stream itself,\" which trivially clears the token/model/thinking bar (it never touches a request), but that triviality is the tell: this isn't a cost-reduction mechanism being shown non-interfering, it's an accounting process with no evidence it is practiced, no quantified savings, and a structural flaw — telecom interconnect settlement relies on peer carriers exchanging CDRs under contractual/regulatory mandate, while an LLM reseller's downstream customer typically cannot obtain \"the underlying provider's own usage report\" at all, since that opacity is usually the commercial point of using a reseller. Only two of the claimed three legs (caller's own log, gateway's invoice) are realistically obtainable in most real arrangements.",
    "verifiedAgainst": "https://www.neon-soft.com/blog/revenue-assurance-audit-a-telecom-operators-guide/ (read 2026-08-30)",
    "provenance": "inferred",
    "telemetrySignal": "A spend audit could observe deltas between a caller's own logged prompt/completion token counts and the gateway/reseller's invoiced token counts per request-id — that comparison is mechanically possible and occasionally done informally today. It cannot observe the claimed third leg: an independent usage report from the underlying provider, since reseller customers generally have no contractual or API access to the upstream provider's own metering for their traffic.",
    "savings": "UNQUANTIFIED — no evidence cited or found of any discrepancy this method has caught or dollar amount it has recovered in an LLM billing context",
    "providers": [
      "all"
    ],
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "settlingExperiment": "Identify one real LLM gateway/reseller relationship (e.g., an enterprise reselling Azure OpenAI, Bedrock-routed Anthropic access, or an OpenRouter-style aggregator) and check contractually and via API whether the downstream customer can obtain the upstream provider's own native usage/billing export for their specific traffic, separate from the reseller's invoice. Then run one billing cycle comparing (a) caller's own token log, (b) reseller invoice, (c) provider-native usage report if obtainable, and record whether a real discrepancy surfaces. If leg (c) is never obtainable in practice, the \"three-party\" framing is false and the entry should be re-adjudicated as a two-party (self-log vs. invoice) check or dropped as not-transplantable.",
    "trap": "Mistaking \"the audit itself doesn't touch the request stream\" (trivially true of any post-hoc accounting process) for evidence the technique is real, practiced, and delivers savings — and assuming telecom's mandated peer CDR-exchange transplants cleanly onto reseller relationships, where the upstream provider's usage report is normally invisible to the reseller's own customer by commercial design.",
    "whoActsOnIt": "FinOps/procurement teams with existing audit rights or dual-access (both reseller invoice and some direct provider visibility) — not applicable to a typical single-gateway customer with no upstream access.",
    "estimatedReach": "Narrow even if valid — requires a customer relationship where three independent data sources are actually obtainable, which is the exception rather than the rule for gateway/reseller LLM spend.",
    "implementationNote": "If pursued, the only reliably obtainable comparison today is (caller's own token log) vs (gateway invoice line items) — a two-party reconciliation, not the three-party settlement-verification pattern claimed."
  },
  {
    "drop": false,
    "name": "MRC GIVT invalid-traffic filtration standard transplanted to LLM spend reporting",
    "class": "cost-reporting/accounting hygiene — not a cost-reduction mechanism",
    "strictVerdict": "FAIL",
    "reasoning": "Source verified verbatim on the raw HTML of pixalate.com: the \"MRC GIVT Definition\" block reads exactly '\"General Invalid Traffic,\" consist[s] of traffic identified through routine means of filtration executed through application of lists or with other standardized parameter checks.' That is a real, MRC-accredited ad-tech standard, but the transplant fails for a reason deeper than the wire-level bar: in ad-tech, GIVT filtration operates UPSTREAM of billing — it defines what counts as a billable impression before the advertiser pays, so filtering it genuinely lowers the invoice. The candidate explicitly disclaims touching \"the request stream itself,\" which means in the LLM case the flagged calls (health-check pings, accidental retries, duplicate sends from a bug) have already been sent to the model, processed, and billed in full by the provider BEFORE this filtration ever runs. Excluding them from an internal spend report afterward changes nothing about what the provider invoices — it only makes the company's own dashboard understate real spend. Zero dollars are saved; only a report number moves. That is not a cost technique, it is an accounting relabeling, and a risky one, since it can mask a live cost-causing bug behind a cleaner-looking chart instead of fixing it.",
    "savings": "None — the request stream (and therefore the provider's bill) is explicitly left untouched by design; only an internal report number changes, not actual spend.",
    "provenance": "inferred",
    "providers": [
      "none observed — speculative transplant; not implemented by any known LLM provider, gateway, or FinOps/observability platform"
    ],
    "telemetrySignal": "A spend audit comparing the LLM provider's own usage/billing dashboard (actual tokens billed) against a company's internally \"GIVT-filtered\" spend report would show a persistent, unexplained gap — provider-billed total exceeding internally-reported total by exactly the volume of health-check/retry/duplicate calls excluded. That gap is itself the signal that the technique produces zero real savings and only suppresses visibility into real spend.",
    "trap": "Borrowed-legitimacy trap: cites a real, MRC-accredited ad-tech billing standard to make a reporting relabeling sound rigorous. In ad-tech GIVT filtration sits upstream of the invoice (it defines what's billable), so filtering there really cuts the bill. Transplanted to LLM spend with the request stream declared off-limits, the same filtration sits downstream of an already-final invoice — it can only edit a report, never the bill. MRC's credibility doesn't cross that upstream/downstream line.",
    "verifiedAgainst": "https://www.pixalate.com/blog/mrc-definitions-sivt-givt-invalid-traffic-detection-filtration (read 2026-08-30)",
    "whoActsOnIt": "FinOps/platform teams building internal LLM spend dashboards who want cleaner numbers by excluding known noise (health checks, retry storms, a buggy duplicate-send path) from what's reported to budget owners — tempting because MRC accreditation makes it sound like an audited standard, but it doesn't change what the provider invoices.",
    "estimatedReach": "Negligible / no known adopters; if adopted, affects only how a company labels its own internal spend dashboard, not any provider's bill.",
    "implementationNote": "The real money-saving move is upstream of this proposal, not equivalent to it: fix the bug causing duplicate sends, add idempotency keys/dedup before the request leaves the client, and stop health-check pings from ever reaching the LLM endpoint. Those touch the request stream and are genuine, evaluable cost techniques (candidates elsewhere in the register for PASS_ABSOLUTE/PASS_SCHEDULING). This candidate explicitly rules that class of fix out, so it can't inherit that credit."
  },
  {
    "drop": false,
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "name": "Freight-style three-way match (invoice / logged-request / logged-response) as an LLM billing audit gate",
    "class": "billing-audit / AP-reconciliation control (not an inference-time cost technique)",
    "reasoning": "Source check: fetched the raw HTML directly and the quote is verbatim — \"That is why many finance, AP, freight audit, and logistics teams rely on a three-way match between the freight invoice, bill of lading, and proof of delivery before approving payment... When they do not match, the exception should be routed for review before payment is released.\" The page (149,666 chars of body text) never mentions LLM, token, GPT, Claude, Anthropic, OpenAI, or 'large language' anywhere — this is a pure freight-logistics article with zero LLM content; the LLM application is 100% the miner's unevidenced analogy. On the bar itself the mechanism is trivially wire-neutral: comparing a provider invoice line item to already-captured local request/response logs happens strictly after the call completes and reads no extra tokens, invokes no model, and changes no reasoning — so 'does the model read a different sequence of tokens / does a different model answer / does a different amount of thinking happen' is unambiguously NO. That alone would clear PASS_ABSOLUTE. But the transplant has a structural flaw the miner didn't flag: freight's three-way match derives its power from the invoice, BOL, and POD being produced by three DIFFERENT parties (carrier, shipper, receiving dock), so no single party's error goes uncaught. Here two of the three 'documents' — the logged request and the logged response — are both self-generated by the same client from the same API call, collapsing this to a two-way check of 'your own copy of the transaction' against the provider's bill, which is also largely redundant with the fact that OpenAI/Anthropic-style responses already return usage.input_tokens/output_tokens in-band. No adopter, no billing-discrepancy rate, and no savings figure is cited anywhere — 'UNQUANTIFIED' per the miner is itself unverifiable since there is no known deployment to measure.",
    "verifiedAgainst": "https://www.ashconversions.com/blog/logistics-document-management/freight-invoice-bill-of-lading-and-proof-of-delivery-how-the-three-way-match-reduces-billing-disputes/ (read 2026-08-30)",
    "provenance": "inferred",
    "telemetrySignal": "A spend audit could observe whether the per-call token counts a client already receives in each response's usage object, summed over a billing period, equal the quantity billed on the provider's invoice/statement — a mismatch would show up as an aggregation delta with no matching logged call. It cannot observe anything about model behavior, since the check runs entirely after the wire transaction and touches no request or response content.",
    "savings": "UNQUANTIFIED — no cited instance of an LLM provider invoice diverging from client-logged usage, so there is no basis to estimate a dispute rate or dollar recovery; likely near-zero for major providers since usage is already returned in-band per response and billing is automated end-to-end.",
    "providers": [
      "all"
    ],
    "estimatedReach": "Unknown / no known adopters. Theoretically any org paying metered LLM API bills could build this themselves; nothing suggests it currently exists as a practiced or productized control anywhere in LLM spend management.",
    "implementationNote": "If built: log each outbound request and inbound response (including the provider-returned usage/token-count fields) locally, then reconcile the monthly invoice's line-item quantities against the sum of locally logged usage before approving payment, routing mismatches for manual review — never modifying the request or response themselves. Note the two self-generated 'documents' (request log, response log) come from the same client and the same call as each other, so this is functionally a two-way match (own records vs. invoice), not an independent three-party corroboration like real freight audit.",
    "trap": "Not one of the six numbered traps (no PR, no repo, no pricing table) — the trap here is the register's implicit seventh: an analogy source from a totally unrelated industry gets cited as if it were evidence for the LLM claim, when the fetched page never mentions LLMs at all. Confirming the quote is verbatim in the source does NOT confirm the LLM application is real or evidenced — those are two separate claims and only the first was checked by the miner.",
    "whoActsOnIt": "FinOps/AP teams at organizations with meaningful direct LLM API spend, only if they build custom per-call logging plus invoice-reconciliation tooling themselves — no LLM provider, gateway, or FinOps vendor is known to offer this as a feature today.",
    "settlingExperiment": "Instrument a real production LLM integration to log every request/response (including each response's in-band usage counts) for a full billing cycle across a large call volume (e.g. tens of thousands of calls on at least one major provider), then reconcile the aggregate against the provider's actual invoice/usage export. If the discrepancy rate is non-trivial and recoverable, the technique has real, quantifiable savings and merits re-adjudication with practitioner-data provenance; if it is ~0 (expected, since usage is already returned per-call and billing is automated), the technique should be marked savings: None and likely dropped as solving a problem that doesn't occur in practice for reputable providers."
  },
  {
    "drop": false,
    "name": "Energy time-of-use / demand-response tariff load-shifting, transplanted as off-peak LLM batch scheduling",
    "class": "transplant",
    "strictVerdict": "INSUFFICIENT_EVIDENCE",
    "reasoning": "FERC's glossary confirms TOU tariffs are a real, well-documented electricity mechanism: \"Time-of-Use (TOU) Rate: A rate where usage unit prices vary by time period, typically greater than one hour within a 24-hour day. TOU rates reflect the average cost of generating and delivering power during those time periods. Daily pricing blocks might include an on-peak, partial-peak, and off-peak price for non-holiday weekdays, with the on-peak price as the highest price, and the off-peak price as the lowest price.\" But that source only proves the electricity-side half of the analogy exists — it says nothing about whether an LLM-side target exists, so I re-verified that half myself instead of trusting the miner. Fetched raw pricing/docs pages: OpenAI's Batch API (via developers.openai.com/api/docs/guides/batch, since openai.com/api/pricing is Cloudflare-gated) is a flat \"50% cost discount\" with a fixed 24h window, not time-of-day conditional; OpenAI's Flex Processing is \"priced at Batch API rates\" with latency varying by spare capacity, i.e. a service-level tier, not a clock-time tariff; Anthropic's, Google Gemini's, and AWS Bedrock's raw pricing HTML each mention only \"batch,\" zero hits for off-peak/time-of-day/peak-hour language. Four major providers, no evidence of a clock-time-conditional rate anywhere. If such a tariff existed, scheduling into it would trivially clear the bar (identical tokens, identical model, only WHEN the request is sent changes — a PASS_SCHEDULING case), but since no provider currently publishes one, there is no live instance to test the bar against, and the absence can't be proven as a universal negative (a provider could ship one tomorrow). That is exactly what INSUFFICIENT_EVIDENCE is for.",
    "verifiedAgainst": "https://www.ferc.gov/sites/default/files/2020-06/2008-glossary.pdf (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit could in principle detect this by correlating request timestamp against realized per-token price in provider invoices/usage exports — a real TOU tariff would show a step-function price drop inside a fixed daily window regardless of workload. As of 2026-08-30 that signal cannot currently be observed: sampled pricing/docs pages for OpenAI, Anthropic, Google Gemini, and AWS Bedrock show flat per-token rates (or a flat batch/flex discount) with no timestamp dependence, so there is nothing for such an audit to find yet.",
    "savings": "None currently realizable — no mainstream provider (OpenAI, Anthropic, Google, AWS Bedrock checked directly) publishes a clock-time-conditional rate to schedule into; existing \"cheap\" tiers (batch, flex) are flat discounts independent of submission time, so there is no off-peak window to exploit.",
    "providers": [
      "OpenAI",
      "Anthropic",
      "Google",
      "AWS Bedrock"
    ],
    "estimatedReach": "0 today — the underlying pricing mechanism this transplant depends on does not exist at any checked provider, so no spend is currently reachable by this technique.",
    "implementationNote": "Do not build a scheduler for this yet — there is nothing to schedule into. If a provider ever announces wall-clock-keyed pricing, the correct implementation would be trivial and safe: delay only fully discretionary/batch jobs into the published window, sending the byte-identical request unchanged, which would land as PASS_SCHEDULING with no bar risk.",
    "whoActsOnIt": "No one, currently — there is no live tariff for a FinOps or eng team to act on. Revisit if a provider's pricing changelog ever adds time-of-day-variable rates.",
    "trap": "Easy to conflate a provider's \"cheap-but-slower\" service tier (OpenAI Flex Processing, batch tiers generally) with a genuine off-peak tariff because both are framed as trading time/latency for cost — but Flex/batch discounts are flat and submission-time-independent, keyed to accepting variable latency or a fixed turnaround window, not to the wall-clock hour the request was sent. Grading this against a live pricing page instead of assuming the electricity-side FERC citation implies an LLM-side equivalent is what prevents that mix-up.",
    "settlingExperiment": "Quarterly (or on provider changelog/pricing-page-diff alerts), re-fetch raw pricing and API docs for OpenAI, Anthropic, Google Gemini, AWS Bedrock, Azure OpenAI, and major resellers (Together, Fireworks, Groq), grepping for off-peak/time-of-day/TOU/peak-hour/wall-clock pricing language. The candidate closes the moment any provider publishes a rate card keyed to wall-clock submission time (distinct from batch/flex service tiers) — at that point, test by sending the byte-identical request inside vs. outside the stated window and confirming the response and token count are unchanged while only price differs, which would settle it at PASS_SCHEDULING."
  },
  {
    "drop": false,
    "name": "Claude Code skill-invocation dedup: identical re-render becomes an \"already loaded\" note instead of a second SKILL.md copy",
    "class": "agent-harness context deduplication",
    "reasoning": "Confirmed verbatim on the cited page via raw curl fetch (not markdown-summarized): \"When Claude re-invokes a skill whose rendered content is identical to the copy already in context, Claude Code adds a short note that the skill is already loaded rather than a second copy of the content.\" That note is injected directly into the conversation the model reads, replacing what would otherwise be a full SKILL.md copy — so the actual token sequence delivered to the model on a repeat invocation is objectively different (and shorter) depending on whether the dedup fires. The bar's deciding question, \"does the model read a different sequence of tokens,\" resolves YES. Every PASS_* tier requires the wire payload to the model be provably unchanged (byte-identical replay, model-invisible metadata, or timing only); this technique's entire value proposition is that the payload changes. It is a real, working harness optimization, not a free no-op, so it FAILs per the bar despite being legitimate and well-documented behavior.",
    "strictVerdict": "FAIL",
    "verifiedAgainst": "https://code.claude.com/docs/en/skills#skill-content-lifecycle (read 2026-08-30)",
    "provenance": "primary-doc",
    "providers": [
      "Anthropic"
    ],
    "savings": "UNQUANTIFIED — magnitude scales with SKILL.md size (the docs describe skill bodies ranging from short snippets to several-thousand-token reference material) and with how often a session identically re-invokes the same already-loaded skill; no measured production figure was found in this check.",
    "telemetrySignal": "A spend audit could observe this by diffing per-turn input-token deltas in a Claude Code transcript: a repeat invocation of an already-loaded skill with unchanged rendered content should show a small flat token delta (the \"already loaded\" note) instead of a delta proportional to the SKILL.md body's size, contrasted against a first invocation or an invocation whose arguments/dynamic-context output changed. No such measured transcript comparison was performed here; this is what would need to be run to quantify it.",
    "estimatedReach": "Claude Code users who re-invoke the same skill multiple times within one session with unchanged arguments and unchanged dynamic-context output — a real but narrow subset of sessions, since most repeat invocations of a skill come with different arguments.",
    "whoActsOnIt": "Nobody — it is automatic, default harness behavior inside Claude Code; there is no user- or team-facing lever to enable, tune, or disable it.",
    "implementationNote": "No action needed or possible — this is unconditional built-in Claude Code harness behavior, not an opt-in technique or configuration flag.",
    "trap": "First-party doc describing internal harness behavior would normally cap at CONTRACTUAL_ONLY when it merely asserts output/response identity we can't independently verify — but this doc sentence describes an actual token-sequence substitution (shorter note replacing full content), not an equivalence claim, so it doesn't even reach CONTRACTUAL_ONLY: it's a straightforward FAIL under the \"different sequence of tokens\" test. Verified against raw HTML via curl, not a markdown/AI-summarized fetch, per trap 3's warning about silently dropped content."
  },
  {
    "drop": false,
    "strictVerdict": "FAIL",
    "name": "Claude Code auto-compaction skill re-attachment (5,000-token/skill cap, 25,000-token shared budget)",
    "class": "context-management",
    "providers": [
      "Anthropic"
    ],
    "provenance": "primary-doc",
    "verifiedAgainst": "https://code.claude.com/docs/en/skills#skill-content-lifecycle (read 2026-08-30)",
    "reasoning": "Fetched the source directly (raw HTML and markdown-rendered) and the quote matches verbatim: \"Claude Code re-attaches the most recent invocation of each skill after the summary, keeping the first 5,000 tokens of each. Re-attached skills share a combined budget of 25,000 tokens... so older skills can be dropped entirely after compaction.\" The bar asks whether the model reads a different token sequence than it otherwise would; here the provider's own documentation says yes, explicitly and by design — skill bodies get truncated to their first 5,000 tokens, and skills beyond the shared 25,000-token pool vanish outright. That is not an equivalence claim needing external verification (which would cap at CONTRACTUAL_ONLY); it is a first-party admission of non-identical post-compaction context. There is no framing under which this clears PASS_* — it is a lossy fallback, not a free-lunch optimization, and the doc's own mitigation advice (\"re-invoke it after compaction to restore the full content,\" line 521) confirms the loss is real and known to Anthropic.",
    "telemetrySignal": "A spend/behavior audit cannot see this from raw API billing alone — input-token counts simply vary turn to turn the way any compaction-driven summarization varies them. It becomes observable only by diffing session transcripts (or Claude Code's own session logs) for skill-body length before vs. after a compaction event: content that was previously multi-thousand-token instructions collapses to a 5,000-token head, or disappears, with no signal in the API response itself distinguishing \"skill dropped\" from \"skill never invoked.\"",
    "savings": "UNQUANTIFIED — Anthropic publishes the cap (5k/skill, 25k/session) but no benchmark of tokens or dollars actually saved versus an uncapped re-attachment. Because the mechanism's effect is truncated or entirely absent skill content post-compaction, it is not a bankable \"savings\" in the register's sense — the trade is fewer context tokens for a materially different (and possibly wrong) model response, which is a correctness cost, not a free efficiency gain.",
    "trap": "None of the six standard traps apply mechanically (no repo, no PR, no pricing table, no wrapped dependency) — the closest hazard is mistaking a documented lossy fallback for a cost-saving technique just because it \"bounds\" token growth; the provider's own doc names the loss (truncation/drop) in the same breath as the bound, so there's no ambiguity to resolve.",
    "whoActsOnIt": "Claude Code users/agents running long sessions that invoke many skills before hitting auto-compaction — anyone relying on skill instructions surviving a compaction boundary should treat re-attached content as unreliable past 5,000 tokens per skill or 25,000 tokens combined, and re-invoke critical skills afterward per Anthropic's own stated mitigation.",
    "estimatedReach": "Every Claude Code session long enough to trigger auto-compaction while multiple skills are active — likely common among power users and agentic/swarm workflows (per this environment's own CLAUDE.md, which layers many mandatory skills into single sessions), though Anthropic publishes no session-count or frequency data to size it precisely.",
    "implementationNote": "If cataloguing this for the register, file it as a known gotcha/limitation entry (FAIL/behavior-changing), not as a technique someone would deliberately adopt for savings — its only actionable content is the mitigation Anthropic already documents: re-invoke a skill after compaction if it's large or several others were invoked after it."
  },
  {
    "name": "Anthropic Memory tool (memory_20250818) — just-in-time context retrieval",
    "class": "agent-memory / context-engineering pattern (not a wire-level cost technique)",
    "drop": false,
    "strictVerdict": "FAIL",
    "reasoning": "The provider's own docs are definitional, not incidental, evidence of a \"yes\" to the disqualifying question: \"Rather than loading all relevant information up front, an agent records what it learns in memory files and reads them back on demand. This keeps the active context focused on the current task, which matters for long-running sessions that would otherwise overwhelm the context window.\" By design the model receives a different, smaller, task-scoped subset of tokens on later turns than it would if everything stayed in context — that is a content change to the wire payload, not metadata, not scheduling, and not a byte-identical replay. It can also change which answer comes back (different context in → different completion out) and adds extra think/tool-call turns for the read/write operations themselves. Even setting the mechanism aside, a docs sentence asserting savings caps at CONTRACTUAL_ONLY at best; here the mechanism itself independently fails the bar, so FAIL is correct, matching the miner's own proposed verdict.",
    "verifiedAgainst": "https://platform.claude.com/docs/en/agents-and-tools/tool-use/memory-tool (read 2026-08-30)",
    "provenance": "primary-doc",
    "telemetrySignal": "A spend audit CAN observe this directly in the transcript: extra tool_use/tool_result turns for memory file read/write calls (ordinarily-billed input/output tokens), and — over a long session — a smaller per-turn input-token count than a naive keep-everything-in-context baseline, because materially different (smaller, curated) content is actually sent to the model on later turns. That visible difference in the token stream is exactly why the technique fails the identical-wire-bytes bar rather than qualifying as a hidden/transparent optimization.",
    "savings": "UNQUANTIFIED on the cited page — no percentage or benchmark figure appears near the \"just-in-time context retrieval\" claim; only illustrative code samples with max_tokens params. Real savings exist only relative to a \"keep full history in every request\" baseline the caller chooses not to use, and are entirely a function of the caller's own read/write policy, not a property of the API call itself.",
    "providers": [
      "Anthropic"
    ],
    "estimatedReach": "Narrow. This is an opt-in beta tool (memory_20250818) that requires the caller to implement and operate their own client-side file storage backend (\"Claude requests file operations, and your application executes them\"). Relevant only to teams building long-running/multi-session agents, not to typical single-turn or short-session API callers.",
    "implementationNote": "The tool is explicitly client-side: Claude emits file-operation tool calls, the caller's application executes and persists them, and the caller controls storage. There is no server-side automatic memory. Correctly bounding what gets read back on each turn is entirely the integration's responsibility — get it wrong (over-read) and you pay full price for redundant context; get it wrong (under-read) and you silently lose needed information versus the full-history baseline. This is an architecture decision with its own token costs, not a drop-in optimization.",
    "trap": "General trap for this whole class of candidate: the provider's docs use the word \"savings\" and \"context window\" explicitly, which makes it read like a caching/efficiency feature, but the mechanism described is the agent's own information-selection policy changing what's in the prompt — the same trap that sinks RAG and subagent-context-offloading submissions. Self-reported design intent on a docs page is evidence of a documented product goal, never evidence of wire-identical request bytes; it should never be mistaken for one and never scores above CONTRACTUAL_ONLY even in cases where the mechanism itself doesn't independently fail (this one does).",
    "whoActsOnIt": "Agent/framework engineers deciding whether to adopt the memory tool for long-running Claude agents. They should evaluate it as a context-architecture and product-quality decision (does the agent behave better across long sessions) and budget its token cost explicitly (extra read/write tool turns, curated-context tradeoffs) — not treat it as a transparent, model-behavior-preserving cost optimization they can bolt on for free."
  }
]